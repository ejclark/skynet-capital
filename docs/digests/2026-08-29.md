# Digest — 2026-08-29

**Period:** 2026-08-24 → 2026-08-29 · **Landed:** 107 commits · **Trigger:** threshold (5 days / 107 commits since last digest)

## Needs you

1. Merge [PR #880](https://github.com/ejclark/skynet-capital/pull/880) — adds one additive `[env]`
   line to `fly.toml` (the envelope-protected carve-out) to pin the ladder-progress-log's storage
   dir on the mounted volume; nothing writes to the store yet, this just lands the container.
2. Rename the GitHub App's display name to "Moneypenny" in GitHub App settings
   (`github.com/settings/apps/<app-slug>`) — whenever convenient, not blocking anything;
   full 2-step instruction is in `docs/MONEYPENNY.md`.

Gist: one real merge click (#880) unlocks the ladder-progress-log container; the rest is
housekeeping you can do on your own schedule.

## Headlines

- The GitHub App has a name and a charter: Moneypenny — `docs/MONEYPENNY.md` defines her domain
  (issue/PR orchestration, sequencing, proactive friction-hunting) and her authority relative to
  governor/postmaster/secretary (subordinates them; still bound by `envelope.json`).
- A feedback-to-shipped latency number exists for the first time: median plan-issue open→closed
  is **0.7 days**, feedback-issue open→closed is **0.1 days** (`scripts/latency-scan.mjs`, #901).
- The merge-automation pipeline that broke 3x same-day (#889→#890→#892) now has a permanent
  regression gate: `workflow-lint.mjs` gained a 6th rule catching a workflow job that runs a
  script needing `node_modules` with no `npm ci` step ahead of it (#903).
- Plan issues that get a `ready`-flip but never get built now get flagged after 48h instead of
  silently vanishing (#902, closing #877's deferred slice 3) — though see the finding below, this
  mechanism has a real gap worth a fast follow-up.
- IA decisions on nav/surface redesigns now have a written-down-first process step in `CLAUDE.md`,
  after three PRs in 2.5 hours re-litigated the same nav placement (#881→#886→#888) (#900).
- Account-level playbook subscriptions shipped: bots can now sub-allocate capital across
  member-authored playbooks (#891, #893).
- A durable, honest multi-leg draft-order review screen landed for the trade ticket (#883).

## Noise absorbed

- 6 friction-fix PRs auto-merged clean on green (#900, #901, #902, #903, plus #898/#899 held for
  and cleared by your manual click).
- 3 same-day pipeline incidents (#889→#890→#892) self-corrected within the hour each time.
- ~14 event-research docs landed this window (PCE/GDP/consumer-confidence/election prints) — all
  carried call sheets + falsifiers per the research contract; none flagged for review here.
- 1 real bug found and **not yet fixed**: live testing shows `isReadySignal` (the plan-ready
  trigger's pattern-matcher) has a **0% hit rate in production** — 0 of 8 real `issue_comment`
  runs since the trigger went live today matched, including Eric's own actual phrasing ("ready —
  use the proposed defaults" on #724, before the trigger existed). The whole-comment regex is too
  strict for natural phrasing. This is being queued as an immediate fix, not left in this digest's
  "needs you" tier — it's a clear bug, not a decision.
