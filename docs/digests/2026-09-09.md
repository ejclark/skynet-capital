# Digest — 2026-09-09

**Period:** 2026-09-08 → 2026-09-09 · **Landed:** 50 commits · **Trigger:** threshold (50 commits since last digest)

## Needs you

1. Re-run the failed `verify` check on PR [#1912](https://github.com/ejclark/skynet-capital/pull/1912)
   — flagged in the last digest, still unaddressed 3 days later. Its only CI run
   ([34043354163](https://github.com/ejclark/skynet-capital/actions/runs/34043354163)) failed
   2026-09-06 and has never been re-triggered; every one of its ~50 sibling research PRs this cycle
   merged within the hour. A green re-run should auto-merge it like its siblings.
2. Merge PR [#2292](https://github.com/ejclark/skynet-capital/pull/2292) — your own workflow-file
   fix (App token signs the event-research re-dispatch instead of `GITHUB_TOKEN`), CI green
   ([34303637515](https://github.com/ejclark/skynet-capital/actions/runs/34303637515)), `hold-merge`
   label holding it per the envelope carve-out. This is the root-cause fix for the 5 recurring
   `build-events` matrix failures `incident-scan.mjs` flagged in the last digest — merging it closes
   the incident. Its own follow-ups note the `docs/LESSONS.md` entry (2026-09-08, covering the 5
   recurrences) still isn't written; worth a `/retro` pass once merged. (`incident-scan.mjs`
   couldn't reach GitHub this run — offline no-op — so this is carried forward from the last digest's
   evidence, not fresh.)
3. Comment `full` or `patch` on issue [#784](https://github.com/ejclark/skynet-capital/issues/784)
   — the Activity-page unification fork, open since 2026-08-28 (12 days now, ~5-PR scope), unchanged
   since the last digest. #2502 (below) shipped a new Activity section on the *old* three-widget
   path while this fork sits open — worth resolving before the page grows a fourth ad hoc widget.
4. Comment-bloat budget breached again (proposal-grade, `scripts/comment-bloat-scan.mjs`, no
   decision needed): narration comments grew to **166** against a committed budget of 44 (up from
   136 last digest) — the 6th consecutive digest with this breach and no cleanup yet. Worst
   offender: `src/server/wire-routes.ts` (7). A cleanup target for whoever picks up debt burn-down
   next, then `--update` to reset the budget.

Gist: one one-click CI retry, one green fix of yours waiting on your own merge, one 12-day-old
design fork, one long-running free cleanup FYI.

## Headlines

- `/trade`'s Symbol field now falls back to a live Alpaca lookup when the curated 369-entry
  directory misses — a new IPO or obscure small-cap (e.g. "GATO") still autocompletes, debounced
  and fail-soft ([#2509](https://github.com/ejclark/skynet-capital/pull/2509)).
- Activity gained a "Council" section — one 280-char thesis line per member per week, visible
  league-wide, resubmission replaces rather than duplicates — shipping shape 1 of issue #2224
  ([#2502](https://github.com/ejclark/skynet-capital/pull/2502)).

## Noise absorbed

- 50 commits landed; 48 were `docs(research)` pulse/initial ledger entries across the Sep 2026 –
  2028 macro/Treasury/Fed/BOE/JGB calendar.
- Config/capability-drift audit (`scripts/config-audit.mjs`): all four sections clean — no orphaned
  capabilities, no owner-claim contradictions, no recurring-intent themes, every agent at or above
  its compute-routing floor.
- 2 new bottleneck issues self-filed and already routed to `next-slice`/no-decision-needed: close-out
  re-dispatch churn on a clock-gated event, 7 forced session deferrals in 36h
  ([#2485](https://github.com/ejclark/skynet-capital/issues/2485)); 3 duplicate event-id pairs
  found in the market-event calendar, all passing `--validate`
  ([#2318](https://github.com/ejclark/skynet-capital/issues/2318)).
- Comms-scan: ~50 PRs measured since the last digest, near-zero merge latency on all but #1912
  (flagged above); the 2 feature PRs above carried real screenshots (not waived), zero Eric comments
  or reactions logged on the research volume — expected.
- 2 dependabot PRs still open since 09-08 (#1985 dev-deps group, #1984 `actions/cache`), unchanged,
  not yet picked up.
