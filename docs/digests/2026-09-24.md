# Digest — 2026-09-24

**Period:** 2026-09-23 → 2026-09-24 · **Landed:** 50 commits · **Trigger:** threshold (50 commits since last digest)

## Needs you

1. Grant the Skynet Envoy GitHub App **Contents: Read and write** — GitHub → Settings → Developer
   settings → GitHub Apps → Skynet Envoy → Permissions, then accept the permission update on the
   installation. — The conflict-repair lane (`moneypenny-repair.yml`) mints its App token correctly
   now (fixed 2026-09-19), but the *installation itself* only grants read: live-tested on #3435,
   `git push` gets `403` even with the right token. No conflict on any PR is currently
   self-resolvable by this lane until the permission changes; a credential grant, so nothing in-repo
   can fix it ([#3334](https://github.com/ejclark/skynet-capital/issues/3334), reopened 2026-09-22).
2. Merge PR [#2292](https://github.com/ejclark/skynet-capital/pull/2292) — the App-token fix for the
   event-research re-dispatch, CI green (`mergeable_state: clean`), `hold-merge` label holding it per
   the envelope carve-out, now **15 days** untouched. Fixes the re-dispatch-token bug in
   `docs/LESSONS.md`; merging clears the oldest item in the protected-path queue.
3. Answer the step-4 fork on plan [#3194](https://github.com/ejclark/skynet-capital/issues/3194) —
   steps 1–3 shipped and behavior-inert; a follow-up correction landed the dial as
   mode-keyed (`exitSafety[mode]`, tied to the existing conservative/standard/aggressive preset
   rather than a second axis). Step 4 is still the first PR in the repo that can autonomously close a
   position: is the mode-keyed shape right, or a different trigger/shape? Open since 2026-09-17,
   unchanged since 2026-09-20.
4. Answer the slice-2 fork on bottleneck [#1343](https://github.com/ejclark/skynet-capital/issues/1343)
   — "yes, board lane-authored protected changes too" (reviewed as one ledger line, same as
   session-authored items today) vs. "no, they keep their own held PR" (closes the issue as-is).
   Re-posted 2026-09-22, still open; slice 1 (session/governor platter) is live.
5. **Still unlearned — repeat from the last digest.** Run a `/retro` pass on the same three
   Moneypenny Events failures (`incident-scan.mjs`, shas `2847f66`/`dd036e1`/`7973483`,
   [#1421](https://github.com/ejclark/skynet-capital/issues/1421)) — the App-installation rate-limit
   root cause flagged 2026-09-23 has now sat one full digest cycle with no `docs/LESSONS.md` entry.
6. Comment-bloat budget still breached (proposal-grade, `scripts/comment-bloat-scan.mjs`, no decision
   needed): narration comments now at **325** against a committed budget of 44 — up from 303 one
   digest ago. Worst offenders: `src/playbooks/playbook.ts` (11), `src/trading/round-trips.ts` (10),
   `src/domain/market-events-data.ts` (9), `src/server/dashboard-server-config.ts` (9). A cleanup
   target for whoever next picks up debt burn-down, then `--update` to reset the budget.

Gist: one credential grant, one one-click PR merge, two open design forks (autonomous-trading exit
mechanism, protected-path batching policy), one incident still waiting on its retro a cycle later,
one long-running free cleanup FYI.

## Headlines

- The Accounts surface got a full rebuild: net-worth card, form strip, league card, allocation/
  greeks view, plain-language positions and glossary, positions search and lenses (list/map/expiry),
  a "needs a decision" card, a phone-first overview, an all-accounts roster, and a bot heartbeat chip
  on every tab — 13 PRs
  ([#3692](https://github.com/ejclark/skynet-capital/pull/3692)–[#3707](https://github.com/ejclark/skynet-capital/pull/3707)).
- The Decisions tab retired into Activity and Heartbeat — every bot trade now opens to the decision
  that placed it, and the decisions trail pages through full history instead of just the newest 30
  passes ([#3705](https://github.com/ejclark/skynet-capital/pull/3705)–[#3707](https://github.com/ejclark/skynet-capital/pull/3707)).
- A market clock landed in the topbar (time left to trade on every route), and playbook performance
  now splits the viewer's own accounts from the house aggregate
  ([#3690](https://github.com/ejclark/skynet-capital/pull/3690),
  [#3679](https://github.com/ejclark/skynet-capital/pull/3679)).
- Every autonomous decision pass now records each playbook's verdict, and the trade-stats family
  grew the playbook metrics Eric had listed
  ([#3691](https://github.com/ejclark/skynet-capital/pull/3691),
  [#3671](https://github.com/ejclark/skynet-capital/pull/3671)).

## Noise absorbed

- 50 commits landed: 28 `docs(research)` pulse/ledger entries (mostly deterministic screens and
  scheduled-event pulses, no stance changes), 22 feature/fix commits.
- Config/capability-drift audit (`scripts/config-audit.mjs`): all four sections clean.
- Doctrine-scan (`scripts/doctrine-scan.mjs --due`): nothing due.
- Incident-scan: same 3 unlearned incidents as last digest (folded into Needs-you above as a
  repeat, not a new item) — no new unlearned failures since.
- Bottleneck [#3279](https://github.com/ejclark/skynet-capital/issues/3279) (sibling-repair
  collision) has a completed call sheet recommending a small prompt-file PR; that PR has not been
  opened yet. The call sheet itself says opening it needs no decision — flagging for visibility,
  not a Needs-you item.
- 10 dependabot PRs still open, none reviewed since last digest; [#1984](https://github.com/ejclark/skynet-capital/pull/1984)
  (`actions/cache` 4→6, a major bump) remains correctly escalated `needs-eric` since 09-08.
- 11 orphaned `[ci] Moneypenny Events` issues (#3215–#3220, #3229–#3233) from 2026-09-17 are
  unchanged — mechanical rework for a session to pick up, not a new decision.
