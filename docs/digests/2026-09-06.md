# Digest — 2026-09-06

**Period:** 2026-09-05 → 2026-09-06 · **Landed:** 55 commits · **Trigger:** threshold (50 commits since last digest scan)

## Needs you

1. Comment `ready` on issue [#1442](https://github.com/ejclark/skynet-capital/issues/1442) — still
   sitting on yesterday's ask. You already confirmed the direction in chat on 2026-09-04
   ("i don't know how much of a pillar this has become but it feels like we should move towards
   that direction"); the flip starts slice 1 (research only, no code) into what a trusted eval
   suite would need to show before `envelope.json`'s mechanical gate can relax.
2. Two design calls on issue [#1671](https://github.com/ejclark/skynet-capital/issues/1671)
   (extending the trade ladder to 400/500): **(a)** the wheels-off door — keep the one-click
   bypass button, remove it (seeding-rule only), or gate it behind a typed acknowledgment; **(b)**
   whether the 400 rung is vertical spreads only, or verticals + iron condors as two rungs. Both
   are named with a recommendation in the issue body.
3. Comment-bloat budget breached again (proposal-grade, `scripts/comment-bloat-scan.mjs`, no
   decision needed): narration comments grew to **124** against a committed budget of 44 (up from
   105 in the last digest). Worst offenders: `src/trading/round-trips.ts` (5),
   `src/domain/market-events-data.ts` (4), `src/observatory/activity-event.ts` (3),
   `src/observatory/playbook-store-json-view.ts` (3), `src/observatory/research-json-view.ts` (3).
   A cleanup target for whoever picks up debt burn-down next, then `--update` to reset the budget.

Gist: one cheap ready-flip, two named design calls, one free cleanup FYI.

## Headlines

- The protected-path platter ([#1757](https://github.com/ejclark/skynet-capital/pull/1757))
  merged while this digest was being assembled — it retires three live incidents at once: the
  `Set up flyctl` 403 that had been red on `main`
  ([#1561](https://github.com/ejclark/skynet-capital/issues/1561)), the false-red repair loop that
  fired five times overnight on finished research work (`docs/LESSONS.md`, the max-turns entries),
  and raises the research fan-out cap.
- The conflict-repair backlog that needed a call in two straight digests is gone: the 15 PRs stuck
  `conflict-flagged` against a fast-moving `main` are down to **zero** open today.
- A retro closed the loop on 30 unlearned CI incidents in one PR
  ([#1756](https://github.com/ejclark/skynet-capital/pull/1756)) — a Fly-volume preflight check, a
  GitHub 5xx retry, and the workflow platter absorbing bursts of research sessions account for the
  bulk of them; 7 root causes fixed at once.
- A pre-push hook now refuses pushing to a branch whose PR already merged and whose upstream
  vanished — closes an orphan-commit pattern that hit twice in one night
  ([#1765](https://github.com/ejclark/skynet-capital/pull/1765)).
- `docs/PATTERNS.md` picked up its first real rows, seeded straight from shipped research phone
  frames ([#1758](https://github.com/ejclark/skynet-capital/pull/1758),
  [#1763](https://github.com/ejclark/skynet-capital/pull/1763)).
- Last digest's stuck-push concern (permission denied on research PRs #1286/#1428) did not recur —
  both merged clean this cycle, along with #1434.

## Noise absorbed

- 51 commits landed; 43 were `docs(research)` ledger entries (initial/pulse) across the
  Sep 2026 – Mar 2027 macro/housing/Fed/BoJ calendar.
- Config/capability-drift audit (`scripts/config-audit.mjs`): all four sections clean — no orphaned
  capabilities, no owner-claim contradictions, no recurring-intent themes above threshold, every
  agent at or above its compute-routing floor.
- Comms-scan: ~50 PRs measured since the last digest, near-zero merge latency on nearly all, zero
  Eric comments or reactions logged — expected for routine research-ledger volume.
- Last digest's duplicate-note ask (which of #1434's or main's version survives for
  `treasury-buyback-20y30y-2026-10-08`) resolved itself without needing your call: a later PR
  (#1443) synthesized both findings into one doc. The `needs-eric` label still sitting on merged
  PR #1434 is stale, not a live ask.
- 1 dependabot PR (#1337) open since 09-04, unremarkable volume, not yet picked up.
- 2 self-healed Moneypenny Events CI issues from last digest (#1421, #1390) stayed green; no new
  recurrence.
