# Digest — 2026-09-01

**Period:** 2026-08-30 → 2026-09-01 · **Landed:** 49 commits · **Trigger:** threshold (50 commits since last digest)

## Missed-interrupt retro flag

`release · deploy` (Pipeline → **Deploy to Fly**) failed **8 times in a row**, 2026-08-31
01:01–04:02 UTC — every push to `main` in that ~3-hour window failed to deploy
(`authentication handshake failed: EOF` against Fly's API; [issue #1001](https://github.com/ejclark/skynet-capital/issues/1001)
tracked each recurrence live). It self-healed — every Pipeline run on `main` since has been
green (confirmed against the last 15 runs, none touching the deploy step) — and no code fix
was needed. But nothing surfaced this to you while it was happening; the digest is the first
time it reached you. Nothing to do now — it's resolved and #1001 is still open only as a stale
receipt — but a 3-hour deploy outage is exactly the class of thing that should interrupt in
real time, not wait two days for a digest.

## Needs you

1. Comment `ready` on [issue #894](https://github.com/ejclark/skynet-capital/issues/894) — you
   already approved this plan in the 2026-08-30 digest (a dry-run harness for the merge
   pipeline); the flip itself is the only thing still missing, so it hasn't started building.
2. Four research pulses landed on the same calendar entry as a concurrent branch and are stuck
   in a same-logic merge conflict Moneypenny won't guess through — each already carries a
   worked suggested resolution; comment `go` on any you accept, or say which side should win:
   - [PR #1000](https://github.com/ejclark/skynet-capital/pull/1000) (cpi 10-14 D-44) — two
     independent D-44 pulses disagree on which way Brent moved and what it implies. Suggested:
     append this pulse as a new row that supersedes #988's rather than editing it in place.
   - [PR #1012](https://github.com/ejclark/skynet-capital/pull/1012) (import/export prices
     11-17) — two branches filed the same calendar id from different BLS source pages.
     Suggested: keep one entry, `notes` naming both discovery paths.
   - [PR #1027](https://github.com/ejclark/skynet-capital/pull/1027) (fomc minutes 11-18) — this
     branch's entry is `estimate`; `main`'s (via #1032) is already `confirmed` off two re-fetched
     primaries. Suggested: main's confirmed entry wins; drop this branch's duplicate.
   - [PR #1068](https://github.com/ejclark/skynet-capital/pull/1068) (waller 9/3) — same event,
     two different ids. Suggested: keep `main`'s id (already referenced by a landed doc), fold
     this branch's two unique facts into its `notes`.
3. **Comment-bloat budget breached** (proposal-grade, from `scripts/comment-bloat-scan.mjs`):
   narration comments grew to 46 against a committed budget of 44. Worst offender:
   `src/participants/participant-store.ts` (3 flagged). No action needed from you — this is a
   cleanup target for whoever picks up debt burn-down next (keep only comments stating a
   non-obvious WHY, delete the rest, then run `--update` to reset the budget).

Gist: one repeat ready-flip, four small editorial calls on duplicate calendar entries, and one
FYI about a comment-cleanup target — nothing urgent-blocking.

## Headlines

- 43 event-research pulses landed across the Sep–Dec 2026 macro corridor — CPI, FOMC (meetings,
  minutes, blackouts), PPI, jobs, ISM ×2, Beige Book, Treasury auctions/refunding, OPEC+, plus
  AVGO/GOOG/META/CRWV earnings — every one carrying a call sheet and dated falsifier.
- The feedback-build lane's model routing landed in two steps: default flipped from always-Opus
  to Sonnet with signal-based Opus escalation (#1004 — issue #981's 3-criteria build ran ~98%
  agent time in 18.5 minutes as the proof case), then a third Haiku tier was added for
  single-criterion asks (#1052).
- The observatory's shell (nav/theme chrome) is now extractable as a standalone design system,
  rendering offline straight from `theme.css`/`gate.css` (#1062).
- Eric's own feedback-form ask — image upload + custom title on the AI-assisted path (#1020) —
  shipped and closed same day (#1037).
- Ladder field colors deepened twice for readability (#1053, #1046).

## Noise absorbed

- 46 PRs auto-merged clean on green by the bot; 3 merged directly by Eric (#1062, #1037, #1004).
- 43 research pulses ran with no stance change requiring review (the 4 flagged above are stuck
  on merge conflicts, not review).
- Comms-scan: 50 PRs measured since the last digest, ~0h merge latency on nearly all (one
  outlier at 9.5h, #1036), zero Eric comments or reactions logged on any — expected for a
  stretch of routine research-ledger PRs.
- Config/capability-drift audit (`scripts/config-audit.mjs`): all four sections clean — no
  orphaned capabilities, no owner-claim contradictions, no recurring-intent themes above
  threshold, every agent at or above its compute-routing floor.
