# Digest — 2026-09-26

**Period:** 2026-09-24 → 2026-09-26 · **Landed:** 50 commits · **Trigger:** threshold (50 commits since last digest)

> [!WARNING]
> **Missed-interrupt retro item.** The fix this digest asked you to merge two days ago (#2292,
> merged 2026-09-24 22:58) broke the exact lane it was meant to repair, and it has been broken for
> the full period since — nobody was told until now. Root-caused below (Needs you #1). Every
> `docs(research)` commit stopped landing the moment it merged: 28 in the prior period, **zero**
> since.

## Needs you

1. **Say go on the one-line fix for the event-research outage** — `allowed_bots: "github-actions"`
   on `.github/workflows/moneypenny-events.yml:485` needs `skynet-envoy` added (or diffed for a
   revert of #2292's identity change). Root cause, read off the job log directly: #2292 switched
   the event-research re-dispatch step's signing identity from `GITHUB_TOKEN` (actor
   `github-actions`) to the App token (actor `skynet-envoy`) so the repair lane could see the
   dispatch — but the `research due events` job's own `claude-code-action` step still only
   allow-lists `github-actions` as a legitimate bot actor (its own comment, lines 479-484, names
   `github-actions` as "the one and only bot that can ever legitimately land here" — now stale).
   Every matrix leg since the merge has hit `##[error]Action failed with error: Workflow initiated
   by non-human actor: skynet-envoy (type: Bot)` and died in ~3 seconds, before Claude Code ever
   starts. Verified: `incident-scan.mjs` shows 47 unlearned failures, every one dated 2026-09-25 or
   2026-09-26 (i.e. only after the merge); 21 are still open as auto-filed `[ci] Moneypenny Events`
   capsule issues; zero `docs(research)` commits landed this entire period. — Event research has
   been fully down for 24+ hours; the fix is a one-line workflow-file diff, always your merge.
2. Grant the Skynet Envoy GitHub App **Contents: Read and write** — same ask as last digest,
   unchanged. GitHub → Settings → Developer settings → GitHub Apps → Skynet Envoy → Permissions,
   then accept the installation update. Blocks the conflict-repair lane
   ([#3334](https://github.com/ejclark/skynet-capital/issues/3334), reopened 2026-09-22, still
   open) from pushing its own resolved conflicts.
3. Answer the step-4 fork on plan [#3194](https://github.com/ejclark/skynet-capital/issues/3194) —
   unchanged since 2026-09-20. Steps 1–3 shipped, behavior-inert. Step 4 (graduated exit-safety
   actually firing, keyed to `exitSafety[mode]`) is the first PR in the repo that could
   autonomously close a position: is that shape right, or a different trigger?
4. Answer the slice-2 fork on bottleneck [#1343](https://github.com/ejclark/skynet-capital/issues/1343)
   — unchanged since 2026-09-22. "Yes, board lane-authored protected changes on the platter too"
   (one ledger line, same as session-authored items today) vs. "no, they keep their own held PR"
   (closes the issue as-is).
5. Comment-bloat budget still breached (proposal-grade, `scripts/comment-bloat-scan.mjs`, no
   decision needed): narration comments now at **356** against a committed budget of 44 — up from
   325 one digest ago. Worst offenders: `src/playbooks/playbook.ts` (11),
   `src/server/dashboard-server-config.ts` (11), `src/trading/round-trips.ts` (10),
   `src/domain/market-events-data.ts` (9). A cleanup target for whoever next picks up debt
   burn-down, then `--update` to reset the budget.

Gist: one live outage from a fix you merged two days ago (one-line revert-or-allowlist, needs your
merge either way), one still-open credential grant, two open design forks (autonomous-trading exit
mechanism, protected-path batching policy), one long-running free cleanup FYI.

## Headlines

- The `/mermaid` skill landed: 24 reference cards, a delta-marks grammar v2, and every diagram in
  every PR is now parsed against GitHub's own Mermaid version before it ships — no more broken
  charts discovered after merge.
- The architecture storybook shipped — one C4 page per container, each generated and refuted
  against the code it cites, plus a before/after "shoot" script for showcase-quality diagram pairs.
- Plan issues gained a **state block** (#3766/#3767): a machine-readable context store on the issue
  itself, so `/work-issues` and `/secretary` read current status from one block instead of
  replaying the whole comment thread.
- The Guidance tab shipped actions for calls you've sold — keep, buy back, roll, or let go — with
  spot-disagreement counts and the research view's buy line stated in plain words.
- `ship.sh` now falls back to the session's own REST route for arming auto-merge and promoting
  drafts when a cloud session's proxy refuses GraphQL — closes the gap the held-PR bug (#3735/#3736)
  found two days ago, on the same day it was found.

## Noise absorbed

- 50 commits landed, all feature/process/docs work — zero `docs(research)` pulse commits this
  period (28 in the prior period); see Needs-you #1, this is the outage's own signature, not a
  quiet week.
- Config/capability-drift audit (`scripts/config-audit.mjs`): all four sections clean.
- Doctrine-scan (`scripts/doctrine-scan.mjs --due`): nothing due.
- 21 auto-filed `[ci] Moneypenny Events` capsule issues are open, all the same root cause as
  Needs-you #1 — not separately actioned, they close on that one merge.
- All PRs from last digest's queue are closed, including the 10 open dependabot PRs and
  [#1984](https://github.com/ejclark/skynet-capital/pull/1984) — 0 open PRs repo-wide as of this
  digest.
- Bottleneck [#3279](https://github.com/ejclark/skynet-capital/issues/3279) (sibling-repair
  collision) unchanged — call sheet still open, no PR yet; visibility only, no decision needed.
