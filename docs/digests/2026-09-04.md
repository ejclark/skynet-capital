# Digest — 2026-09-04

**Period:** 2026-09-01 → 2026-09-04 · **Landed:** 97 commits · **Trigger:** threshold (97 commits since last digest)

## Needs you

1. Release the stuck member submission — issue #1028: run `node scripts/moneypenny/index.mjs
   --release feedback-1020` (or delete git tag `refs/tags/claim/feedback-1020`) and re-toggle the
   `feedback` label on [issue #1020](https://github.com/ejclark/skynet-capital/issues/1020) — a
   member's feedback has sat behind a silently-stalled build session (zero comments beyond the
   receipt) for 4 days, with no self-healing retry.
2. Say how to clear the growing research-PR conflict queue — 12 PRs are stuck in the same
   same-logic merge-conflict class the 09-01 digest flagged (that digest's 4 — #1000, #1012,
   #1027, #1068 — are all *still* open and unresolved 3 days later) plus 8 new ones since:
   #994, #1112, #1115, #1141, #1142, #1144, #1197, #1201. Comment `go` on any you want merged
   as-drafted, or say `automate` if you want a future build to extend #894's dry-run-harness idea
   into auto-resolving this specific append-not-overwrite conflict shape.
3. `fly volumes create` for a second, bots-only volume on `skynet-capital-bots` — [issue
   #1181](https://github.com/ejclark/skynet-capital/issues/1181): unlocks durable
   momentum/sentiment/cooldown state across deploys; the code already shipped dark, waiting only
   on this.
4. Decide [issue #666](https://github.com/ejclark/skynet-capital/issues/666) (the owner
   ops-status panel) — give it a Fly read token, or say "degraded mode" to ship the credential-free
   version instead. Open since 08-26 with no call made yet.
5. Comment `ready` on [issue #894](https://github.com/ejclark/skynet-capital/issues/894) (the
   merge-automation dry-run harness) — this is the *third* digest carrying this ask (08-30, 09-01,
   now); the flip is still the only thing missing.
6. Comment-bloat budget breached (proposal-grade, from `scripts/comment-bloat-scan.mjs`):
   narration comments grew to 66 against a committed budget of 44. Worst offenders (3 each):
   `src/observatory/activity-event.ts`, `src/participants/participant-store.ts`,
   `src/server/owner-link-store.ts`. No action needed from you — a cleanup target for whoever
   picks up debt burn-down next (keep only comments stating a non-obvious WHY, delete the rest,
   then run `--update` to reset the budget).

Gist: one member-facing stall to unstick, a growing conflict queue that needs a process call, two
infra/credential decisions, one repeat ready-flip, and one free cleanup FYI.

## Headlines

- Sauron's dead key is fixed — bots now rotate their Alpaca credentials live through the internal
  controls bridge with zero restart and zero lost state, security-reviewed and merged (#1200);
  #1180's only remaining step is the teardown PR once a live rotation is proven.
- Bot momentum/sentiment/cooldown state now survives a restart — ships dark until the Fly volume
  from #1181 (above) lands (#1203).
- Moneypenny's chat rail shipped end to end: she drafts filings and answers live inside the
  companion chat, and the old feedback modal is gone, replaced by an accordion onboarding guide
  (#1169, #1151, #1138).
- The observatory reshaped: a fluid shell that fills the window, one unified activity event bus
  replacing the old ledgers, and profile-centric nav (#1179, #1219, #1149).
- CI stabilized after a day of flaky installs — `mise` is now the single Node-version source of
  truth, install duration is a tracked fitness dimension, and npm caching moved to restore-keys
  (#1226, #1216, #1217).

## Noise absorbed

- 51 research-ledger PRs landed (initial/pulse/close-out) across the Sep–Dec 2026 macro corridor;
  2 forward-tests scored against live prints (FT-47/48, ISM services).
- 12 CI/pipeline fixes shipped the same day the flake surfaced; 2 brief pipeline failures
  (12-second fast-fails, mid CI-tooling churn) both self-healed — every run since is green.
- Comms-scan: 82 PRs measured since the last digest, near-zero merge latency on nearly all
  (outliers: #1150 at 13.3h, #1230 at 7.5h — both eric-token-lane research PRs), zero Eric
  comments or reactions logged on any — expected for a stretch of routine research-ledger and
  autonomous-lane volume.
- Config/capability-drift audit (`scripts/config-audit.mjs`): all four sections clean — no
  orphaned capabilities, no owner-claim contradictions, no recurring-intent themes above
  threshold, every agent at or above its compute-routing floor.
- `serve-dashboard.ts` wiring extracted into account/desk-trading modules (#1231, decomposer
  output).
