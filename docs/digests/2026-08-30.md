# Digest — 2026-08-30

**Period:** 2026-08-29 → 2026-08-30 · **Landed:** 47 commits · **Trigger:** threshold (5 commits since last digest)

## Needs you

1. Comment `ready` on [issue #894](https://github.com/ejclark/skynet-capital/issues/894) — green-lights
   a plan to add a dry-run harness for the merge-automation pipeline (the class of bug behind
   #889→#890→#892's same-day triple patch); it touches workflow files, so it can never auto-merge
   regardless.
2. Decide the trade-off on [issue #666](https://github.com/ejclark/skynet-capital/issues/666) — the
   owner ops-status panel (bots up/down, glanceable from a phone) needs either a Fly read token, or
   your sign-off on a credential-free degraded mode that shows less but holds no infra key. Either
   answer unblocks it; no wrong choice.

Gist: two decisions, neither urgent-blocking — a plan ready-flip and a scope/credential call on a
phone dashboard.

## Headlines

- Moneypenny (the GitHub App) is fully renamed end-to-end — code, workflows, GitHub's own UI — and
  now self-repairs: a PR that goes conflicted against `main` gets a dispatched repair session that
  merges and resolves it, or escalates to you only when the conflict genuinely isn't safe to guess
  (#909–#912, #924). Exercised live this session: PR #947 hit exactly that escalation (two research
  pulses independently proposed the same calendar entry) and got resolved and merged.
- The event-research pipeline's direct-push-to-`main` bug — the one that caused 10 duplicate
  "Moneypenny Events" failures in a day — is fixed (screens now land via PR, never a raw push,
  #942/#968) and fully retro'd, including a scanner gap that was letting duplicate pre-fix failures
  re-flag forever after the root cause was already fixed (this session, #973).
- The only code allowed to turn a raw broker credential factory into a usable client is now isolated
  to one file (`account-identity-gate.ts`) — the rest of the trading surface (guards, playbooks,
  Alpaca clients) is ordinary contribution surface again, no longer envelope-protected (#928 slice 3).
- Ladder milestones are now earned from real account activity instead of manual checkboxes (#938).
- 19 event-research pulses landed across the September macro corridor (CPI, FOMC, jobs, ISM ×2,
  Beige Book, Treasury auctions) plus AAPL/AMZN/MU/AVGO earnings prints — every one carrying a call
  sheet and a dated falsifier per the research contract, none flagged for review here.

## Noise absorbed

- ~40 structural/CI/feature PRs auto-merged clean on green.
- 1 merge conflict auto-repaired by the new CI Medic lane; 1 more (PR #947) needed a one-line
  editorial call (duplicate calendar entry, no info lost either way) and got resolved directly.
- 10 duplicate incident recurrences closed in one ledger fix, down from a standing budget breach.
- 19 research pulses ran with no stance changes requiring review.
- 2 unrelated small fixes shipped clean: TS `target`/`lib` bumped to ES2025 + a missing incremental
  build cache added (#970), on top of the retro fix above (#973).
