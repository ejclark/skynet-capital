# Digest — 2026-08-24

**Period:** 2026-08-22 → 2026-08-24 · **Landed:** 33 merged PRs · **Trigger:** threshold (11 commits queued the check; 33 PRs actually landed once counted)

## Needs you

1. Comment "ready" on [#467](https://github.com/ejclark/skynet-capital/issues/467), [#468](https://github.com/ejclark/skynet-capital/issues/468), [#469](https://github.com/ejclark/skynet-capital/issues/469) — the beta-ladder plan set (trading companion, options order path, 100→200→300 unlocks). All three were re-triaged this period: stale premises corrected (the options path already shipped in #446; a live P/L bug was found and fixed in #499), and each body now names the exact protected-path edits (`trade-service.ts`, `option-trade-service.ts`, `authenticator.ts`) it needs pre-approved at flip time.
2. Comment "go" on [#461](https://github.com/ejclark/skynet-capital/issues/461) — the Desk Chassis v2 handoff. Its trade-ticket half already shipped in #446; only the chassis itself (net-worth hero, treemap, unified event bus) remains, and it's been waiting on your word since the 2026-08-21 migration.
3. Comment "go" on [#462](https://github.com/ejclark/skynet-capital/issues/462) to have the two `/guide` renders built (flame-as-bundled vs. flattened-to-Nocturne-teal) — its own first acceptance criterion is your 2026-08-17 "show me both first" ruling, so nothing else in that handoff can start until you pick.

These three were themselves invisible to every queue and digest until this period — a label bug (`idea` used for both member intake and owner-authored build contracts) hid them; self-caught and fixed below.

## Headlines

- **Owner-link trading is live** — #472 ships per-member linked trading and removes the `SKYNET_DESK_TRADING` kill switch, closing out your 2026-08-21 ruling on #466.
- **A live 100× P/L bug in options is fixed** — #499: the history/analysis tabs were computing realized option P/L per-share instead of per-contract (a $300 move showed as $3.00), while the positions tab had it right all along.
- **The feedback lane got more autonomous and cheaper** — the build envelope widened and the model now routes by who pays, closing a spend loophole (#474); the coach fills the "where in the app" field itself instead of dropping it (#502); a filed issue is now correlated back to what the member actually typed (#506).
- **The design lane shipped** — issues can now arm a Claude Design build with a paste-and-fill contract, gated on its own branch (#512–#516); the brand palette dropped from five pasted copies to one definition in `src/ui/tokens.ts` (#511); every observatory page is now fluid, capped at 2.5k (#509).
- **Postmaster reliability hardened** — CI medic restored and 8+ lease/tag/field bugs fixed so feedback builds stop silently stalling (#481, #483–#498).

## Noise absorbed

- Config/capability-drift audit (`config-audit.mjs`): clean — 0 orphaned capabilities, 0 contradicting routes, 0 recurring-intent themes, 0 compute-floor violations.
- 33 PRs auto-merged this period, mostly self-correcting postmaster/lease/lane fixes.
- 2 handoffs (#461, #462) self-caught mislabeled (`idea` → `handoff`) and corrected same-day; the underlying registry gap (5 active labels missing from `postmaster.mjs`, `issue-lint` never checking labels) filed as #500.
- 1 real-time Q&A resolved without a build: you asked on #429 whether feedback storage needs a Fly database — answered same-session (no, the existing volume covers it) with no action needed from you.
- `docs/BACKLOG.md` retired to a pointer; the two trophies it over-promised (first to +50%, biggest single-day gain, longest green streak) re-filed as a scoped next-slice issue (#503) instead.
- 3 event-research updates: a consumer-confidence pulse check (no stance change, still no August consensus), treasury-20y-bond closed out, and the AI-hardware-constraints doc restructured to lead with a call sheet plus a dated calendar row.
