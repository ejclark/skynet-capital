# Digest — 2026-09-23

**Period:** 2026-09-09 → 2026-09-23 · **Landed:** 50 commits · **Trigger:** threshold (50 commits since last digest)

## Needs you

1. Merge PR [#2292](https://github.com/ejclark/skynet-capital/pull/2292) — the App-token fix for the
   event-research re-dispatch (`GITHUB_TOKEN` → App token), CI green since 2026-09-09, `hold-merge`
   label holding it per the envelope carve-out, completely untouched for 14 days. It fixes the
   re-dispatch-token bug documented in `docs/LESSONS.md` (a different root cause from item 4 below);
   merging it clears the oldest item in the protected-path queue.
2. Answer the step-4 fork on plan [#3194](https://github.com/ejclark/skynet-capital/issues/3194)
   — "is the mode-keyed `exitSafety[mode]` scoped force-flatten shape right, or do you want a
   different trigger/shape?" Steps 1–3 (characterization specs, additive schema, isolation/determinism
   audits) are shipped and behavior-inert; step 4 is the **first PR in the repo that can autonomously
   close a position**, and the session correctly stopped rather than guess. Live since 2026-09-17,
   re-confirmed still open on 2026-09-20.
3. Answer the slice-2 fork on bottleneck [#1343](https://github.com/ejclark/skynet-capital/issues/1343)
   (platter PR) — "yes, board lane-authored protected changes too" (reviewed as one ledger line, same
   as session-authored items today) vs. "no, they keep their own held PR" (closes the issue as-is).
   Either is a one-time call. Posted 2026-09-22, still open; slice 1 (session/governor platter) has
   been live and used twice since.
4. Run a `/retro` pass on the 2026-09-22 Moneypenny Events failures (`incident-scan.mjs`, shas
   `2847f66`/`dd036e1`/`7973483`, tracked on [#1421](https://github.com/ejclark/skynet-capital/issues/1421))
   — a genuinely new root cause, not a repeat: the circuit breaker's own pre-flight `gh run list` call
   is now hitting an **installation-level** GitHub API rate limit (`HTTP 403`), distinct from every
   prior Moneypenny-Events entry in `docs/LESSONS.md` (2026-09-11, 2026-09-17). Confirmed unlearned.
5. Comment-bloat budget breached again (proposal-grade, `scripts/comment-bloat-scan.mjs`, no decision
   needed): narration comments grew to **303** against a committed budget of 44 — up from 166 two
   digests ago, roughly doubling in two weeks. Worst offenders: `src/playbooks/playbook.ts` (10),
   `src/trading/round-trips.ts` (10), `src/domain/market-events-data.ts` (9). A cleanup target for
   whoever next picks up debt burn-down, then `--update` to reset the budget.

Gist: one one-click PR merge, two open forks (an autonomous-trading mechanism call, a protected-path
batching policy call), one new-signature incident to retro, one long-running free cleanup FYI.

## Headlines

- `/trade` gained an Alerts strip — expiry reminders, assignment risk, expiring-worthless — the first
  member-facing consumer of the #586 alert substrate, dismissable per-position
  ([#3483](https://github.com/ejclark/skynet-capital/pull/3483)).
- The trade ticket collapsed to one card with spread priced off the instrument toggle, and the
  milestone strip returned across the top of `/trade`
  ([#3574](https://github.com/ejclark/skynet-capital/pull/3574),
  [#3575](https://github.com/ejclark/skynet-capital/pull/3575)).
- Hardcore Sauron's bespoke trading logic is now a reusable "tactical playbook" rule chain
  (momentum-stop / sentiment-fade / sentiment-claim / momentum-scalp), registered and opt-in rather
  than bespoke code ([#3570](https://github.com/ejclark/skynet-capital/pull/3570)–[#3572](https://github.com/ejclark/skynet-capital/pull/3572)).
- Research was renamed to R&D and the Playbook Store moved under it; Plays and Collections retired now
  that Playbook cards carry their own evidence, window, and traits
  ([#3625](https://github.com/ejclark/skynet-capital/pull/3625)–[#3627](https://github.com/ejclark/skynet-capital/pull/3627)).
- Every PR's e2e job stopped silently skipping after `@playwright/test` was reverted to 1.62.1
  ([#3577](https://github.com/ejclark/skynet-capital/pull/3577)); a separate fix made bots' recent
  decisions reach the desk immediately instead of waiting on the backlog
  ([#3581](https://github.com/ejclark/skynet-capital/pull/3581)).

## Noise absorbed

- 50 commits landed: 32 `docs(research)` pulse/ledger entries across the Sep 2026 – 2028 macro/
  Treasury/Fed/BOE/JGB calendar, 18 feature/fix commits.
- Config/capability-drift audit (`scripts/config-audit.mjs`): all four sections clean.
- Incident-scan: 24 failed `main` runs in 14 days; 21 already match a known signature (comment-logged
  recurrences or an existing `docs/LESSONS.md` entry), 3 unlearned (folded into Needs-you above).
- PR [#1912](https://github.com/ejclark/skynet-capital/pull/1912) (flagged stuck in the last digest)
  merged 2026-09-22 — resolved, no longer stuck.
- Two plan issues ([#3527](https://github.com/ejclark/skynet-capital/issues/3527),
  [#3596](https://github.com/ejclark/skynet-capital/issues/3596)) each hit a mid-flight design
  question, banked it as a comment, got Eric's answer, and shipped multiple slices — the
  bank-a-question-not-guess loop working as designed. #3527 also surfaced a real bug (a cutover step
  likely broken by a config-file split between the dashboard and bots app) as its own issue,
  [#3595](https://github.com/ejclark/skynet-capital/issues/3595), holding the flip until it lands —
  no Eric decision needed, just a fix.
- 11 open `[ci] Moneypenny Events — research due events` issues from 2026-09-17 (#3215–#3220,
  #3229–#3233) are orphaned: their fix, PR #3222, closed unmerged on a conflict 2026-09-19 and was
  never redone. #3215's `needs-eric` label now points at a dead PR — mechanical rework for a session
  to pick up, not a new decision.
- 10 dependabot PRs opened 2026-09-20/22 (#3369, #3371, #3374–#3377, #3380, #3516, #3518, #3521),
  none yet reviewed; #1984 (`actions/cache` 4→6, a major bump) still correctly escalated `needs-eric`
  since 09-08.
- Comms-scan (`scripts/comms-scan.mjs --table`, 50 PRs measured): near-zero merge latency across the
  board, one Eric comment logged total (on #3620), zero reactions — expected for autonomous research
  and build volume at this altitude.
