# Digest — 2026-08-22

**Period:** 2026-08-15 → 2026-08-22 · **Landed:** 73 merged PRs · **Trigger:** heartbeat

## Needs you

1. **Merge #478** (https://github.com/ejclark/skynet-capital/pull/478) — restores the CI medic,
   which has been down since this morning (its own file went unparseable); also unblocks #475
   (your Mission Control move, filed today) from re-triggering its stalled build. Carve-out
   (workflow file) so it won't auto-merge itself. Pre-verified: `mergeable_state: clean`, and its
   own `Pipeline` check run succeeded (run 489).
2. **Decide #472** (https://github.com/ejclark/skynet-capital/pull/472) — owner-link trading +
   removes the `SKYNET_DESK_TRADING` kill switch, per your 2026-08-21 ruling on #466. Already
   carries a security review + red-team pass (one real account-takeover chain found and closed
   pre-merge, never deployed). Currently shows `mergeable_state: dirty` — it'll need a rebase onto
   main before it can land, but the call is yours either way.
3. **Decide #474** (https://github.com/ejclark/skynet-capital/pull/474) — widens the feedback
   lane's build envelope and lifts `claude.yml`'s blanket "never auto-merge" (the exact fix you
   asked for on #449, still open); also fixes a live duplicate-job bug in the feedback lane found
   while resolving this PR's own conflict. Also shows `mergeable_state: dirty`.
4. **Read the beta-ladder plan set** — #467 (trading companion chat), #468 (options order path),
   #469 (graduated 100→200→300 ladder) — filed 2026-08-21 as `needs-eric` alongside #466 (already
   decided; its slice 1 is #472 above). All three still have zero comments.

Merging #478 first is the one with no downside and unblocks the most; the other three wait on your
read either way.

## Headlines

- **Mission Control shipped** — an owner GUI to suspend/resume the autonomous bot fleet, trimmed
  to a v1 scope (#471).
- **Plans and the backlog now live entirely as GitHub issues** — the capsule communication format
  (#473) closed out the migration off `docs/plans` (#470).
- **The feedback lane builds member issues into merged PRs automatically** — an AI coach curates
  intake completeness before a session ever picks it up.
- **Trading got real** — a review-first `/trade` ticket (options + stock via Alpaca) and
  self-service day-2 account management shipped.
- **`/research` went live** — an event calendar (Treasury auctions, OpEx, macro tape), symbol
  pages, and 24 backlog ledgers drained with primary-sourced dates.

## Noise absorbed

- Config/capability-drift audit (`config-audit.mjs`): clean — 0 orphaned capabilities, 0
  contradicting routes, 0 recurring-intent themes, 0 compute-floor violations.
- 2 workflow-parse incidents (duplicate job keys made `postmaster.yml`/`ci-medic.yml` unparseable)
  self-diagnosed and repaired same-day (#476, #477); a `workflow-lint` gate now guards against the
  same shape recurring (#478, above, is the last piece).
- 38 research-ledger docs written or updated (14 initial assessments, 3 pulse checks, a 24-ledger
  queue drain, 1 at-a-glance header rollout) — all auto-merged via the postmaster's event-research
  lane, no stance changes flagged.
- 1 dependabot batch (10 dev-dependency updates) auto-merged.
