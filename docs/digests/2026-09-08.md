# Digest — 2026-09-08

**Period:** 2026-09-06 → 2026-09-08 · **Landed:** 20 commits · **Trigger:** threshold (20 commits since last digest)

> **Missed-interrupt flag:** research PR [#1912](https://github.com/ejclark/skynet-capital/pull/1912)
> (`fomc-blackout-start-2027-07-17`) has sat unmerged for **2 days** — its only CI run failed
> 2026-09-06 15:47 UTC on a single flaky test (`spawnSync node ENOBUFS`, a transient resource
> error in `tests/arch/event-scan.spec.ts`, nothing to do with the PR's own content), and nothing
> since has re-triggered it. Every one of its ~50 sibling research PRs this cycle merged within
> the hour. Nobody caught this in real time because nothing watches PRs the autonomous research
> lane opens for exactly this failure mode — nothing acted on it here either, per this Routine's
> report-only limit.

## Needs you

1. Comment `full` or `patch` on issue [#784](https://github.com/ejclark/skynet-capital/issues/784)
   — the Activity-page unification fork has sat open since 2026-08-28 (11 days, ~5-PR scope):
   fully unify trades/feedback/milestones/development onto #1211's event bus, or a lighter patch
   that reorganizes today's three widgets without merging their data. The issue body names the
   trade-off (full path costs more now; the patch leaves the same seam for the next event) and
   links a rendered option artifact.
2. Re-run the failed `verify` check on PR [#1912](https://github.com/ejclark/skynet-capital/pull/1912)
   — one flaky test, not a real failure (see the flag above); a green re-run should auto-merge it
   like its siblings.
3. Run `/retro` on the oldest unlearned incident, run
   [34053202825](https://github.com/ejclark/skynet-capital/actions/runs/34053202825) (commit
   `b11b2aa`, 2026-09-06) — `scripts/incident-scan.mjs` (wired into this digest for the first time
   today, PR #1978) found **5 unlearned "Moneypenny Events" failures** since 2026-09-06, all in the
   `research due events` matrix (8 parallel legs failed together on the oldest run, each job's tail
   showing orphaned `npm`/`playwright` processes being killed — points at runner resource
   contention across the matrix, not a code bug, but nobody's confirmed that with a root cause yet).
4. Comment-bloat budget breached again (proposal-grade, `scripts/comment-bloat-scan.mjs`, no
   decision needed): narration comments grew to **136** against a committed budget of 44 (up from
   124 two digests ago). Worst offender: `src/trading/round-trips.ts` (5). A cleanup target for
   whoever picks up debt burn-down next, then `--update` to reset the budget.

Gist: one real design fork, one one-click CI retry, one incident that owes a retro, one free
cleanup FYI.

## Headlines

- Options and equity tickets now offer symbol autocomplete — type "NV", get "NVDA - Nvidia" —
  from a 369-entry curated directory spanning 11 sectors, ETFs, crypto, and small caps
  ([#2007](https://github.com/ejclark/skynet-capital/pull/2007)).
- A meta-orchestrator audit of this repo's own orchestration (governor/coaches/grind/Moneypenny)
  closed seven independent gaps at once
  ([#1978](https://github.com/ejclark/skynet-capital/pull/1978)): a real duplicate-dispatch race
  between `/work-issues` and Moneypenny's claim-lease, structural-graph staleness promoted to its
  own blocking gate, a 200-item sanity cap on `/grind`, and — the reason this digest ran the
  incident scan above — `incident-scan.mjs` wired into the daily digest as a third ride-along.
- A research scoring bug (the VIX guard) was caught and fixed before either of the two forward
  tests it would have corrupted actually scored
  ([#1987](https://github.com/ejclark/skynet-capital/pull/1987)).
- Options-chain premiums now round to the cent and two ticket labels read clearer
  ([#1995](https://github.com/ejclark/skynet-capital/pull/1995)); both tickets dropped a leftover
  idle-draft status line ([#1988](https://github.com/ejclark/skynet-capital/pull/1988)).
- The "a fork thin on data is not `needs-eric` yet" doctrine (erosion vs. a deciding question) is
  now banked in CLAUDE.md ([#1986](https://github.com/ejclark/skynet-capital/pull/1986)).

## Noise absorbed

- 20 commits landed; 15 were `docs(research)` pulse/initial ledger entries across the Sep 2026 –
  Nov 2027 macro/Treasury/Fed calendar.
- Config/capability-drift audit (`scripts/config-audit.mjs`): all four sections clean — no
  orphaned capabilities, no owner-claim contradictions, no recurring-intent themes above
  threshold, every agent at or above its compute-routing floor.
- Comms-scan: ~20 PRs measured since the last digest, near-zero merge latency on all but #1912
  (flagged above), zero Eric comments or reactions logged — expected for routine research-ledger
  volume.
- 2 dependabot PRs open since 09-08 (#1985 dev-deps group, #1984 `actions/cache`), checks still
  pending as of this run, not yet picked up.
