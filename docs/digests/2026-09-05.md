# Digest — 2026-09-05

**Period:** 2026-09-04 → 2026-09-05 · **Landed:** 64 commits · **Trigger:** threshold (48 commits since last digest scan)

## Needs you

1. Check the GitHub App's installation permissions (Settings → GitHub Apps → the repair-dispatch
   app → Permissions) and confirm **Contents: Read and write** is still granted — two
   conflict-repair sessions today (PRs [#1286](https://github.com/ejclark/skynet-capital/pull/1286),
   [#1428](https://github.com/ejclark/skynet-capital/pull/1428)) resolved their conflicts cleanly,
   verified green, then had their push refused (`Permission … denied to github-actions[bot]`).
   Both diagnoses note prior repairs in the same lane pushed fine, so the grant — not the
   conflict — is what changed.
2. On PR [#1434](https://github.com/ejclark/skynet-capital/pull/1434), say which research note
   survives for the duplicate `treasury-buyback-20y30y-2026-10-08` calendar entry: **this PR's**
   version (the `sb0607` stale-cap finding + 10-07 announcement adjacency) or **main's** version
   from #1433 (the eligibility argument + predicted CUSIP, already cited by a live forward test at
   `forward-tests.md:170`) — or say "merge both" to keep both findings in one `notes` string. The
   repair lane declined to guess; it's the one thing blocking this PR.
3. Comment `ready` on issue [#1442](https://github.com/ejclark/skynet-capital/issues/1442) — starts
   slice 1 (research only, no code) into what a trusted eval suite would need to show before
   `envelope.json`'s mechanical gate can relax. You already confirmed the direction in chat on
   2026-09-04; this just starts the research.
4. Comment-bloat budget breached (proposal-grade, `scripts/comment-bloat-scan.mjs`): narration
   comments grew to 105 against a committed budget of 44 (up from 66 in the last digest). Worst
   offenders: `src/trading/round-trips.ts` (5), `src/observatory/activity-event.ts` (3),
   `src/participants/participant-store.ts` (3), `src/scripts/dashboard-access.ts` (3),
   `src/server/owner-link-store.ts` (3). No action needed from you — a cleanup target for whoever
   picks up debt burn-down next, then `--update` to reset the budget.

Gist: one credential grant to check, one research-note pick, one cheap ready-flip, one free
cleanup FYI.

## Headlines

- The protected-path platter mechanism (#1344/#1347) shipped its first real batch: PR
  [#1402](https://github.com/ejclark/skynet-capital/pull/1402) landed several envelope-protected
  changes as one reviewable, one-touch merge instead of separate held PRs.
- The interrogation pass (#1350) fired for real on issue #666 and caught a stale `needs-eric`
  after three digests carrying the same ask: rather than waiting on a Fly read token, the bots
  process now stamps its own commit sha on the poll it already makes, closing 5 of 6 EARS lines
  credential-free (#1389) — no token minted, no question asked.
- The growing research-PR merge-conflict queue that needed your call in the last two digests got
  its actual root cause diagnosed instead: a conflict is repaired exactly once today, so `main`
  outruns a slow-to-land repair (#1403). The fix is scoped and doesn't need your judgment, just
  building.
- Fleet ops status moved from an owner-only Settings-page card to a group-visible topbar status
  pill (#1377/#1296).
- `/grind` runs now narrate their own progress and name themselves like a table of contents, chores
  declare their own effort/model instead of per-call tribal knowledge, and a registry gate keeps
  the workflow's `meta` block honest (#1356, #1346, #1339, #1331).

## Noise absorbed

- 64 commits landed; ~48 were `docs(research)` ledger entries (initial/pulse/close-out) across the
  Sep–Dec 2026 treasury/ECB/buyback corridor.
- Config/capability-drift audit (`scripts/config-audit.mjs`): all four sections clean — no orphaned
  capabilities, no owner-claim contradictions, no recurring-intent themes above threshold, every
  agent at or above its compute-routing floor.
- Comms-scan: 46 PRs measured since the last digest, near-zero merge latency on nearly all, zero
  Eric comments or reactions logged — expected for a stretch of routine research-ledger and
  autonomous-lane volume.
- 2 Moneypenny Events CI failures (#1421 `route`, #1390 `build feedback issue`) — both self-healed;
  every run since is green.
- 15 open research PRs currently sit `conflict-flagged` against a fast-moving `main` — tracked by
  #1403 above, not a new ask.
- `stall-flagged` lingers on issue #666 despite it being fully answered same-day — cosmetic, no
  action needed, clears on the next label pass.
