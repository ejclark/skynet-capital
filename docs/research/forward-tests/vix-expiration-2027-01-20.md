# Forward tests — vix-expiration-2027-01-20

| # | Hypothesis | Prediction | Kill switch | Score by | Outcome |
|---|---|---|---|---|---|
| FT-vix-expiration-2027-01-20-1 | Registered 2026-09-06. **The opposite-sign design is degenerate, made falsifiable.** The [`vix-expiration-2027-02-17`](../events/vix-expiration-2027-02-17.md) sibling could not separate the holiday-drag and FOMC-in-window mechanics because at `VX/G7` both predict the same sign. At `VX/F7` they predict **opposite** signs — drag `√(21/22)−1` ≈ **−0.46 pts** (21 sessions vs a 22 norm, from Washington's Birthday 2027-02-15) against one *assumed* double-variance FOMC day in a 21-session strip, `√(22/21)−1` ≈ **+0.47 pts** — netting **≈ +0.01**, so F7 should sit *on* the curve. Measured this session, F7's `log(dte)`-fit residual is **−0.0083** (09-04) / **+0.0222** (09-03), the smallest on the nine-contract curve. This is registered as a **refusal, not a confirmation**: "both mechanics priced and cancelling" and "neither mechanic priced" predict the *same* near-zero reading, so the design identifies nothing while the two terms stay within 0.01 pts of each other. See [`../events/vix-expiration-2027-01-20.md`](../events/vix-expiration-2027-01-20.md) leg 7. | Re-running this session's computation (OLS of the nine listed `VX` daily settlements on `log(days-to-expiry)`, residual per contract) at **every pulse** through settlement, `VX/F7`'s residual stays inside **±0.20** — i.e. the degeneracy persists and the reading keeps discriminating nothing. | `VX/F7`'s residual printing **outside ±0.20** on any Cboe settlement date before 2027-01-20 — the two mechanics are no longer cancelling, one is visibly dominating, and the opposite-sign design becomes identifying after all rather than degenerate. Void if the NYSE publishes a 2027 calendar putting a closure inside 2027-01-21 → 2027-02-19 other than 02-15 (the session count changes), if `fomc-2027-01-27` moves off the window, or if Cboe settles the January contract on another date — in which case this is re-specified, not scored. | 2027-01-19 | — |
| FT-vix-expiration-2027-01-20-2 | Registered 2026-09-06. **Business-time drag prices as a threshold, not a slope.** Regressing each listed contract's `log(dte)` residual on its predicted drag across all nine gives a slope of **0.563** (09-04) / **0.503** (09-03), r² 0.47 — but **ex-`VX/Z6` the slope collapses to 0.221 / 0.181**, r² 0.11. One point carries the whole relationship: `VX/Z6` is the only strip on the curve containing **two** holidays (Christmas 2026-12-25 and New Year 2027-01-01, 20 sessions), it predicts **−0.8830 pts** of drag and realizes **−0.5521** (**63%**), while H7, J7 and K7 all show *positive* residuals despite predicted drag of −0.48 to −0.49. Two readings are consistent with n = 9: drag prices **linearly at ~20%** (the ex-Z6 slope, which also reproduces the March sibling's λ ≈ 0.2 and the February sibling's 16–31%), or it prices as a **threshold**, invisible until the session deficit is large enough to clear the noise. This test picks the threshold reading and makes it killable. See [`../events/vix-expiration-2027-01-20.md`](../events/vix-expiration-2027-01-20.md) leg 8. | Re-running the same regression at **every pulse** through 2026-12-16, `VX/Z6`'s realized drag (its `log(dte)` residual divided by its predicted drag) stays **at or above 30%**, and it remains the largest-magnitude negative residual on the curve — the leverage point stays a leverage point. | `VX/Z6`'s realized drag printing **below 30%** of prediction on any Cboe settlement date before 2026-12-16, or another contract overtaking it as the curve's largest negative residual — the threshold reading dies and the linear ~20% reading is what survives. Void if Cboe delists or re-dates `VX/Z6`, or if the NYSE alters the 2026-12-25 / 2027-01-01 closures — in which case this is re-specified, not scored. Note the drag is measured against the **Federal Reserve K.8** holiday table plus Good Friday, because nyse.com was 403 to this runner on 2026-09-06. | 2026-12-15 | — |
