# Forward tests — lennar-q3-fy2026-2026-09-16

Registered by [`events/lennar-q3-fy2026-2026-09-16.md`](../events/lennar-q3-fy2026-2026-09-16.md)'s
initial research, 2026-09-16. **Neither test proposes a trade** — the parent stance is stand-aside on
every horizon, and both rows exist to adjudicate a *mechanism*, at zero size. Both score by
2026-09-18, inside this event's `closeOutWithinDays: 6` window, so its own close-out can fill them.

`-1` is deliberately written as a **joint region** rather than a marginal direction call, because the
sweep's most recent kill (`FT-treasury-buyback-tips-10y30y-2026-09-15-1`) is exactly this trap: *do
not register a base rate from a sub-sample when another factor in the same document predicts moving
the variable that defines the sub-sample.* Here the sub-sample is the margin-compression regime and
the variable is FOMC, deciding the same afternoon.

| # | Hypothesis | Prediction | Kill switch | Score by | Outcome |
|---|---|---|---|---|---|
| FT-lennar-q3-fy2026-2026-09-16-1 | LEN's modern-era earnings-gap asymmetry (3 up-gaps in 14 prints since 2023, mean −2.45%, p=0.029 vs its own 52% overnight base rate) is an artifact of the **margin-compression regime** — 12.9% incentives against a 4–6% normalized load — not a standing property of the name. | Stated as a joint region. **(a)** If the 2026-09-16 release leaves the regime intact — reported homebuilding gross margin **≤16.0%** OR incentives **≥12.9%** — LEN's **2026-09-17 open gap is negative**. **(b)** If both invert — gross margin **>16.0%** AND incentives **<12.9%** — the **gap is positive**. Region (a) is the expected branch on the company's own ~16% guide and Q2's 15.6%/12.9%. | Either decoupling kills the *mechanism*, not just the direction: a **positive** gap with the regime intact (GM ≤16.0% or incentives ≥12.9%), or a **negative** gap with both inverted. On a kill, the asymmetry must be re-derived from scratch and never re-cited as regime-explained. | 2026-09-18 | — |
| FT-lennar-q3-fy2026-2026-09-16-2 | On LEN the earnings reaction is an **overnight repricing, not an intraday trend** — which is why the S3-shaped reaction-day fade is absent here (session open D+1 → close D+1: mean −0.50%, median −0.38%, win 43%, n=14) while the gap is significant. | On 2026-09-17, **\|open gap\| > \|open→close session move\|** — the overnight move is the larger half of the reaction day. | The prediction **misses** if \|session\| ≥ \|gap\|. The **hypothesis dies** if \|session\| ≥ **2×**\|gap\|, i.e. the reaction day is dominated by intraday trend — which would put LEN inside S3's scope after all and reopen a playbook question this ledger closed. | 2026-09-18 | — |
