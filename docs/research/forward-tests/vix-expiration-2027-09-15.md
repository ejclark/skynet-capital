# Forward tests — vix-expiration-2027-09-15

| # | Hypothesis | Prediction | Kill switch | Score by | Outcome |
|---|---|---|---|---|---|
| FT-vix-expiration-2027-09-15-1 | Registered 2026-09-06. **The collision null, on the one comparison that holds meeting class fixed.** A monthly VIX settlement that falls on an FOMC decision day cash-settles with the 14:00 ET statement unresolved inside its own 30-day reference window — Cboe's spec ends expiring-`VX` trading at 09:00 ET and strikes the SOQ from opening prints at 09:30 ET. The story that invites is an event premium in the settlement print. This session tested it on **37 historical collisions, 2008-07 → 2026-09**, against Cboe's own `VRO_History.csv`, and every arm came back inert: the raw comparison has the **wrong sign** (VIX opens −1.30% below the prior close on collisions against +0.29% on non-collision settlements), that decline is the **pre-FOMC morning drift** rather than the collision (it appears at −0.52% vs +0.83% on FOMC days that are *not* settlements, P = 0.0015), and holding meeting class fixed — quarterly SEP-carrying decisions only, varying only whether the day is also the settlement — the difference is **−0.88 points at permutation P = 0.33** (median −0.72, P = 0.40), reversing sign inside September (**+0.10%** on 11 collisions against **−1.38%** on 7 non-collisions). The settlement print's own basis to the concurrent index is **+0.14% vs +0.31%, P = 0.79**. All 41 collisions 2007–2027 are SEP-carrying quarterly meetings, so that class is the whole population. See [`../events/vix-expiration-2027-09-15.md`](../events/vix-expiration-2027-09-15.md) legs 3–4. | Re-running this session's computation at close-out — the settlement-rule engine over Cboe's `VRO_History.csv` and `VIX_History.csv`, re-fetched rather than cached, with the collisions that land between registration and scoring added (**2026-09-16**, **2027-03-17** and **2027-09-15** itself) — the controlled quarterly comparison of VIX open-vs-prior-close **still returns permutation P ≥ 0.05**. A real collision effect should strengthen as instances accumulate; a null that stays a null on 40 collisions is the finding. | The controlled comparison reaching **P < 0.05** with the added instances, in either direction — the collision would be carrying something the pre-FOMC drift does not explain, leg 4's refusal converts to a live question, and the ledger is rebuilt on the newer data. Void if the September 2027 FOMC moves off 2027-09-15 (the treatment is destroyed, not settled), if Cboe lists `VX/U7` on a date other than 2027-09-15, or if `VRO_History.csv` stops being published or is revised such that the 217-of-218 rule reproduction no longer holds — each removes the measurement rather than answering it, so the row is re-specified, not scored. | 2027-09-21 | — |
| FT-vix-expiration-2027-09-15-2 | Registered 2026-09-06. **The near-term discriminator between the pooled story and the controlled null, on a single observation that is genuinely uncertain.** The pooled comparison says a collision settlement's VIX open sits **−1.30%** below the prior close; the controlled comparison (leg 4, step three) says that is the ordinary pre-FOMC morning and the collision adds nothing, and September's own 11 instances point the *other* way at **+0.10%**. **2026-09-16 is the next collision** — an FOMC decision day that is also the monthly VIX settlement (`VX/U6`, listed at 2026-09-16 on Cboe's settlement surface, HTTP 200 2026-09-06), ten days after registration, and a **September** one, i.e. the same subsample 2027-09-15 belongs to. Against the non-collision settlement dispersion measured this session (sd 2.84 log points) a draw at or below the pooled mean has a probability near one in three, so this is a real test rather than a formality. | The VIX **open** on **2026-09-16**, read from Cboe's `VIX_History.csv`, is **greater than −1.30%** in log terms relative to the **2026-09-15** VIX close — the pooled collision mean does **not** reproduce on the next instance, consistent with the controlled null and with September's reversal. | The 2026-09-16 VIX open printing **at or below −1.30%** relative to the 2026-09-15 close — the pooled collision mean reproducing on the first out-of-sample instance, which is one observation and therefore not a refutation of leg 4, but is enough to put the controlled null back in question and to force the next pulse to re-run the full comparison rather than cite this one. Void if 2026-09-16 is not a trading session, if the September 2026 FOMC decision moves off that date, or if Cboe re-dates `VX/U6` — in each case the instance never existed and the row is re-specified. | 2026-09-17 | — |
