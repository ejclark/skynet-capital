# Forward tests — trading-parity-2026-09

Registered 2026-09-21 by the trading-experience parity study
(`docs/research/trading-parity-2026-09.md`). No event ledger owns these rows, so each names its scorer
explicitly (the FT-4 process finding in `legacy.md`: a test with no ledger has no close-out owner).

| # | Hypothesis | Prediction | Kill switch | Score by | Outcome |
|---|---|---|---|---|---|
| FT-trading-parity-2026-09-1 | **Lo-fi-first converges** — four greybox shapes (real values, no chrome) on one artifact, derived from the competitor research, let Eric settle template and flow faster than #2017-V1's un-recommended "three rendered shapes" fork (parked 12 days at registration) | One lo-fi shape, or a named merge, is quoted in the plan issue's decision log within 10 days of the artifact's first publish, with ≤ 2 republishes before the pick | No pick by 2026-10-05, ≥ 3 republishes before a pick, or a fifth shape requested → kill; fall back to one hi-fi on the synced components carrying an `adopt` row, and ship P1 regardless. Scorer: the session that opens P0 | 2026-10-05 | _open_ |
| FT-trading-parity-2026-09-2 | **The journey gaps are what stop members trading** — pending / cancel / "Confirmed" on an unsent order are execution blockers, not cosmetics | Human `order.submitted` lines in the audit log (`src/server/order-audit-log.ts`) over the 4 weeks after P1 lands exceed the count over the 4 weeks before it, and ≥ 1 member cancel appears as a canceled `order.updated` | Count flat or lower over the window, or zero cancels by 2026-10-31 → the blocker was elsewhere (onboarding; 2 of 5 members have never traded, `docs/THE-GAME.md`); P2–P4 sequencing reopens. Scorer: the P1 close-out session | 4 weeks after P1 merges (≤ 2026-11-14) | _open_ |
| FT-trading-parity-2026-09-3 | **Borrowed Robinhood mechanics reward discipline on paper, not churn** — the ledger's `borrow` / `adapt` rows wired to a plan-adherence trigger raise disciplined exits | Closes with `RoundTrip.realized > 0` per member-week (`src/trading/trade-stats.ts` `realizedByDay`) over the 4 weeks after the first P4 mechanic ships exceed the prior 4-week count, with `realized < 0` closes not rising faster | Green closes flat while red closes or submit counts rise → the mechanic trained volume (`docs/FOG-OF-WAR.md` Q1) — unwire it and mark the row `skip` in the study. Void if no renown module exists by 2026-12-31 (`docs/THE-GAME.md`: not built). Scorer: the P4 close-out session | 4 weeks after P4 slice 1 | _open_ |
