# Forward tests — census-benchmark-revision-nsa-2026-09-28

<!-- One event's pre-registered hypotheses, written ONLY by the lane that owns
     docs/research/events/census-benchmark-revision-nsa-2026-09-28.md — never by a sibling lane, which
     is what lets every research PR merge without touching a shared file (issue #1449). The register
     at ../forward-tests.md is composed from these files; never add a row there. Ids are
     FT-census-benchmark-revision-nsa-2026-09-28-<n>, <n> counting up within this file. Rows append
     only; the Outcome column is the one cell the close-out fills. -->

| # | Hypothesis | Prediction | Kill switch | Score by | Outcome |
|---|---|---|---|---|---|
| FT-census-benchmark-revision-nsa-2026-09-28-1 | **This event's own id describes it wrongly, and the primary sources say which reading is right** — registered **2026-09-06 at D-22** of the `estimate`-dated [census-benchmark-revision-nsa-2026-09-28](../events/census-benchmark-revision-nsa-2026-09-28.md). The `-nsa-` token comes from reading `census.gov/mtis/index.html` (HTTP 200, 684,348 bytes, fetched direct 2026-09-06) — "Revised not adjusted estimates and corresponding adjusted estimates are tentatively scheduled for release on **September 28, 2026** and **October 26, 2026**, respectively" — as mapping estimate TYPE to date. The two program pages fetched the same day map it to PROGRAM instead: `census.gov/retail/index.html` (743,183 bytes) puts "Revised not adjusted estimates **and corresponding adjusted estimates**" for **retail** on "September 28, 2026 at 10:00 a.m. EDT", and `census.gov/wholesale/index.html` (527,648 bytes) puts both **wholesale** halves on "October 26, 2026 at 10:00 a.m. EDT". Each program page gives ONE date for BOTH estimate types, which only the program reading satisfies. `symbols: []`, so no cached instrument applies | **The 2026-09-28 release carries both the not-adjusted AND the seasonally adjusted revised retail estimates**, evidenced by the Census release text on `census.gov/retail` or the release itself naming adjusted estimates, re-fetched at score time and never read from memory. Registered in the **program-reading** direction: two program pages state it in their own programme's words, against one compressed cross-programme sentence. A pass means the seasonally adjusted retail basis — what every month-over-month retail comparison runs on — moves on 09-28 rather than four weeks later, and that this id's `-nsa-` is a misnomer to be carried, not a description. A fail means the MTIS sentence was literal, the adjusted retail history waits for 10-26, and the id was right all along. Zero capital — a publication's scope is not a position, and this event has `symbols: []` and no macro-keyed house playbook | **The 2026-09-28 release publishing revised NOT-ADJUSTED retail estimates only**, with the corresponding adjusted estimates explicitly deferred, kills it. **Void** (not killed) if Census does not publish on 09-28 at all — a schedule slip says nothing about scope, and the ledger's date kill switch covers that case separately | 2026-10-05 | _open_ |
| FT-census-benchmark-revision-nsa-2026-09-28-2 | **A benchmark keyed to two survey years reaches past those years into the current level** — registered **2026-09-06 at D-22**. Census's notice scopes the revision to "historical corrections and the results of the 2023 and 2024 Annual Integrated Economic Survey" and says nothing about whether the new benchmark carries forward through the subsequent months. That is the difference between a bookkeeping restatement and a basis change every 2025–2026 retail comparison has to net against, and it is the one magnitude question this session could pin, because the historical route is closed: `alfred.stlouisfed.org` returned 404 on three URL shapes and three 120-second timeouts on a fourth, and `fred.stlouisfed.org`'s CSV accepts `vintage_date` and silently ignores it (byte-identical, 7,495 bytes) — so the wholesale sibling's twelve-revision magnitude table has no retail counterpart in this ledger and is deliberately not borrowed. Pinned prospectively instead, from this session's own FRED pull: `RSAFSNA` **2026-07 = $784,610M**, 414 observations from 1992-01. Scope of the named window, for size: 2023 **$8,139.5B** + 2024 **$8,386.0B** ≈ **$16.5T** across 24 reference months | **`RSAFSNA` for 2026-07 differs from $784,610M by at least 0.1%** on a FRED re-pull dated 2026-10-16 or later. Registered in the **carries-forward** direction, because Census dates the revision's appearance in a specific downstream edition (the August-2026 MTIS release of 2026-10-15) rather than describing it as confined to the survey years — but at low confidence, since no primary source this session reached states the carry-forward either way. A pass says any retail level or ratio comparison spanning 09-28 is comparing two different series; a fail says the restatement is genuinely historical and the recent months are untouched. Zero capital | **`RSAFSNA` 2026-07 unchanged at $784,610M** (or moved by less than 0.1%) on the 2026-10-16-or-later re-pull kills it. **Void** if FRED has not ingested a post-09-28 retail vintage by the score-by date — an un-refreshed source is not evidence about the revision, and the test re-scores on the next pulse | 2026-10-16 | _open_ |
| FT-census-benchmark-revision-nsa-2026-09-28-3 | **Nothing on the 2026-09-28 tape belongs to this release, and the slot itself is ordinary** — registered **2026-09-06 at D-22**. 09-28 is a Monday with **zero** scheduled Census releases (all 179 rows of `economic-indicators/calendar-listview.html` parsed 2026-09-06: September runs 09-25 → 09-30) and one low-impact tracked event, `dallas-fed-mfg-2026-09-28`. It sits two sessions before quarter-end, in front of four high-impact tracked events in five days — the **09-30** government funding deadline and PCE, **10-01** ISM manufacturing, **10-02** jobs — plus quarter-end and the S&P select-sector secondary reweight. Measured on 4,189 Yahoo SPY sessions 2010-01-04 → 2026-08-28: the quarter-end-minus-2 class runs a median session range of **0.910%** (n=66) against **0.904%** for the other 4,123, a 20,000-iteration permutation test on the median difference giving **p=0.942**. The September-only subset (n=16) medians 0.975%, quoted only so a later reader does not rediscover sixteen observations as a signal. VIX **14.53** (2026-09-04 close) | **SPY's 2026-09-28 session range is at or below 1.5%** of the open, from re-pulled daily bars. Registered with its prior stated rather than hidden: the base rate is **80.0%** since 2010 (n=4,189) and **85.9%** since 2024 (n=667, median 0.863%), so a pass is weak evidence and is meant to be. The test exists to give the *attribution* claim a dated falsifier — that whatever happens on 09-28 is quarter-end positioning for the 09-30/10-02 corridor and not a reaction to a restatement of 2023–2024 history — not because the tape is in doubt. Zero capital | **SPY's 2026-09-28 session range printing above 1.5%** kills it, and obliges the next row to say what moved it before this ledger repeats that the slot is inert. **Void** if 2026-09-28 is not a trading session | 2026-09-29 | _open_ |
