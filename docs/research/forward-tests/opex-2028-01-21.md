# Forward tests — opex-2028-01-21

| # | Hypothesis | Prediction | Kill switch | Score by | Outcome |
|---|---|---|---|---|---|
| FT-opex-2028-01-21-1 | This ledger's headline, and the one claim here that would change other ledgers if it fell: the geometric inflation of the house five-session ratio is **one-sided, not a mirror**. A closure in the POST window (spans 7/10, +3 calendar days) inflates it — med(raw ÷ √calendar-day) **1.1320** (QQQ, n = 31) and **1.1096** (SPY), Mann-Whitney vs clean **P = 0.0010 / 0.0001** — while a closure in the PRE window, this event's own geometry (spans 8/7, **+1** day), does **not** deflate it and does not separate from clean at all: **1.0102 / 0.9905** against a clean baseline of **1.0050 / 1.0012**, **P = 0.8841 / 0.5138** (n = 21). The mechanism is arithmetic and exception-free — the three span classes are 7/10, 8/7 and 7/7 with no other value in 31, 21 and 189 observations. See [the ledger](../events/opex-2028-01-21.md) legs 3–4. | Re-running `scripts/research/expiration-window-geometry.mjs` on a **third** instrument — **IWM**, with **`^GSPC`** as a cross-check — returns the holiday-INSIDE class at med(raw ÷ √cal-day) within **1.5 points of 1.000** and **failing to separate from clean at P < 0.05**, while the holiday-AFTER class separates at **P < 0.05** on the same run. Base rate disclosed: the two instruments already run land at 1.0102 and 0.9905 for INSIDE (P = 0.88 / 0.51) and 1.1320 / 1.1096 for AFTER (P = 0.0010 / 0.0001), so a null for INSIDE is expected and is NOT a miss. | The INSIDE class separating from clean at **P < 0.05** on the third instrument, or landing **below 0.97** — the asymmetry is wrong, a pre-window closure biases the measure after all, and this event inherits `opex-2028-02-18` leg 5's warning along with `opex-2027-01-15`'s estimator-disagreement caution. AFTER failing to separate would instead break the sibling's own result, not this one, and is reported rather than scored here. | 2027-03-01 | — |
| FT-opex-2028-01-21-2 | The weakest quantitative claim in this ledger, registered because it is the weakest: an ordinary monthly expiration sitting **inside an FOMC communications blackout** is indistinguishable from one that is not. The frequency is solid and is a calendar count — **58/408 = 14.2%** of expirations 1994–2027, ordinary monthlies **19/272 = 7.0%**, January **3/34 = 8.8%**, from 300 meetings parsed off the Board's own `fomchistorical*.htm` pages plus `fomccalendars.htm`, with the blackout rule validated by reproducing the tracked `fomc-blackout-start-2027-01-16` window exactly. The **null** rests on only the 12 such monthlies inside the price sample: permutation **P = 0.997 / 0.896** on the raw five-session ratio, **0.134 / 0.314** on the first session after, **0.347 / 0.618** on the five-session return. See [the ledger](../events/opex-2028-01-21.md) legs 7–8. | Re-running the containment comparison on a **third** instrument (**IWM**, cross-checked on **`^GSPC`**) returns **P > 0.05 on all three channels** — the null survives an instrument it was not measured on. Base rate disclosed: n = 12 versus ~209, so this test is under-powered by construction and a wide P is expected; a narrow one is the informative outcome. | Any channel clearing **P = 0.05** on the third instrument — containment is an effect rather than a calendar fact, the "worth stating, worth nothing" framing in the stance is wrong, and `mlk-market-closure-2028-01-17` leg 7's containment observation becomes a condition rather than a corridor note. | 2027-03-01 | — |
| FT-opex-2028-01-21-3 | This event's own directional instance, carried so its own date adjudicates its own call. Its prior is **measured and it is a genuine null**: January expirations with MLK **inside** the expiration week (n = 10, 2005–2024) run a raw up-rate of **50.0%** (QQQ) and **40.0%** (SPY) with the gap-free estimator at **50.0%** on both, and med(raw ÷ 1-day-returns) **0.9996** / **0.9668** — the two estimators **agree**, unlike the January-holiday-**after** cell that reads **100% raw against 40% gap-free** with med 1.8919. A coin flip registered as a coin flip. Confidence **LOW**. See [the ledger](../events/opex-2028-01-21.md) leg 5. | QQQ five-session realized volatility over the sessions **2028-01-24 → 2028-01-28** comes in **above** the same statistic over **2028-01-14 → 2028-01-21** (base close 2028-01-13), on **both** the raw close-to-close estimator and the one-calendar-day-return estimator — i.e. the two agree on sign, as they did on 5 of 10 and 5 of 10 historical observations of this class respectively. | The two estimators landing on **opposite sides of 1.0** for this expiration — leg 5's "the estimators agree in this geometry" claim loses its only out-of-sample observation, and the pre-window class inherits the sibling's fragility warning after all. A both-below reading is a directional miss, not a kill: the prior is 50%, and the class is registered as a null. | 2028-02-04 | — |
