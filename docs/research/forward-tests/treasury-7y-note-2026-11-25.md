# Forward tests — treasury-7y-note-2026-11-25

<!-- One event's pre-registered hypotheses, written ONLY by the lane that owns
     docs/research/events/treasury-7y-note-2026-11-25.md — never by a sibling lane, which is what lets every
     research PR merge without touching a shared file (issue #1449). The register at
     ../forward-tests.md is composed from these files; never add a row there. Ids are
     FT-treasury-7y-note-2026-11-25-<n>, <n> counting up within this file. Rows append only; the Outcome
     column is the one cell the close-out fills. -->

| # | Hypothesis | Prediction | Kill switch | Score by | Outcome |
|---|---|---|---|---|---|
| FT-treasury-7y-note-2026-11-25-1 | **The 11:30am early close adds no concession — the pre-holiday half day is an ordinary 7-year auction with the clock moved 90 minutes** — registered 2026-09-09 at D-77 of the `estimate`-dated [treasury-7y-note-2026-11-25](../events/treasury-7y-note-2026-11-25.md). Both proposals that created this event filed it on the opposite intuition (a belly auction selling into a thin pre-Thanksgiving session should cheapen more). Measured this session from Treasury's `auctions_query` (152 auctions since 2014, `security_term` filter) against 3,172 par-curve sessions: the auction-day 7Y−10Y move is **not a constant** (2023+ −0.955bp t = −5.04, but **2014–2021 +0.479bp t = +3.98**, pooling to −0.079bp t = −0.66) — it is a linear function of primary-dealer takedown, **`7s10s = −1.894 + 0.090 × dealer%`**, slope **t = 7.83**, residual sd **1.244bp**, surviving a linear time-trend control (partial corr +0.349, t = 4.55) and year-demeaning (slope +0.082, t = 3.83). Against that model the nine **11:30 AM** auctions (six pre-Thanksgiving, two Christmas-Eve, one unexplained 2016-02-26) residual **+0.267bp, t = 0.92** — the wrong sign for amplification — vs −0.017bp for the ordinary 143 | The **2026-11-25 residual is greater than −1.5bp**, i.e. `7s10s_actual − (−1.894 + 0.090 × dealer%)` > −1.5, where `7s10s_actual` is the 7Y-minus-10Y CMT change close 11-24 → close 11-25 from Treasury's daily par yield curve and `dealer%` is `primary_dealer_accepted` over the competitive base (indirect + direct + dealer) from `auctions_query`. **The null pass rate is stated up front and it is high: 89.5%** — only 16 of 152 historical residuals reach −1.5bp. This is registered as a **weak** test rather than dressed up as a strong one: its value is that the alternative hypothesis (thin session → bigger concession) is the one this event was filed on, so the 10.5% tail is where that story would have to appear. It is also **explanatory, not predictive** — `dealer%` prints with the auction, so the fitted value does not exist until 13:00 ET on the day | **A residual at or below −1.5bp reinstates the early-close-amplifies-concession story** and puts leg 6 of the ledger back in play; it would also be the first evidence that the dealer-takedown model needs a half-day term. **Separately kills the model itself if |residual| > 2bp** in either direction (9.2% of the 152-auction history), which would leave this lane with neither the [10-29 sibling's](../events/treasury-7y-note-2026-10-29.md) −0.95bp constant nor the replacement. **Voids (does not score) if** the 2026-11-19 announcement moves the auction off 11-25, the 11-04 refunding or the announcement moves the size off $44B (the dealer-share quantiles are $44B-era), or dealer takedown prints outside the era's observed 9.34–17.00% support, which would make the fitted value an extrapolation rather than a test | 2026-11-30 | _open_ |
