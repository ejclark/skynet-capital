# Forward tests — retail-benchmark-revision-2026-09-28

<!-- One event's pre-registered hypotheses, written ONLY by the lane that owns
     docs/research/events/retail-benchmark-revision-2026-09-28.md — never by a sibling lane, which is
     what lets every research PR merge without touching a shared file (issue #1449). The register at
     ../forward-tests.md is composed from these files; never add a row there. Ids are
     FT-retail-benchmark-revision-2026-09-28-<n>, <n> counting up within this file. Rows append only;
     the Outcome column is the one cell the close-out fills.

     NOTE: this event is a DUPLICATE of census-benchmark-revision-nsa-2026-09-28, which registered its
     own three tests on the same release the same day. These two are deliberately NOT restatements of
     those — they test the measurement route this lane opened, not the release's scope or its tape. -->

| # | Hypothesis | Prediction | Kill switch | Score by | Outcome |
|---|---|---|---|---|---|
| FT-retail-benchmark-revision-2026-09-28-1 | **A retail benchmark revision restates the WHOLE published history, not just the survey years it names** — registered **2026-09-06 at D-22** of the `estimate`-dated [retail-benchmark-revision-2026-09-28](../events/retail-benchmark-revision-2026-09-28.md). Census scopes the 09-28 restatement to "historical corrections and the results of the 2023 and 2024 Annual Integrated Economic Survey" and says nothing about reach. Measured this session from Census's own vintage archive rather than an aggregator: `www2.census.gov/retail/releases/historical/mrts/mrtssales92-present25.xlsx` (HTTP 200, 428,539 bytes, data through 2025-02) against the current `www.census.gov/retail/mrts/www/mrtssales92-present.xlsx` (HTTP 200, 440,847 bytes, data through 2026-06) — **all 398** overlapping months of the headline not-adjusted series moved, **372 of 372** pre-2023 months by ≥0.05%, median **−2.098%**, worst **−3.201%** (1992-04). That window bundles a revision and a general-merchandise reclassification, so it sizes the class, not this date. `symbols: []`, so no cached instrument applies | **Re-downloading `mrtssales92-present.xlsx` after the 09-28 release and finding at least 90% of pre-2023 months changed by ≥0.05%** against the values pinned today (currently published NSA headline, $M: 1992-06 **163,417** · 2000-06 **274,749** · 2010-06 **351,056** · 2019-06 **505,132** · 2022-06 **676,507** · 2023-06 **688,753**), re-fetched at score time and never read from memory. Registered in the **whole-history** direction on the measured precedent above. A pass means every stored retail comparison spanning 09-28 is comparing two series and must be rebuilt from the new file, not netted; a fail means the restatement is confined to 2023–2024 and older history is safe to hold. Zero capital — a publication's reach is not a position, and this event has no symbols and no macro-keyed house playbook | **Fewer than 90% of the pre-2023 months moving by ≥0.05%**, or the six pinned anchors above returning byte-identical values, kills it. **Void** (not killed) if Census does not publish on 09-28 at all, or if the current-file URL stops resolving — a schedule slip and a moved path are both silence about reach, and the ledger's date kill switch covers the first separately | 2026-10-05 | _open_ |
| FT-retail-benchmark-revision-2026-09-28-2 | **Census archives its superseded MRTS vintage after a benchmark, which is what keeps the magnitude of the NEXT one measurable** — registered **2026-09-06 at D-22**. The twin ledger recorded the retail revision size as unmeasurable after `alfred.stlouisfed.org` returned 404 on three URL shapes and three 120-second timeouts, and `fred.stlouisfed.org`'s CSV silently ignored `vintage_date`; this session hit the same two ALFRED 404s from a different runner and then found the primary route open instead — `www2.census.gov/retail/releases/historical/mrts/` (HTTP 200, 36,760 bytes, 107 entries) serves Census's own superseded workbooks. Exactly one MRTS sales vintage is archived there today (`mrtssales92-present25.xlsx`; `…24`, `…23` and `…26` all 404), so whether the route stays open is an assumption this book should test rather than inherit | **The `historical/mrts/` directory listing carrying a newly-added archived MRTS sales workbook that is not `mrtssales92-present25.xlsx`** — a `…26`-style successor, or the `25` file's byte size/sha256 changing from **428,539** bytes / `ccafd894cb8d252b…` — on a re-fetch dated 2026-10-15 or later. Registered in the **route-stays-open** direction because Census has archived at least one superseded vintage and the archive is a standing published artifact, but at low confidence: one observation is not a policy. A pass means the next retail restatement's magnitude is measurable exactly, from the primary, by diffing two Census files; a fail means this ledger's Leg 3 method dies with it and the next session must say so rather than assume the shelf is stocked. Zero capital | **No new archived MRTS sales workbook and an unchanged `…present25.xlsx`** on the 2026-10-15-or-later re-fetch kills it. **Void** if `www2.census.gov` is unreachable at score time (record the status in `probe-ref.blocked` and re-score on the next pulse) or if the 09-28 release does not happen — no revision means nothing to archive | 2026-10-15 | _open_ |
| FT-retail-benchmark-revision-2026-09-28-3 | **A retail benchmark revision takes the ORDINARY shape — a recent-years correction, not a re-basing of the whole history** — registered **2026-09-15 at D-13**, deliberately on the opposite side of `-1`, which stands unedited and is scored on its own merits the same day. `-1` was registered on **one** measured vintage pair; Census's benchmark archive (`www.census.gov/retail/mrts/www/benchmark/<year>/excel/benchsales<yy>.xls[x]`, 18 vintages 2008-2025, all HTTP 200, `.xls` parsed via xlrd and `.xlsx` via openpyxl, each vintage adding exactly 12 months) supplies **17** consecutive revisions, and that pair turns out to be one of only two exceptions. Scored on `-1`'s own bar (months at least 4 calendar years older than the benchmark moving by ≥0.05% — for the 2026 benchmark that is exactly `-1`'s "pre-2023" cut), reach is **bimodal**: **15 of 17 at ≤28.2%** (median **4.2%**; sorted 0.0 0.3 0.4 1.1 1.2 1.7 1.8 3.7 4.2 4.5 6.0 11.9 14.8 25.6 28.2) and **2 at exactly 100%** (benchmarks **2012** and **2025**), with no revision ever landing between the modes. Kind-of-business relabeling does **not** separate them — 2012 re-based with zero label changes while 2014/2017/2018 relabeled at 0.0/0.4/1.1% reach — so this is a base rate, not a rule. `symbols: []`, so no cached instrument applies | **Re-downloading `www.census.gov/retail/mrts/www/mrtssales92-present.xlsx` after the 09-28 release and finding FEWER THAN 30% of pre-2023 months changed by ≥0.05%** against `-1`'s six pinned anchors (currently published NSA headline, $M: 1992-06 **163,417** · 2000-06 **274,749** · 2010-06 **351,056** · 2019-06 **505,132** · 2022-06 **676,507** · 2023-06 **688,753**), re-fetched at score time and never read from memory. The 30% line sits inside the empty band between the two observed modes (28.2% → 100%), so it classifies rather than guesses a threshold: every ordinary revision in 17 clears it, every re-basing fails it. Registered at **Medium** confidence, not High, on a base rate of **15/17 = 88.2%** discounted by the one precedent in kind — the 2025 benchmark was itself AIES-driven and re-based everything, and Census's 09-28 notice names the 2023 and 2024 AIES too. A pass means stored retail history older than ~2023 survives 09-28 intact and only recent years need rebuilding; a fail means `-1` is right and every comparison spanning 09-28 crosses two series. Zero capital — `estimate`-dated, no symbols, no macro-keyed house playbook | **30% or more of the pre-2023 months moving by ≥0.05%** kills it (and is `-1` passing — the two cannot both survive). **Void** (not killed) under exactly `-1`'s conditions: Census not publishing on 09-28 at all, or the current-file URL ceasing to resolve — a slip and a moved path are both silence about reach | 2026-10-05 | _open_ |
