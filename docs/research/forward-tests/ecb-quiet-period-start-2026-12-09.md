# Forward tests — ecb-quiet-period-start-2026-12-09

<!-- One event's pre-registered hypotheses, written ONLY by the lane that owns
     docs/research/events/ecb-quiet-period-start-2026-12-09.md — never by a sibling lane, which is what lets every
     research PR merge without touching a shared file (issue #1449). The register at
     ../forward-tests.md is composed from these files; never add a row there. Ids are
     FT-ecb-quiet-period-start-2026-12-09-<n>, <n> counting up within this file. Rows append only; the Outcome
     column is the one cell the close-out fills. -->

| # | Hypothesis | Prediction | Kill switch | Score by | Outcome |
|---|---|---|---|---|---|
| FT-ecb-quiet-period-start-2026-12-09-1 | **The quiet window is sized by its content, not by its silence — the sibling null's own explanation, tested on the one window that can discriminate it** — registered **2026-09-05 at D-95** of the `estimate`-dated [ecb-quiet-period-start-2026-12-09](../events/ecb-quiet-period-start-2026-12-09.md). [`ecb-quiet-period-start-2026-10-21`](../events/ecb-quiet-period-start-2026-10-21.md) measured ECB quiet windows at **0.97×** (`^STOXX50E`) / **0.99×** (`^GDAXI`) an ordinary day across the 13 scheduled meetings since 2025, **0.69×/0.68×** once the two largest windows are trimmed, and then explained those two exceptions with a mechanism in its own words: *"the quiet period neither manufactures volatility nor suppresses it. When something real lands inside, the window is enormous; absent that, it is quieter than an ordinary day."* Those two claims are compatible in that sample **only because none of its 13 windows had a top-tier shock scheduled in advance**. **December 2026 does**, and it is the only such window in the tracked calendar: on the binding Guiding Principles the clause runs **2026-12-09 → 12-15**, which contains the **FOMC decision plus dot plot on 12-09**, **CPI on 12-10** and the **CR expiry / government funding deadline on 12-11**. So the two readings split — "content sets size" predicts an above-baseline window, "quiet windows are quiet" predicts at-or-below | **`^STOXX50E` mean close-to-close \|ret\| over the quiet window 2026-12-09 → 2026-12-15 comes in ABOVE the sibling's 0.75% all-days baseline** (ratio > 1.0), scored on Yahoo adjusted daily bars via `scripts/research/market-data.mjs` with the instrument cache busted first. **The prior is stated at registration and runs AGAINST this prediction:** 9 of the sibling's 13 windows came in below baseline (median window ratio 0.69×), so the unconditional base rate favours a fail at roughly 2-to-1; the entire case for the prediction is the pre-loaded content, which is exactly what makes the test discriminating rather than confirmatory. Anchors on the record: baseline 0.75% (n=417, 2025-01-01 → 2026-09-04), VIX **14.53**, EUR/USD **1.1621**, Brent **95.83** (all 09-04 closes) | A ratio **≤ 1.0** kills the "content sets window size" reading on its cleanest available test and gives the damping reading real support — [`ecb-quiet-period-start-2026-10-21`](../events/ecb-quiet-period-start-2026-10-21.md) leg 4 would need re-argument, not patching. **Void** (not scored) if the ECB moves the 16–17 December meeting (the window moves with Day 1) or if the Fed moves the Dec 8–9 FOMC (the pre-loaded content this test rests on leaves the window). **Inherited-failure mode, named before the fact:** the 0.75% baseline is a sibling's measurement this session did not reproduce, so a fail could reflect a mis-measured baseline rather than a wrong mechanism; n=1 either way, and a pass is one observation, never a promotion | 2026-12-16 | _open_ |
