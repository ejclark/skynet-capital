# Forward tests — opex-2026-09-18

| # | Hypothesis | Prediction | Kill switch | Score by | Outcome |
|---|---|---|---|---|---|
| FT-opex-2026-09-18-1 | Leg 5 of [`opex-2026-09-18`](../events/opex-2026-09-18.md) — the post-witching "unpin": when the quarter's largest block of dealer gamma rolls off, the stabilizing hedge flow goes with it and realized volatility rises. Literature-sourced (SpotGamma / MenthorQ / StrikeWatch) and never tested against this repo's own data; registered at D-10 (2026-09-08) precisely because the D-10 gamma read came back too dispersed to assume the regime — four vendors put the SPX flip at 7,651–7,741 around a spot of 7,718.60 and two disagreed on the sign. | SPX close-to-close realized volatility over the **three sessions after** the 2026-09-18 triple witching (09-21, 09-22, 09-23) exceeds realized volatility over the **three sessions before** it (09-15, 09-16, 09-17). Confidence **low** — a stand-aside-sized measurement, not a bet. Deliberately stacked against itself: the pre-window contains the 09-16 FOMC *and* the 09-16 VIX expiration, so the comparison hands the "before" side the corridor's two loudest vol events. A pass under that handicap is worth more than a pass on a quiet pre-window; a fail is genuinely ambiguous and must be scored as such. | Post-window realized vol ≤ pre-window realized vol. Also killed if the pre-window is contaminated beyond the FOMC by an unscheduled shock (a Hormuz closure, an intervention, a BoJ surprise large enough to gap the 09-18 open on its own) — in which case score `n/a`, not `pass`/`kill`, and say why. | 2026-09-24 (`closeOutWithinDays: 6` from the 09-18 event date) | |

**Scoring note.** Score from re-run instrument data with the cache busted first
(`rm -rf node_modules/.cache/earnings-cycle node_modules/.cache/intraday-edges`), never from memory
of the tape. **One data trap is already known and must be honoured:** both investing.com's VIX
history and Yahoo's `^VIX` carry a bar stamped **2026-09-07**, a day US equity *and* bond markets
were shut ([`labor-day-market-closure-2026-09-07`](../events/labor-day-market-closure-2026-09-07.md)),
while `^GSPC` correctly stops at 09-04. That bar is an extended/global-hours artifact and is
excluded from any session count; this test reads SPX regular-session closes only.
