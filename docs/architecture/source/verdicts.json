[
  {
    "label": "refute:runtime:0",
    "element": "Browser app container (React 19 observatory shell at /app)",
    "grounded": true,
    "evidence_for": "app/package.json deps: react ^19, @tanstack/react-router ^1.168, @tanstack/react-query ^5.102, zustand ^5, lightweight-charts 5.2.1; devDeps @rsbuild/core + typescript. app/rsbuild.config.ts: entry ./src/main.tsx, output.assetPrefix \"/app/\", server.base \"/app\", dev proxy of /api, /events, /board, /login, /auth etc. to SKYNET_SERVER_ORIGIN default http://localhost:8787, TanStack router plugin registered at the rspack level. app/src/main.tsx:10 createRouter({ basepath: \"/app\" }). app/src/live/channel.ts:51 new EventSource(`/events?by=${metric}`), and lines 21-67 apply a patch only when it is seq+1, otherwise resnapshot, using Last-Event-ID. src/server/app-shell-routes.ts:44-58 serveAppShell resolves app/dist and returns index.html for SPA routes. src/server/dashboard-server.ts:215-217 has the comment \"static app/dist behind the same gate as the board\" and calls serveAppShell. The claimed features exist: app/src/shell/mission-control.tsx; app/src/live/playbook-store.ts, moneypenny*.ts, ticket.ts, options.ts; routes accounts, trade, learn, playbooks, research, settings, join, onboarding, leaderboard.",
    "evidence_against": "These are small inaccuracies that don't mislead. app/src/live/post.ts postJson(url, body) takes any URL, not only /api/*. The shell also POSTs to bare paths like /feedback/coach (dashboard-server.ts:200). The SSE stream is one EventSource per visible metric (/events?by=<metric>), not a single stream. The browser also GETs the /board/frame fallback when it hits a seq gap. Routes also include activity, feedback and u/$id/* (a member's decisions, playbooks and pulse), which the claim doesn't list.",
    "correction": "Optional refinements: label the stream as \"SSE /events?by=<metric> (seq patches; resnapshot via /board/frame on a gap)\", and label writes as \"JSON POST via postJson (mostly /api/*, plus /feedback/coach)\". In dev, Rsbuild serves at :5174 and proxies to the node server at :8787. In production, the node server serves app/dist."
  },
  {
    "label": "refute:runtime:1",
    "element": "Observatory server (Fly app skynet-capital, process `app`, port 8787 + internal 8788 bridge)",
    "grounded": true,
    "evidence_for": "mise.toml:6 node=\"24\", and Dockerfile:2 FROM node:24-slim. package.json:18 \"serve:dashboard\": \"tsx src/scripts/serve-dashboard.ts\". fly.toml [processes] app = \"npm run serve:dashboard\", app=\"skynet-capital\", [http_service] internal_port=8787, plus the comment that 8788 is deliberately left out of that block so it stays unforwarded. src/server/resolve-port.ts: env.PORT ?? env.SKYNET_DASHBOARD_PORT, default 8787. serve-dashboard.ts:49 PORT=resolvePort(process.env), :209 createDashboardServer({...}), :320 .listen(PORT). src/scripts/dashboard-insights-bridge.ts:96/109 uses resolveInsightsBridgePort + createInsightsListener. src/server/insights-listener.ts:99-102 defaults to 8788 (SKYNET_INSIGHTS_BRIDGE_PORT overrides) and is built on node:http createServer. Bots poll GET /controls and POST /insights and /decisions, which matches \"poll/post\". The fly.toml [mounts] skynet_data->/data is scoped to processes=[\"app\"], and every SKYNET_*_DIR/FILE store points at /data. The fly.toml comment says the bots app has no volume, which backs \"owns every durable store\". SSE lives in src/server/sse.ts, /tower in src/server/dashboard-board-routes.ts:27, the Anthropic companion in src/companion/companion-anthropic-stream.ts, and the dashboard-*.ts wiring scripts exist (access, companion, desk-wiring, feedback, ladder-progress, ops-status, insights-bridge).",
    "evidence_against": "These are minor points. \"ObservatoryHub\" shows up only in comments (src/observatory/events.ts:52, ceremony-channel.ts:9); I did not find a class definition, so the hub may be named differently in code. The 8788 port is an env-overridable default (SKYNET_INSIGHTS_BRIDGE_PORT), not a fixed port. The claim's code_roots list includes src/scripts/dashboard-*.ts, but it leaves out src/autonomous and src/http, even though the listener imports its wire contracts from src/autonomous (bot-controls, decision-wire, insight-record, subscriptions-wire, bot-credentials-wire).",
    "correction": "Label the 8788 bridge by its real routes: GET /controls (it also carries decisionsCursor and subscriptions), POST /insights, POST /decisions, and a bot-credentials route. Note that its port can be overridden with SKYNET_INSIGHTS_BRIDGE_PORT. Add src/autonomous/*-wire.ts to the code roots as the shared bridge contract. Check the actual symbol name for \"ObservatoryHub\" before using it as a component label."
  },
  {
    "label": "refute:runtime:3",
    "element": "CLAIM #3 Observatory volume (Fly volume skynet_data at /data): JSON / JSONL / AES-256-GCM envelope / node:sqlite decisions.db stores",
    "grounded": true,
    "evidence_for": "fly.toml [mounts] source=\"skynet_data\" destination=\"/data\" processes=[\"app\"], and its [env] block pins SKYNET_PARTICIPANT_STORE=/data/participants.json, HISTORY_DIR, ALLOWLIST_STORE, CONTROLS_FILE, ACTIVITY_DIR, FEEDBACK_LOG_DIR, COMPANION_MESSAGE_LOG_DIR, ORDER_AUDIT_DIR, ALERT_DISMISSALS_DIR, OWNER_LINKS_FILE, PROGRESSION_FILE, LADDER_PROGRESS_DIR, SUBSCRIPTIONS_FILE and INSIGHTS_DIR, all under /data. src/runtime/volume-guard.ts:10-26 has PERSISTED_STORES with the same list plus SKYNET_COMMUNITY_PROGRESSION_FILE. tests/arch/volume-persistence.spec.ts:26-34 lists EPHEMERAL, whose one entry is SKYNET_COMMUNITY_PROGRESSION_FILE, and :119-124 fails the test when a store is not pinned. src/storage/secure-envelope.ts:5,40,58 confirms AES-256-GCM keyed by SKYNET_STORE_SECRET. src/scripts/dashboard-insights-bridge.ts:40 calls openDecisionDb(join(dir,\"decisions.db\")) for the \"app-side replication store\". src/autonomous/decision-db.ts:3 imports DatabaseSync from node:sqlite.",
    "evidence_against": "The record list leaves some things out and is imprecise in places. (1) council.json is missing from the list, even though src/server/council-store.ts is a code_root. councilFilePathFrom (council-store.ts:123-127) writes it next to bot-controls.json, so it lands at /data/council.json without its own env var. (2) iv-history/ is missing. fly.toml pins SKYNET_IV_HISTORY_DIR=/data/iv-history (src/research/iv-history-store.ts, iv-sampler.ts:47), but it is not in PERSISTED_STORES and has no relative default. (3) The claim marks only participants.json as encrypted. allowlist.json is also sealed with secure-envelope (allowlist-store.ts:3,82-83,111) and refuses to write without SKYNET_STORE_SECRET. (4) community-progression.json is in PERSISTED_STORES but deliberately NOT on the volume (the EPHEMERAL exception). The claim does say this, but the diagram should not draw it inside the volume.",
    "correction": "Keep the element as drawn and add three things. First, council.json: a sibling of bot-controls.json, derived by councilFilePathFrom, with no env var. Second, iv-history/: SKYNET_IV_HISTORY_DIR, which has no default, so the IV clock is off unless the var is pinned. Third, mark allowlist.json as envelope-encrypted along with participants.json. Draw community-progression.json outside the volume, as container-ephemeral."
  },
  {
    "label": "refute:runtime:2",
    "element": "CLAIM #2 Bot runtime container (skynet-capital-bots, process `bots`)",
    "grounded": true,
    "evidence_for": "fly.bots.toml: app=\"skynet-capital-bots\", [processes] bots=\"npm run run:autonomous\", no [http_service] (the header comment explicitly forbids adding one), [env] SKYNET_AUTONOMOUS_MODE=\"observe\", SKYNET_AUTONOMOUS_BOTS=\"sauron\", SKYNET_INSIGHTS_BRIDGE_URL=\"http://app.process.skynet-capital.internal:8788\", SKYNET_BOTS_DB_PATH=/data/bots-state.db, SKYNET_AUDIT_DIR=/data/audit, SKYNET_BOTS_HEALTH_PATH=/data/health.json, [mounts] skynet_bots_data -> /data. package.json:23 \"run:autonomous\": \"tsx src/scripts/run-autonomous.ts\"; .nvmrc = 24. node:sqlite is used in src/autonomous/bots-state-db.ts, decision-db.ts and decision-db-rows.ts. src/scripts/run-autonomous.ts:79 has a 10-symbol UNIVERSE, :80 LIVE_EVAL_INTERVAL_MS=15_000, :81 NEWS_POLL_MS=60_000 with setInterval(pollNews) at :240, :258 new SafetyController(), :262-264 blockedReason = safety.blockedReason() ?? controls.suspendedReason(\"\"), :353 new LiveCycleRunner, :383 gates evaluation on the 15s interval and marketClock.isOpen(), :394 marketDataStream.start(). Credential rotation: resolveBotCredentialsClient at :104 swaps brokers in place, and credentials.reconcile runs from the controls poll at :127-128. :119 resolveDecisionReplication handles replicating decisions. autonomous-sinks.ts decisionSink writes JSONL audit + DecisionDb. I found no createServer or .listen( calls in src/autonomous, src/scripts/run-autonomous.ts, src/scripts/autonomous-*.ts, src/bots, src/engine, src/news, src/alpaca or src/adapters. pipeline.yml has a deploy-bots job that reuses the dashboard image, and scripts/bot-relevant.mjs exists.",
    "evidence_against": "The claim says \"replicates decisions and insights\". Decisions are replicated: src/autonomous/decision-replication-client.ts is imported at run-autonomous.ts:42/119. Insights are not. resolveInsightBridge (src/autonomous/insight-bridge-client.ts:34) has no production caller anywhere in src, so the bots process does not replicate insights today. The bridge target is also the dashboard app's `app` process listener (src/server/insights-listener.ts, port 8788, via SKYNET_INSIGHTS_BRIDGE_URL). It is not a separate \"observatory\" app. Decision replication and the /controls poll share one 30s cycle (decision-replication-client.ts header comment).",
    "correction": "Change the replication edge to read \"replicates decisions (cursor batches via decision-replication-client) to the dashboard app's insights listener (app.process.skynet-capital.internal:8788) over 6PN, piggybacked on the 30s /controls poll\". Remove \"insights\": insight-bridge-client.resolveInsightBridge is unwired dead code. If \"observatory\" is meant as a separate container, retarget the edge to the dashboard/app container."
  },
  {
    "label": "refute:runtime:4",
    "element": "Bots volume (Fly volume skynet_bots_data at /data): bots-state.db + sibling decisions.db (node:sqlite), JSONL audit, health.json",
    "grounded": true,
    "evidence_for": "All six cited files exist. fly.bots.toml:69-72 is [mounts] source=\"skynet_bots_data\", destination=\"/data\", processes=[\"bots\"], and fly.bots.toml:34-38 gives the redeploy-survival rationale. The env at fly.bots.toml:56-64 sets SKYNET_BOTS_DB_PATH=/data/bots-state.db, SKYNET_AUDIT_DIR=/data/audit and SKYNET_BOTS_HEALTH_PATH=/data/health.json. bots-state-db.ts:1,51 uses DatabaseSync from node:sqlite, and its current-state tables are momentum, sentiment, cooldowns (per persona) and scout_state (lines 53-61; doc comment at 14-15 and 26-46). decision-db.ts:3,112 is a separate DatabaseSync file. Its doc (lines 21-30) says it is append-only, with one intents row per raw intent. Its tables are decisions, intents, playbook_verdicts, market_signals and retrospectives (118-181). decisionDbPathFrom at decision-db.ts:95 derives a sibling decisions.db. autonomous-live-wiring.ts:148 seedBotsState and :169 seedDecisionDb both wrap the open in try/catch (best-effort, never fails boot). autonomous-sinks.ts:26-27 auditStore returns a JsonlAuditStore when SKYNET_AUDIT_DIR is set, and decisionSink is at :65. bots-health-file.ts:24-29 holds gitSha (GIT_SHA), bridge and the last /controls poll. smoke-bots.sh:34,53 reads /data/health.json via `flyctl machine exec ... cat`.",
    "evidence_against": "Minor imprecisions only. The \"funnel\" is not stored state. funnelFor/computeFunnel (decision-db.ts:7,60-64) computes it at read time from the intents/decisions rows. The journal also holds playbook_verdicts and market_signals tables, which the claim leaves out. The label never names the sibling file (decisions.db). Both SQLite stores are dark unless SKYNET_BOTS_DB_PATH is set; it is set in fly.bots.toml.",
    "correction": "Name the files explicitly: /data/bots-state.db (current-state upsert tables: momentum, sentiment, cooldowns, scout_state) and /data/decisions.db (append-only journal: decisions, intents, playbook_verdicts, market_signals, retrospectives; path from decisionDbPathFrom), plus /data/audit (JSONL, SKYNET_AUDIT_DIR) and /data/health.json (SKYNET_BOTS_HEALTH_PATH). Label the funnel as computed at read time from the journal (funnelFor), not stored. Note that both DBs open best-effort and are dark without SKYNET_BOTS_DB_PATH."
  },
  {
    "label": "refute:runtime:5",
    "element": "CLAIM #5 CI and agent lanes container (GitHub Actions workflows: pipeline/claude/moneypenny-events/moneypenny-repair/autonomy-ops/fly-logs/companion-eval)",
    "grounded": true,
    "evidence_for": "All 7 workflows exist in /home/user/skynet-capital/.github/workflows, and every cited script exists. pipeline.yml: verify job (l.68, ubuntu-latest, runs commitlint on the PR title at l.157 plus typecheck/lint/test). e2e job (l.203, display name \"integration tests\", runs `npx playwright test` at l.262). arm-auto-merge (l.272, needs [verify, e2e], checks the hold-merge label and runs scripts/envelope-scan.mjs --check at ~l.353-362, arms with the App token). deploy (l.378, push to main only): `npx semantic-release` l.452, superfly/flyctl-actions/setup-flyctl l.455, `flyctl deploy --remote-only` l.476, `bash scripts/smoke.sh https://skynet-capital.fly.dev` l.482, rollback to the previous image on smoke failure l.495-499. It outputs `image` for reuse. deploy-bots (l.503, needs deploy): preflight via scripts/bots-deploy-preflight.mjs (bot-relevant.mjs is the classifier), stamps GIT_SHA into /tmp/fly.bots.deploy.toml l.608, then `flyctl deploy --config /tmp/fly.bots.deploy.toml --image` l.610, smoke-bots.sh l.617, rollback l.624-627. claude-code-action is pinned to SHA cfc3eb22 with a '# v1 tag' comment (claude.yml:168, moneypenny-events.yml:153/473/612/701, moneypenny-repair.yml:157/196/221). claude.yml triggers on issues opened/labeled, issue_comment, and pull_request_review_comment. moneypenny-events.yml triggers on push, issues, and issue_comment (the comment says 'no cron', l.64) and has jobs dep-warden/route/build-events/build-feedback/build-plan. moneypenny-repair.yml triggers on workflow_run completed for the other workflows. autonomy-ops/fly-logs/companion-eval are workflow_dispatch only (flip-mode, secrets-type actions, logs, eval). No workflow has a cron schedule. docs/ROUTINES.md:42 names the Secretary digest Routine trig_01KaMC2uR3cFW5XTUL6rzPuS as a daily claude.ai Routine.",
    "evidence_against": "1) 'actions/setup-node from mise.toml' is wrong. Every setup-node@v7 step uses `node-version-file: .nvmrc` (pipeline.yml:89,234,322,400), and the comment says .nvmrc is the single source of truth for CI because setup-node cannot reliably resolve mise.toml. mise.toml only drives local dev and session-hook provisioning. 2) The verify → integration tests → arm-auto-merge chain is pull_request-only. e2e also runs on push to main (#3333). 3) deploy-bots can also run on workflow_dispatch with force_bots_deploy, and it has its own smoke-bots.sh check and rollback. The claim implies the only check is smoke.sh on the dashboard. 4) moneypenny-events fires on push, issue labels, and issue_comment (the plan ready-flip), not on 'every label' in general. 5) scripts/ship.sh is a local/session PR tool (the /ship skill), not something CI runs. 6) The claim's own wording 'the only clock is the Secretary Routine' fits docs/ROUTINES.md's no-cron rule. It is still a claude.ai Routine outside CI, so the lane has no clock of its own.",
    "correction": "Change the node source to 'actions/setup-node@v7 (node-version-file: .nvmrc; mise.toml is local-dev only)'. Mark verify/e2e/arm-auto-merge as pull_request lanes (e2e also runs on push to main), with arm-auto-merge gated by the hold-merge label plus scripts/envelope-scan.mjs. Show deploy-bots as: needs deploy → bots-deploy-preflight.mjs (uses bot-relevant.mjs) → flyctl deploy of the reused image to skynet-capital-bots with GIT_SHA stamped → smoke-bots.sh → rollback. It can also be dispatched with force_bots_deploy. Put scripts/ship.sh on the session/agent side, not inside the CI container. Add issue_comment (ready-flip) to the moneypenny-events triggers."
  },
  {
    "label": "refute:runtime:6",
    "element": "CLAIM #6 Test suites container (Rstest + happy-dom app suite, Playwright e2e/auth/ct, fast-check, tests/arch gates, offline fixtures)",
    "grounded": true,
    "evidence_for": "/home/user/skynet-capital/rstest.config.ts has testEnvironment \"node\" and include [\"tests/**/*.spec.ts\"]. /home/user/skynet-capital/app/rstest.config.ts uses happy-dom with the pluginReact plugin, and app/package.json lists @testing-library/react, jest-dom and user-event. The webServer command in /home/user/skynet-capital/playwright.config.ts is exactly `npm run build --prefix app && npm run serve:dashboard:offline` on :8787, and it ignores login.spec.ts and ct/**. playwright.auth.config.ts matches only **/login.spec.ts, using fake OAuth env. app/playwright-ct.config.ts sets testDir ../e2e/ct. Both configs build on e2e/playwright.shared.ts through SHARED_CONFIG. The root package.json pins @playwright/test 1.62.1, @rstest/core 0.12.0 and fast-check 4.10.1. package.json:19 defines serve:dashboard:offline as SKYNET_DATA_SOURCE=offline. src/scripts/serve-dashboard.ts:7,58 reads fixtures/offline, which holds participants.json and events.jsonl. tests/arch/volume-persistence.spec.ts:3 imports PERSISTED_STORES from src/runtime/volume-guard.ts, and line 60 reads fly.toml. src/runtime/data-source.ts and docs/adr/0002-offline-data-source-mode.md both exist. tests/arch/god-file.spec.ts runs scripts/arch-scan.mjs through advisoryScan, and the dead, dupe, spec-gap and envelope specs exist.",
    "evidence_against": "The claim says tests/arch runs `scripts/*-scan.mjs` coaches including volume-persistence, fly-split and workflows. No such scan scripts exist. volume-persistence.spec.ts and fly-split.spec.ts (which does readFileSync on fly.toml and fly.bots.toml) are gates written inline in the spec. workflows.spec.ts calls scripts/workflow-lint.mjs, not a *-scan.mjs. The app package pins @rstest/core ^0.11.9, not 0.12, so \"Rstest 0.12\" is only true for the root suite. The list of coaches is also incomplete: tests/arch has about 48 specs covering many more scans (clone, comms, doc-rot, digest, test-quality, mermaid-lint, and others).",
    "correction": "Change the label to say that tests/arch gates either invoke scripts/*-scan.mjs coaches (arch-scan via god-file.spec, dead, dupe, spec-gap, envelope, clone, doc-rot, and others) or assert invariants inline. volume-persistence.spec.ts is inline: volume-guard PERSISTED_STORES checked against fly.toml. fly-split.spec.ts is inline: fly.toml checked against fly.bots.toml. workflows.spec.ts uses scripts/workflow-lint.mjs. Note that the app suite runs @rstest/core ^0.11.9 while the root suite runs 0.12.0."
  },
  {
    "label": "refute:runtime:7",
    "element": "CLAIM #7 member → browser: Uses the observatory shell",
    "grounded": true,
    "evidence_for": "src/server/app-shell-routes.ts:5-7 describes `/app` as \"the React observatory shell, served as static files from app/dist BEHIND the same auth gate\". serveAppShell is at app-shell-routes.ts:44, isAppShellPath at :36-38, and it falls back to the SPA's index.html. In src/server/dashboard-server.ts:88, handleRequest calls gateRequest and then serveAuthorizedRoute. That route serves the shell at :215-218 (isAppShellPath → serveAppShell) after legacy redirects. serveHomePage (:106-121) sends a 302 for / and /index.html to `/app/` plus the query string. The comment at :105 reads \"every sign-in and every bare visit lands in the shell\". dashboard-auth-gate.ts:13-50 sends unauthenticated users to /login with a 302, so a member only reaches the shell after signing in with OAuth.",
    "evidence_against": "The arrow \"gateRequest → serveAppShell\" is wrong. gateRequest (dashboard-auth-gate.ts) never calls serveAppShell. It returns {handled:false, session}, and handleRequest in dashboard-server.ts then sends the request to serveAuthorizedRoute, which calls serveAppShell (dashboard-server.ts:88-93, 215-218). Two smaller points: when app/dist is missing the shell returns 404 (app-shell-routes.ts:50-56), and the legacy shared-password mode also passes through the gate.",
    "correction": "Fix the evidence chain: dashboard-server.ts handleRequest → gateRequest (auth passes) → serveAuthorizedRoute → serveAppShell for /app/*. gateRequest does not call serveAppShell. The relationship label is fine as is: member uses a browser to reach the React observatory shell (static app/dist at /app, behind the auth gate)."
  },
  {
    "label": "refute:runtime:8",
    "element": "CLAIM #8: eric -> browser: Mission Control suspend/mode toggles, invites, claims",
    "grounded": true,
    "evidence_for": "The cited paths exist. app/src/shell/mission-control.tsx:27-37 calls fetchControls/postControlAction and shows suspend-all / resume / suspend buttons (lines 66-106), plus a companion-model switch (116-130). It renders only for owners (line 30). app/src/live/controls.ts:30,47 does GET/POST /api/controls. app/src/live/admin.ts:39-55 does GET/POST /api/admin/invite and /api/admin/claim. src/server/admin-api-routes.ts:185-197 handles /api/admin/invite and /api/admin/claim as owner-only. isOwnerOf is defined at src/server/dashboard-auth-gate.ts:60.",
    "evidence_against": "The isOwnerOf(config.controls|invite|claim) calls are not in dashboard-auth-gate.ts. They are in src/server/dashboard-server.ts:165-167; dashboard-auth-gate.ts only defines the helper. The server handler for /api/controls is src/server/controls-api-routes.ts:64, which is not cited. Invites and claims are not part of Mission Control: their UI is app/src/shell/admin-cards.tsx and app/src/routes/settings.tsx. The \"mode\" toggle in Mission Control is really a per-bot suspend/resume toggle plus a companion-model selector (set-companion-model).",
    "correction": "Relabel the edge as: \"Owner toggles suspend/resume (global and per bot) and Moneypenny's model in Mission Control, and manages invites and claims on the settings admin cards\". Cite: mission-control.tsx -> app/src/live/controls.ts -> /api/controls (src/server/controls-api-routes.ts); app/src/shell/admin-cards.tsx and app/src/routes/settings.tsx -> app/src/live/admin.ts -> /api/admin/invite and /api/admin/claim (src/server/admin-api-routes.ts). Owner gating: isOwnerOf is defined in src/server/dashboard-auth-gate.ts:60 and applied in src/server/dashboard-server.ts:165-167."
  },
  {
    "label": "refute:runtime:9",
    "element": "eric -> ci: Runs autonomy-ops, fly-logs, companion-eval buttons",
    "grounded": true,
    "evidence_for": "All three workflows exist and are triggered only by workflow_dispatch: .github/workflows/autonomy-ops.yml:17-18, fly-logs.yml:16-17, companion-eval.yml:30-31. autonomy-ops.yml sets the named secrets through flyctl: SKYNET_AUTONOMOUS_MODE at :226-227 (flip-mode), SKYNET_PLAYBOOKS at :158-163, SKYNET_BETA_FORCING at :176-181, and SKYNET_HARDCORE_BOTS at :193-198. The action choices are status, machine-status, logs, bootstrap-bots-app, flip-mode, set-playbooks, set-beta-forcing and set-hardcore (:24-32).",
    "evidence_against": "These are minor points that do not refute the claim. autonomy-ops also runs status, logs and bootstrap actions, and it can unset secrets as well as set them (e.g. :160 and :178). It targets a Fly app chosen through the target_app input, which defaults to skynet-capital-bots (:33-43). Runs are gated by the required reviewers on the `autonomy-ops` GitHub Environment (header :6-8). The header also says bot Alpaca credentials are deliberately not set by this workflow (:10-15). Other workflows are not triggered this way: pipeline, claude, moneypenny-events and moneypenny-repair are not in this relationship.",
    "correction": "Optional clarification: autonomy-ops acts on the Fly app picked by target_app (default skynet-capital-bots) and needs the autonomy-ops environment reviewer to approve before it runs. It never sets bot Alpaca credentials."
  },
  {
    "label": "refute:runtime:10",
    "element": "CLAIM #10: browser → api relationship (GET /api/* JSON, POST writes, EventSource /events and /api/trade/events, fetch-streamed POST /api/companion)",
    "grounded": true,
    "evidence_for": "app/src/live/channel.ts:51 `new EventSource(`/events?by=${metric}`, { withCredentials: true })` and :34 fetchBoard; server /api/board at src/server/content-api-routes.ts:151, and the /events patch stream at src/server/board-patch-routes.ts:16,116, dispatched from src/server/dashboard-server.ts:178. app/src/live/post.ts:6-18 postJson is the single same-origin JSON POST helper for all writes. app/src/live/desk-events.ts:49 EventSource `/api/trade/events?participantId=…`; server DESK_EVENTS_PATH at src/server/desk-events-route.ts:30,66. app/src/live/companion.ts:68 fetch POST, then reads the SSE body frame by frame with a reader (:77-); server route list at src/server/companion-routes.ts:25-28,208-219. content-api-routes.ts:57-164 serves the GET /api/* JSON (wire, research, learn, ops-status, board, networth, equity-curve, desk).",
    "evidence_against": "The streamed POST goes to /api/companion/chat, not /api/companion (companion.ts:68; companion-routes.ts:27,215). GET /api/companion is plain JSON that says whether the companion is enabled (companion.ts:19; companion-routes.ts:211), and POST /api/companion/ack is a non-streamed write (companion.ts:29; companion-routes.ts:219). /events is not under /api/. It is dispatched in dashboard-server.ts:178 and gated in dashboard-auth-gate.ts:39. board-patch-routes.ts only implements it, so the cited server file list leaves out the actual router. Write POSTs are spread across many *-api-routes.ts files (controls, council, feedback, admin, draft-order), not only the ones cited.",
    "correction": "Relabel the companion edge as \"GET /api/companion (enabled JSON); POST /api/companion/chat (fetch-streamed SSE: delta/handoff/done/error); POST /api/companion/ack\". Note that /events (seq-numbered board patch SSE, auth-gated, routed in src/server/dashboard-server.ts:178) sits outside /api/*. It is recovered via GET /api/board snapshot on a gap."
  },
  {
    "label": "refute:runtime:11",
    "element": "api → appvol: \"Reads and writes every durable store\"",
    "grounded": true,
    "evidence_for": "fly.toml:86-89 mounts volume skynet_data at /data, scoped to processes=[\"app\"], and the api process runs `npm run serve:dashboard` (fly.toml:67-68). The [env] block pins 15 stores under /data/: lines 15, 19, 27-30, 34-35, 38, 40, 43, 50, 53, 57 (SKYNET_IV_HISTORY_DIR) and 65 (SKYNET_INSIGHTS_DIR). src/runtime/volume-guard.ts:11-27 lists PERSISTED_STORES, and volumePersistenceWarnings (line ~45) warns at boot if any of them resolves outside /data on Fly. src/scripts/dashboard-insights-bridge.ts:37-40 opens openDecisionDb(join(SKYNET_INSIGHTS_DIR,\"decisions.db\")). The comments in fly.toml confirm the bots app has no volume of its own and sends its durable writes over the 6PN bridge (port 8788) to this app, so api really is the only process that writes to the volume. src/storage/ holds the file-backed primitives: json-file-store.ts, jsonl-store.ts and participant-state.ts.",
    "evidence_against": "\"Every\" overstates it. (1) SKYNET_COMMUNITY_PROGRESSION_FILE is in PERSISTED_STORES (volume-guard.ts) but is deliberately left off the volume: tests/arch/volume-persistence.spec.ts:26-35 records it as EPHEMERAL, and it is not in fly.toml [env]. It defaults to data/community-progression.json inside the container, so each deploy erases it. (2) SKYNET_IV_HISTORY_DIR is pinned in fly.toml:57 but is missing from volume-guard's PERSISTED_STORES, so the claim's two cited sources disagree. (3) Some durable state lives outside the volume entirely: feedback images are committed to a GitHub asset branch through the REST API (src/server/feedback-images.ts:91-132), and the Fly secrets and broker state are external too. (4) src/storage/in-memory-keyed-store.ts is not durable. The stores sit on the volume as JSON/JSONL files plus a SQLite decisions.db, not as a database service.",
    "correction": "Relabel the edge as: \"Reads/writes all volume-pinned stores (JSON/JSONL files plus SQLite decisions.db under /data; bots-app writes arrive via the :8788 insight bridge).\" Add a note that community-progression claim-state is deliberately ephemeral and stays in the container, and that feedback images go to GitHub, not the volume."
  },
  {
    "label": "refute:runtime:12",
    "element": "CLAIM #12 bots → botvol: persists momentum/sentiment/cooldowns/scout state, decision journal, JSONL audit, health stamp",
    "grounded": true,
    "evidence_for": "fly.bots.toml [mounts] source=skynet_bots_data at /data, processes=[\"bots\"]. [env] sets SKYNET_BOTS_DB_PATH=/data/bots-state.db, SKYNET_AUDIT_DIR=/data/audit and SKYNET_BOTS_HEALTH_PATH=/data/health.json. src/autonomous/bots-state-db.ts:53-61 creates the momentum, sentiment, cooldowns and scout_state tables. Its exports are restoreBotsState:193, persistSentiment:211, armMomentumPersistence:224 and scoutStateStore:154. All four are wired in src/scripts/run-autonomous.ts:212,234,357,376, and the DB is opened in src/scripts/autonomous-live-wiring.ts:149-153. The decision journal is src/autonomous/decision-db.ts:95 decisionDbPathFrom, which writes a sibling /data/decisions.db, opened by seedDecisionDb in autonomous-live-wiring.ts:169-174. The JSONL audit is src/scripts/autonomous-sinks.ts:26-27 (JsonlAuditStore on SKYNET_AUDIT_DIR). The health stamp is src/autonomous/bots-health-file.ts:62 (SKYNET_BOTS_HEALTH_PATH).",
    "evidence_against": "These are small gaps and nothing in the claim is wrong. (1) There are two SQLite files on the volume, bots-state.db and decisions.db. The decision journal gets no env var of its own; its path is taken from SKYNET_BOTS_DB_PATH. (2) The code that turns on the JSONL audit is src/scripts/autonomous-sinks.ts. The claim did not cite it. (3) Each store only writes if its env var is set, and a missing or corrupt file makes the bots start with empty state instead of failing boot.",
    "correction": "Optional detail for the diagram label: the volume at /data holds bots-state.db (momentum/sentiment/cooldowns/scout_state), decisions.db (decision journal, a sibling path taken from SKYNET_BOTS_DB_PATH), audit/*.jsonl (turned on in src/scripts/autonomous-sinks.ts) and health.json. Each store is off unless its env var is set."
  },
  {
    "label": "refute:runtime:13",
    "element": "CLAIM #13 bots -> api relationship (GET /controls 30s poll w/ decisionsCursor + Playbook Store subscriptions; GET /bot-credentials; POST /insights and /decisions)",
    "grounded": true,
    "evidence_for": "src/autonomous/bot-controls-client.ts:51 `const POLL_MS = 30_000;`, :100 GET, :123-124 reads body.decisionsCursor, :73 Playbook Store snapshot callback, :149 setInterval(fetchOnce, POLL_MS). src/autonomous/bot-credentials-client.ts:48-53 needs SKYNET_INSIGHTS_BRIDGE_URL + SKYNET_BOT_CREDENTIALS_BRIDGE_SECRET, :66-67 GET `${url}${BOT_CREDENTIALS_PATH}` with persona id param, driven by reconcile() on each /controls poll result (:13). src/autonomous/insight-bridge-client.ts:39 `/insights`, :47 POST. src/autonomous/decision-replication-client.ts:86 `/decisions`, :94 POST. src/server/insights-listener.ts:2 imports CONTROLS_BRIDGE_PATH, :5 BOT_CREDENTIALS_PATH, :20 DECISIONS_PATH=\"/decisions\", :41 MAX_DECISIONS_BODY_BYTES=4MB, :250-254 folds decisionsCursor into GET /controls response, :59-63 subscriptions snapshot folded in, :102 default port 8788. fly.bots.toml:55 SKYNET_INSIGHTS_BRIDGE_URL=\"http://app.process.skynet-capital.internal:8788\". fly.toml:77-79 comment that 8788 is not in [http_service] so not publicly forwarded.",
    "evidence_against": "Minor inaccuracies only: (1) /insights is POSTed one InsightRecord at a time (listener MAX_BODY_BYTES=16KB sized for a single record, insights-listener.ts:22-26), not batches; only /decisions carries batches (DecisionBatch, up to 100 records). (2) CONTROLS_BRIDGE_PATH and BOT_CREDENTIALS_PATH are not defined in insights-listener.ts; they are imported from src/autonomous/bot-controls.ts and src/autonomous/bot-credentials-wire.ts. (3) /bot-credentials pull is per-persona and only when credentialsVersion changes (plus boot prime), piggybacking the /controls poll rather than being an independent poll.",
    "correction": "Label: \"POST /insights (single record) and /decisions (batches <=100, 4MB cap)\"; note GET /bot-credentials is per-persona, triggered by credentialsVersion change on the /controls poll + boot prime, auth via SKYNET_BOT_CREDENTIALS_BRIDGE_SECRET header. Path constants live in src/autonomous/bot-controls.ts (CONTROLS_BRIDGE_PATH) and src/autonomous/bot-credentials-wire.ts (BOT_CREDENTIALS_PATH). Transport: plain HTTP over Fly 6PN private network to app process port 8788 (not publicly forwarded)."
  },
  {
    "label": "refute:runtime:14",
    "element": "api → alpaca relationship (#14)",
    "grounded": true,
    "evidence_for": "I checked every cited file and each part of the claim holds.\n\n- **Client factories:** src/runtime/data-source.ts:97-106 defines clientFactory, which builds AlpacaTradingClient against the paper base URL. optionsClientFactory builds AlpacaOptionsClient with both a trading transport and a data transport.\n- **Streams:** startStreams at :144-166 opens one AlpacaMarketDataStream for heldSymbols, using the first participant's credentials. It also opens a per-participant AlpacaTradeUpdatesStream through startParticipantStream at :117-134, tracked in the fillStreams map.\n- **Trading reads:** src/alpaca/alpaca-trading-client.ts:120 reads /v2/account and :124 reads /v2/positions. :187 lists /v2/orders, and :246-248 placeOrder POSTs to /v2/orders. It also covers portfolio history (/v2/account/portfolio/history).\n- **Options:** alpaca-options-client.ts:275/298/319 read /v2/options/contracts, :527/560 read /v1beta1/options/snapshots, and :475/494 POST /v2/orders.\n- **Broker sync:** broker-sync.ts:104 sets DEFAULT_SYNC_INTERVAL_MS = 60_000. At :119 it calls clientFactory to build a snapshot.\n- **Month-return sync:** month-return-sync.ts:48 sets a 10-minute interval, and :56 calls getPortfolioHistory(\"1M\").\n- **Order placement:** trade-service.ts:117 calls client.placeOrder, and option-trade-service.ts:172 calls placeOptionOrder.",
    "evidence_against": "These are gaps only; nothing in the claim is contradicted.\n\n- The label leaves out order replace and cancel: PATCH and DELETE on /v2/orders/{id} at alpaca-trading-client.ts:201-221. It also leaves out reads of /v2/account/activities (alpaca-options-client.ts:465).\n- Option snapshots and chains come from the Alpaca data host (ALPACA_DATA_BASE_URL, /v1beta1), not the trading host. The market-data websocket also goes to the data host.\n- In offline mode, data-source.ts:200-230 swaps all of this for fixture transports and a ReplayEventStream, so there is no Alpaca connection at all.",
    "correction": "No change is required. For a more precise label, add \"replaces/cancels orders, reads account activities\" and note that the relationship exists in live mode only (offline mode replays fixtures). Optionally note that options snapshots and market data use the Alpaca data API host (/v1beta1) rather than the trading host."
  },
  {
    "label": "refute:runtime:16",
    "element": "api → anthropic: Companion tool rounds + streamed reply; feedback coach turns",
    "grounded": true,
    "evidence_for": "All cited files exist. src/companion/companion-tool-rounds.ts:22 has `export const ANTHROPIC_URL = \"https://api.anthropic.com/v1/messages\"`, and :141 POSTs to it for the non-streaming tool-detection rounds. src/companion/companion-chat.ts:17 and :124 pass ANTHROPIC_URL to the streaming call. The doc comment at :38 describes the design as tool rounds first, then a streamed reply. src/companion/companion-anthropic-stream.ts:2 and :45 read the Messages API with stream:true / text/event-stream. src/server/feedback-coach.ts:166 POSTs to https://api.anthropic.com/v1/messages with x-api-key and anthropic-version 2023-06-01, using MODEL claude-haiku-4-5 from feedback-coach-model.ts. src/http/anthropic-reply.ts is the shared anthropicApiError validator used by both callers (feedback-coach.ts:24 and :141). Boot is confirmed: src/scripts/dashboard-companion.ts:13 imports resolveCompanionChat, and src/scripts/dashboard-feedback.ts:11 imports resolveFeedbackCoach.",
    "evidence_against": "These are minor points and none would mislead a build session. feedback-coach.ts hardcodes the URL instead of reusing ANTHROPIC_URL. anthropic-reply.ts only validates errors and does not make the call itself. src/evals/companion/judge.ts:17 also calls api.anthropic.com, but that is an eval/offline caller, not the api container. The coach runs on claude-haiku-4-5 (metered via ANTHROPIC_API_KEY), which the label does not mention.",
    "correction": "Optional: label the relationship \"HTTPS/JSON + SSE (Messages API, x-api-key)\". Note that the coach uses claude-haiku-4-5 and hardcodes the URL at feedback-coach.ts:166 rather than using companion-tool-rounds.ts ANTHROPIC_URL. Keep evals/companion/judge.ts off the api container edge."
  },
  {
    "label": "refute:runtime:15",
    "element": "CLAIM #15 bots → alpaca (runtime relationship): market-data websocket for the 10-symbol universe, market clock, news poll every 60s, paper orders in live mode",
    "grounded": true,
    "evidence_for": "Every cited file exists. src/scripts/run-autonomous.ts:79 has UNIVERSE, which lists exactly 10 symbols (AAPL, MSFT, NVDA, GOOGL, AMZN, META, AVGO, TSLA, CRWV, MRVL). NEWS_POLL_MS = 60_000 is at :81, and setInterval(pollNews, NEWS_POLL_MS) is at :240. getNews(UNIVERSE) is called at :231. UNIVERSE is passed to startSharedDataConnections at :217-225. src/scripts/autonomous-data-connections.ts:38-60 builds three things: an AlpacaNewsClient against ALPACA_DATA_BASE_URL, startMarketClock, and an AlpacaMarketDataStream with symbols. src/alpaca/market-data-stream.ts:53 opens wss://stream.data.alpaca.markets/v2/${feed}. src/scripts/autonomous-market-clock.ts:41,49-59 polls GET /clock with ALPACA_PAPER_BASE_URL as the default. For orders, src/bots/swappable-bot-broker.ts:31 calls createBotBroker, and src/bots/bot-broker.ts:13-22 returns new AlpacaBrokerAdapter(new AlpacaTradingClient(transport)). src/adapters/alpaca-broker-adapter.ts:30 is documented as 'Adapts the Alpaca paper Trading API'. run-autonomous.ts:268 logs 'PLACING PAPER ORDERS' when mode is not observe.",
    "evidence_against": "Some links in the chain are not cited, but none of this makes the claim wrong. First, the SwappableBotBroker gets to AlpacaBrokerAdapter through src/bots/bot-broker.ts createBotBroker, which the claim does not mention. Second, each bot's broker is actually built in src/scripts/autonomous-live-wiring.ts:370, not in run-autonomous.ts. Third, 'live mode' is decided per bot: at autonomous-live-wiring.ts:351 a bot only trades when SKYNET_AUTONOMOUS_MODE=live and it passes the readiness gate. Otherwise it is pinned to observe and places no orders. Fourth, market data, clock and news all run on one shared set of data credentials (dataCreds), not on each bot's own credentials. Finally, SKYNET_DATA_SOURCE=offline (run-autonomous.ts:84) skips Alpaca entirely.",
    "correction": "Keep the edge. Refine the evidence and label so a build session isn't misled. Order path: SwappableBotBroker (src/bots/swappable-bot-broker.ts) → createBotBroker (src/bots/bot-broker.ts) → AlpacaBrokerAdapter/AlpacaTradingClient, wired per bot in src/scripts/autonomous-live-wiring.ts:370. Orders are only placed when SKYNET_AUTONOMOUS_MODE=live and the bot passes the readiness gate (live-wiring:351); otherwise it runs in observe mode. The data plane (the wss stream.data.alpaca.markets websocket, GET /clock, news on ALPACA_DATA_BASE_URL) uses one shared set of data credentials, is built in autonomous-data-connections.ts, and runs only when SKYNET_DATA_SOURCE=live."
  },
  {
    "label": "refute:runtime:17",
    "element": "CLAIM #17 api → github (feedback issues, feedback-assets screenshots, issue state and Actions runs, OAuth login)",
    "grounded": true,
    "evidence_for": "Every cited file exists and does what the claim says. src/server/feedback-service.ts:112 POSTs to /repos/{repo}/issues and :84 sets labels. src/server/feedback-images.ts:27 has ASSET_BRANCH=\"feedback-assets\", :91-107 creates the ref off main, and :123 PUTs to /contents/{path}. src/server/feedback-status.ts:76 reads /issues/{n}. src/server/feedback-followup.ts:57-81 changes labels and posts comments. src/server/ops-status-deploy-lag.ts:68,79,96 reads Actions pipeline.yml runs and jobs, :126 reads compare and :157 reads commits/main. src/server/github-api.ts only builds the shared headers and error messages. src/server/auth/providers.ts:64 githubProvider handles OAuth: github.com/login/oauth/authorize at :75, access_token at :78, and /user plus /user/emails at :89 and :93.",
    "evidence_against": "Small gaps. src/server/feedback-similar.ts also calls api.github.com (probably an issue search for duplicates) and the claim does not cite it. The label leaves out writes like label edits and follow-up comments, and reads of commits and compare. github-api.ts is a header and error helper, not a client.",
    "correction": "Suggested label: \"Files feedback issues, labels them and posts follow-up comments, commits screenshots to the feedback-assets branch, searches for similar issues, reads issue state and Actions runs, commits and compare for deploy lag; OAuth login\". Add src/server/feedback-similar.ts to the evidence. Describe github-api.ts as the shared headers and error helper."
  },
  {
    "label": "refute:runtime:18",
    "element": "CLAIM #18 api → google: OAuth login",
    "grounded": true,
    "evidence_for": "/home/user/skynet-capital/src/server/auth/providers.ts:31 defines googleProvider (id \"google\", line 33). It builds the authorize URL https://accounts.google.com/o/oauth2/v2/auth at line 45, POSTs to https://oauth2.googleapis.com/token at line 48, and GETs https://openidconnect.googleapis.com/v1/userinfo at line 56. The provider is wired into the server in /home/user/skynet-capital/src/server/auth/resolve-auth.ts:35-41, and the routes are handled by the server authenticator at /home/user/skynet-capital/src/server/auth/authenticator.ts:80 (/auth/google and /auth/google/callback).",
    "evidence_against": "Two details the edge label leaves out. First, the edge only exists when SKYNET_GOOGLE_CLIENT_ID and SKYNET_GOOGLE_CLIENT_SECRET are set, and only when SKYNET_SESSION_SECRET is set too (resolve-auth.ts:31-35). Otherwise auth is disabled. Second, the browser is redirected to accounts.google.com (user → google). The server itself only calls the token and userinfo endpoints (server → google). GitHub is a second OAuth provider next to Google, and the claim is otherwise accurate.",
    "correction": "Optional refinement: label the edge \"OAuth code exchange + userinfo (HTTPS). Optional, env-gated by SKYNET_GOOGLE_CLIENT_ID/SECRET.\" Add a separate user → google edge for the browser authorize redirect, and a parallel api → github OAuth edge."
  },
  {
    "label": "refute:runtime:19",
    "element": "api → edgar: Reads company_tickers.json and recent 8-K submissions for the guidance pulse",
    "grounded": true,
    "evidence_for": "/home/user/skynet-capital/src/adapters/edgar-filings.ts:14 defines TICKERS_URL = \"https://www.sec.gov/files/company_tickers.json\" and fetches it at line 50. Line 84 fetches `https://data.sec.gov/submissions/CIK${cik}.json`, and line 93 keeps only form === \"8-K\". The eightKs method (lines 69-98) returns {fetchedAt, filings}. /home/user/skynet-capital/src/server/guidance-route.ts:4 imports EdgarFilings. Line 66 declares the dep as `edgar: Pick<EdgarFilings,\"eightKs\">`, line 69 defines `const LIVE: GuidanceDeps = { edgar: new EdgarFilings(), ... }`, line 124 calls `deps.edgar.eightKs(symbol, { fresh: refresh })`, and line 237 makes LIVE the default for deps. The route is reached through serveGuidance in src/server/option-api-routes.ts:22. No other non-test file under src uses EdgarFilings, so the guidance route is the only consumer.",
    "evidence_against": "Only small details are missing. The two reads go to different hosts: the ticker map comes from www.sec.gov and the submissions from data.sec.gov. The adapter keeps an in-memory cache: the ticker map is refreshed after TICKER_TTL_MS and filings are cached for 5 minutes (FILINGS_TTL_MS), and a member refresh bypasses that cache. Requests are plain HTTPS GETs with a User-Agent header (line 28).",
    "correction": "Optional refinement for the label: \"HTTPS GET (User-Agent) to www.sec.gov company_tickers.json (ticker→CIK) and data.sec.gov/submissions/CIK*.json (8-K list), with in-memory caching (5 min for filings; refresh bypasses it); only the /guidance route uses it.\""
  },
  {
    "label": "refute:runtime:20",
    "element": "CLAIM #20 ci -> fly: builds one image, deploys skynet-capital on every push to main, deploys skynet-capital-bots only when bot-relevant, sets secrets, pulls logs",
    "grounded": true,
    "evidence_for": "All the cited files exist. In /home/user/skynet-capital/.github/workflows/pipeline.yml, the push trigger on main is at lines 42-43. The `deploy` job (line 378) is gated `if: push && refs/heads/main` (line 385). It runs `flyctl deploy --remote-only` (line 476), then a smoke test with rollback to the previous image (lines 494-498). It also captures the released image as a job output (lines 388-390 and 484-490) so it is built once per push. The `deploy-bots` job (line 503) `needs: deploy` (line 518) and runs the preflight `node scripts/bots-deploy-preflight.mjs` (line 553). That preflight diffs the machine's stamped GIT_SHA against HEAD using scripts/bot-relevant.mjs, which pipeline.yml lines 505-506 and bot-relevant.mjs lines 18-19 name as the single source of truth. deploy-bots reuses the dashboard image and refuses to rebuild (lines 560-573). Secrets: .github/workflows/autonomy-ops.yml runs `flyctl secrets set` at lines 163, 181, 198 and 227. Logs: fly-logs.yml line 63 and autonomy-ops.yml line 120 run `flyctl logs --no-tail`.",
    "evidence_against": "These are nuances only. None of them overturns the claim. (1) GitHub Actions does not build the image itself. `flyctl deploy --remote-only` (pipeline.yml:476) builds it on Fly's remote builder, and CI only triggers that build. (2) Secrets are not set by the push pipeline. `flyctl secrets set` happens only in the manually triggered autonomy-ops.yml (workflow_dispatch). fly-logs.yml is also manual. (3) The decision to deploy the bots app is more than \"bot-relevant\". The preflight also applies a cooldown window (#1181, pipeline.yml:543-544) and fails open to deploying. There is also a force_bots_deploy dispatch override (lines 50-54 and 522), which redeploys the current dashboard image without a new build. (4) The bots deploy uses a different token: FLY_API_TOKEN_BOTS || FLY_ORG_TOKEN (line 547).",
    "correction": "Relabel the edge as: \"pipeline.yml (push to main): flyctl deploy --remote-only builds on the Fly remote builder and deploys skynet-capital, with smoke test and rollback. deploy-bots reuses that image for skynet-capital-bots when the preflight (bot-relevant.mjs diff from the stamped GIT_SHA, plus cooldown, fail-open) says deploy, or when force_bots_deploy is dispatched. Manual workflow_dispatch: autonomy-ops.yml sets or unsets secrets and runs status/logs; fly-logs.yml pulls logs.\" Consider drawing two edges, an automatic push deploy and a manual ops edge, so a build session does not assume the push pipeline sets secrets."
  },
  {
    "label": "refute:runtime:21",
    "element": "CLAIM #21 ci -> github: triggered by PRs, pushes, issue/comment events and workflow_run; opens PRs, issues, receipts, comments, releases",
    "grounded": true,
    "evidence_for": "All the cited evidence exists and says what the claim says. .github/workflows/pipeline.yml:24-48: on pull_request (opened/synchronize/reopened/ready_for_review/edited) and push to main, plus workflow_dispatch; line 452 `npx semantic-release`. .releaserc.json lists @semantic-release/github, which creates GitHub releases. .github/workflows/moneypenny-events.yml:53-81: on issue_comment [created], push, issues [labeled]; line 106 `issues: write # open issues, comment receipts`; line 205 `run: node scripts/moneypenny/index.mjs`, plus --audit/--claim-feedback/--claim-plan/--release at lines 221/390/407/671/723. .github/workflows/moneypenny-repair.yml:59-66: workflow_run on completed runs of Pipeline, Moneypenny Events, Autonomy ops, Fly logs and Companion eval, plus workflow_dispatch pr_number. .github/workflows/claude.yml:85-91: on issues [opened, labeled], issue_comment [created, edited], pull_request_review_comment; permissions contents/issues/pull-requests: write (lines 93-97), so it can open PRs and post comments. scripts/moneypenny/*.mjs contains receipt logic (events.mjs, gh.mjs, audit.mjs, index.mjs).",
    "evidence_against": "Small gaps only. The trigger list leaves out workflow_dispatch (manual triggers). That is the only trigger for autonomy-ops.yml, fly-logs.yml and companion-eval.yml, and those three workflows are not mentioned at all. It also leaves out pull_request_review_comment, which triggers claude.yml. No schedule/cron trigger exists, which fits the repo's event-driven rule. None of this contradicts the claim.",
    "correction": "Optional additions to the edge label: add \"workflow_dispatch (manual)\" and \"PR review comments\" to the triggers. It could also note that autonomy-ops, fly-logs and companion-eval are manual-dispatch only, and that moneypenny-repair watches their runs through workflow_run."
  },
  {
    "label": "refute:runtime:22",
    "element": "ci → anthropic: Runs Claude Code sessions for the human-directed lane, Moneypenny builds and CI repair (via anthropics/claude-code-action@cfc3eb2…)",
    "grounded": true,
    "evidence_for": "All cited jobs exist and each uses the pinned action `anthropics/claude-code-action@cfc3eb22bfed5c26ef66e3223c982af27e4524de`. The lane is described as \"THE HUMAN-DIRECTED LANE\" in /home/user/skynet-capital/.github/workflows/claude.yml:3, and the `claude` job (line 146) calls the action at line 168. In /home/user/skynet-capital/.github/workflows/moneypenny-events.yml, dep-warden (job at line 131) calls the action at line 153, build-events (416) at 473, build-feedback (589) at 612 and build-plan (678) at 701. /home/user/skynet-capital/.github/workflows/moneypenny-repair.yml has a `repair` job at line 122 with 3 invocations at lines 157, 196 and 221. The `triage` job at line 98 has no action call. Every call authenticates with `claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}`, for example claude.yml:170, moneypenny-events.yml:158/475/624/703 and moneypenny-repair.yml:176/203/228. No other workflow (pipeline, companion-eval, autonomy-ops, fly-logs) calls claude-code-action.",
    "evidence_against": "These are minor points and do not mislead. dep-warden checks dependencies; it is not a \"build\", though the label's \"Moneypenny builds\" loosely covers it. The auth is a Claude Code OAuth token (a subscription token), not an ANTHROPIC_API_KEY. A build session wiring secrets should know this. The label does not claim otherwise.",
    "correction": "Optional refinement: label the edge \"Claude Code sessions via claude-code-action (CLAUDE_CODE_OAUTH_TOKEN): human-directed lane (claude.yml), Moneypenny build-events/build-feedback/build-plan + dep-warden, CI repair (moneypenny-repair.yml repair job, 3 invocations)\". This makes the auth mechanism explicit and shows that dep-warden is a dependency job, not a build."
  },
  {
    "label": "refute:runtime:23",
    "element": "CLAIM #23 ci → tests: \"Runs verify on every PR and e2e in the integration-tests job\"",
    "grounded": true,
    "evidence_for": "The cited jobs exist. In /home/user/skynet-capital/.github/workflows/pipeline.yml, the `verify` job (lines 68-70) runs when `github.event_name == 'pull_request' && draft == false`. Its PR triggers are opened, synchronize, reopened, ready_for_review and edited (line 41). At lines 181-209 it runs commitlint on the PR title, plus typecheck, lint and test for both root and app/. The job with id `e2e` and display name \"integration tests\" (lines 202-262) `needs: verify`. Its step \"Build app/ and run the e2e suite\" runs `npx playwright test` (lines 261-262). playwright.config.ts line 22 has webServer.command `npm run build --prefix app && npm run serve:dashboard:offline`, which confirms that the job builds app/ and runs it.",
    "evidence_against": "\"Every PR\" is too broad in three ways. (1) verify skips draft PRs (line 70). (2) On docs-only PRs (only *.md changed), verify reports green but skips typecheck, lint and test (lines 128-137, 182). (3) The integration-tests job runs only on non-draft PRs where needs.verify.outputs.code == 'true', and it also runs on push to main (lines 221-224, #3333). The claim leaves out the push-to-main run. The yml comment (lines 203-210) says Eric renamed the job away from \"e2e\" on 2026-09-19 because \"e2e\" suggested testing the live deployed system. The job actually runs Playwright against a throwaway localhost:8787 build with fixtures/offline data. It never tests the Fly deployment; only deploy's smoke.sh does that.",
    "correction": "Relabel the edge: \"verify (commitlint + typecheck/lint/unit tests; heavy steps skipped for docs-only) on every non-draft PR; `integration tests` job (id e2e: builds app/ and runs Playwright against a local offline-fixture server) on non-draft code PRs and on push to main\". Don't call it an e2e test of the deployed app."
  },
  {
    "label": "refute:runtime:24",
    "element": "CLAIM #24 tests → api (e2e boots offline dashboard; unit specs import server/observatory/autonomous/companion modules with fake transports)",
    "grounded": true,
    "evidence_for": "The e2e half checks out. /home/user/skynet-capital/playwright.config.ts:19-26 has a webServer that runs `npm run build --prefix app && npm run serve:dashboard:offline` on :8787. package.json:19 defines that script as `SKYNET_DATA_SOURCE=offline tsx src/scripts/serve-dashboard.ts`. src/runtime/data-source.ts:4-7, 40, 193-221 wires FixtureTradingTransport and ReplayEventStream into offline mode (\"same interfaces, no network\"). The unit specs run under rstest (package.json:12 `rstest run`). tests/ has one folder per src/ module (adapters, alerts, alpaca, autonomous, companion, observatory, server, etc.). The fixture transport is used directly by 8 specs in tests/server, including trade-service, account-service and participant-service. Scripted and in-memory fakes are used in tests/autonomous, tests/engine, tests/research and tests/runtime.",
    "evidence_against": "1) The folder mirror is not exact. src/ports has no tests/ports, and tests/ has three folders with no src match: arch (fitness gates), fixtures and support. 2) The Playwright e2e specs do not live under tests/**. They are in /home/user/skynet-capital/e2e/*.spec.ts, with component tests in e2e/ct, and playwright.config.ts ignores **/ct/**. A separate playwright.auth.config.ts covers login.spec.ts. 3) FixtureTradingTransport and ReplayEventStream are mainly production offline-mode adapters in src/adapters, wired in src/runtime/data-source.ts, observatory/history-boot.ts and scripts. They are not test-only fakes. Among unit specs outside tests/adapters, only tests/server imports them. Autonomous and engine specs use other fakes: in-memory-broker, scripted-market-data and scripted-atm-quotes. I found no evidence that companion or observatory specs use these transports.",
    "correction": "Split the element into two edges. (a) e2e/ (Playwright, playwright.config.ts webServer) → dashboard server started with `serve:dashboard:offline`, which sets SKYNET_DATA_SOURCE=offline. That makes src/runtime/data-source.ts use FixtureTradingTransport and ReplayEventStream. (b) tests/** (rstest unit specs, roughly one folder per src module, plus tests/arch fitness gates and tests/support and tests/fixtures helpers) → src modules, using adapter fakes. The fakes are FixtureTradingTransport for server services, and InMemoryBroker, ScriptedMarketData and ScriptedAtmQuotes for autonomous and engine. Label the fixture and replay adapters as offline-mode adapters that tests reuse, not as test-only fakes."
  },
  {
    "label": "refute:runtime:25",
    "element": "CLAIM #25 tests -> bots: \"Specs the cycle runner, trader, guards, safety, bridge clients and offline runner\"",
    "grounded": false,
    "evidence_for": "Most of the relationship holds. /home/user/skynet-capital/src/scripts/run-autonomous.ts:351 says the cycle is \"fully spec'd in tests/autonomous/live-cycle.spec.ts\", and that spec imports src/autonomous/live-cycle.js. tests/autonomous/ has 30 specs, one for each src/autonomous module, including autonomous-trader.spec.ts, safety.spec.ts, insight-bridge-client.spec.ts, decision-replication-client.spec.ts, bot-controls-client.spec.ts, bot-credentials-client.spec.ts and subscriptions-bridge.spec.ts, so \"bridge clients\" is fair. /home/user/skynet-capital/tests/engine/guards.spec.ts imports applyGuards from src/engine/guards.js, and tests/engine/trading-engine.spec.ts also exists. InMemoryBroker (src/adapters/in-memory-broker.ts) is imported as a test double by live-cycle.spec.ts:1, autonomous-trader.spec.ts:1, trading-engine.spec.ts:1 and tests/runtime/run-cycle.spec.ts:1.",
    "evidence_against": "No test covers \"offline runner\". A grep of tests/ for \"autonomous-offline-runner\" finds nothing. The only importer of src/scripts/autonomous-offline-runner.ts (runOffline) is src/scripts/run-autonomous.ts:72, so the claim cites it as spec'd when it has no spec at all. A build session would wrongly assume offline replay mode is covered. Two smaller problems: guards live in src/engine/guards.ts, the trading engine and not the bots container, and in-memory-broker.ts is a fake broker the tests use, not something the tests check. The evidence list also leaves out tests/runtime/run-cycle.spec.ts.",
    "correction": "Relabel as: tests -> bots: \"Specs the live cycle runner, AutonomousTrader, safety/equity-watch/breakers, and the bridge clients (insight, decision-replication, bot-controls, credentials, subscriptions); tests/engine specs guards and the trading engine. Uses InMemoryBroker as a test double.\" Evidence: tests/autonomous/*.spec.ts, tests/engine/guards.spec.ts, tests/engine/trading-engine.spec.ts, tests/runtime/run-cycle.spec.ts. Drop \"offline runner\": src/scripts/autonomous-offline-runner.ts has no spec and is only called from run-autonomous.ts:72. Mark it as a coverage gap, or draw guards as belonging to the engine and not bots."
  },
  {
    "label": "refute:ops:26",
    "element": "Claim #26 — Interactive session toolkit (ops/container): .claude/ settings, Orient output style, SessionStart hooks, 16 skills, 13 agents, grind.js/symbol-sweep.js workflows",
    "grounded": true,
    "evidence_for": "Every cited path exists. /home/user/skynet-capital/.claude/settings.json line 2 sets \"outputStyle\": \"Orient\". Its SessionStart hooks run `bash scripts/setup-commit-signing.sh` and `.claude/hooks/session-start.sh` (timeout 600). PreToolUse and PostToolUse (matcher Task|Agent) and UserPromptSubmit are wired to `node scripts/duel-log.mjs fanout|fanin|intent`. .claude/output-styles/orient.md has frontmatter `name: Orient`. .claude/hooks/session-start.sh provisions mise 2026.9.0 so Node 24 is installed from mise.toml. .claude/skills/ holds exactly 16 skills (backfill, bury, charter, decompose, dedupe, ears, governor, issue, journey, retro, secretary, ship, teardown, telestrator, vision, work-issues). .claude/agents/ holds exactly 13 agents. .claude/workflows/ contains grind.js and symbol-sweep.js. docs/COMPUTE.md lines 10-12 document the `model`/`effort` subagent frontmatter. docs/grind/README.md documents the step kinds (script and instructions, lines 4 and 32-34). scripts/worktree-setup.sh provisions node_modules in athlete worktrees; the governor, backfill and bury skills and six athlete agents cite it.",
    "evidence_against": "The agent summary is incomplete. Of the 13 agents, 7 are sonnet (decomposer, dep-warden, mortician, piece-wright, set-dresser, test-backfiller, ui-librarian) and 3 are fable at xhigh (red-team, render-alchemist, reviewer). The other 3 are opus at high (art-director, artifact-smith, linguist), and the label leaves them out. render-alchemist is a fable agent but works on 3D fidelity, so it is not strictly a \"reviewer\". session-start.sh exits at once unless CLAUDE_CODE_REMOTE=true, so the mise/Node 24 step only runs in cloud sessions, not \"local or cloud\". The commit-signing hook runs in both. There are 2 output styles (focus.md and orient.md); Orient is only the default. duel-log.mjs is a telemetry hook, not something the session \"loads\".",
    "correction": "Relabel the agents as \"13 agents (7 sonnet athletes, 3 opus art/artifact/linguist, 3 fable xhigh red-team/reviewer/render-alchemist)\". Mark the mise Node 24 SessionStart step as cloud-only: it is gated on CLAUDE_CODE_REMOTE=true. Add scripts/duel-log.mjs as the hook target for fan-out, fan-in and intent telemetry. Optionally note that there are 2 output styles (Orient is the default, Focus is the other)."
  },
  {
    "label": "refute:ops:27",
    "element": "#27 Ship loop (ops / container): scripts/ship.sh + .claude/skills/ship/SKILL.md",
    "grounded": true,
    "evidence_for": "All six cited code_roots and tests/arch/ship.spec.ts exist. The scripts/ship.sh header (lines 4-33) states the design: REST core bucket, no polling, the merge webhook as the completion signal, and one enable_pr_auto_merge call followed by STOP. api() at lines 54-60 is curl against api.github.com. The script uses python3 for JSON (json_field at line 64, and payload building at lines 184 and 313). cmd_open at line 151 runs `npm run verify` at line 248, then incident-scan.mjs (line 277), plan-closure-scan.mjs (line 288) and test-quality-scan.mjs (line 294). cmd_checkbody at line 88 is a pure linter for the fridge rule ('## The picture' at line 97, plus SHA-pinned screenshot URLs); ship.spec.ts line 51 tests it. cmd_checkarm at line 397 refuses a diff that touches the irreversible class, using envelope-scan.mjs. cmd_platter at line 785 has open, board and ledger subcommands and batches items needing Eric's merge onto one held PR, one commit per item (header lines 19-21). .claude/settings.json line 72 allows \"Bash(scripts/ship.sh checkbody *)\". The dispatcher case statement is at line 795.",
    "evidence_against": "1) It is not purely REST. promote_ready (lines 71-80) makes a GraphQL markPullRequestReadyForReview call for held PRs, with a CCR-proxy REST route as fallback. The auto-merge arm is also GraphQL: an enable_pr_auto_merge MCP call, or cmd_automerge at line 411. 2) ship.sh never invokes scripts/deploy-lag.mjs. Only SKILL.md line 115 mentions it, as a separate diagnostic, so listing it as a code_root of the loop overstates it. 3) Other scripts the claim does not cite: envelope-scan.mjs (the actual source checkarm uses for the irreversible class) and commitlint (run at line 242). 4) The label \"without polling\" is right. \"One auto-merge arm\" is done by the agent through the MCP call after `ship open`, not by cmd_open itself. For held or irreversible diffs cmd_open labels the PR hold-merge and explicitly does not arm.",
    "correction": "Change the technology label to \"REST core bucket, plus a single GraphQL call each for auto-merge arming (MCP enable_pr_auto_merge or `ship automerge`) and for promoting a held draft to ready (promote_ready, with REST fallback)\". Remove scripts/deploy-lag.mjs from code_roots, or mark it as an adjacent diagnostic named in SKILL.md. Add scripts/envelope-scan.mjs + envelope.json as the dependency checkarm and cmd_open use to classify the irreversible class. Note that `ship open --hold` / the platter labels the PR hold-merge and does not arm it."
  },
  {
    "label": "refute:ops:28",
    "element": "#28 Fitness gates and coaches (ops / container)",
    "grounded": true,
    "evidence_for": "package.json:43 \"verify\": \"run-p typecheck lint test typecheck:app test:app\". Pinned versions match: @biomejs/biome 2.5.14 (97), @commitlint/cli 21.2.2 (98), @rstest/core 0.12.0 (102), dependency-cruiser 18.3.1 (108), husky 9.1.7 (111), jscpd 5.3.0 (112), knip 6.37.0 (114). Every cited path exists: biome.json, .husky/{pre-commit,pre-push,commit-msg}, commitlint.config.js, knip.json, .jscpd.json, .dependency-cruiser.cjs, arch-grandfather.json, many *-budget.json at the root, the tests/arch/*.spec.ts suite, and .claude/skills/governor/SKILL.md. docs/COACHES.md:212-232 has the \"Defensive roster\" table with the columns Coach | Eye (eval + budget) | Drill (skill) | Athlete (agent), and it lists scan script + budget JSON + tests/arch spec for each coach. tests/support/advisory-scan.ts:6-11 records the 2026-08-29 demotion to advisory of arch, dupe, dead, clone, dep-graph, spec-gap, doc-rot and open-incidents. Blocking gates stay: typecheck, lint, tests, envelope.spec.ts, and the forward-test-id --contract mode (COACHES.md:224). governor SKILL.md:4-39 dispatches athletes on a cheap model in isolated worktrees.",
    "evidence_against": "The element is partly overstated in four places:\n1. The code_roots glob scripts/*-scan.mjs is too broad. It also matches scans that are not fitness gates: digest-scan, event-scan, comms-scan, feedback-scan, journey-scan, latency-scan, plan-closure-scan and others.\n2. The workflow-structure gate (COACHES.md:227) uses scripts/workflow-lint.mjs, which is not a *-scan.mjs file.\n3. Not every eye has a ratchet budget. Size uses arch-grandfather.json, a flat exceptions list, not a numbered budget.\n4. Not every coach has an athlete dispatched by /governor. dep-graph, ci-install-duration, doc-rot, comment-bloat and forward-test-id have none. The incident athlete is Moneypenny repair, which runs from events and is not dispatched by governor (COACHES.md:222; SKILL.md:30).",
    "correction": "Narrow code_roots to the fitness scans: arch, dupe, clone, dead, dep-graph, spec-gap, incident, ci-install-duration, forward-test-id, doc-rot and comment-bloat, plus scripts/workflow-lint.mjs. Say that digest, event, comms, feedback and similar *-scan.mjs scripts belong to other containers. In the label, write that the eye is a budget JSON or arch-grandfather.json (for size). Say that /governor dispatches only the coaches that have an agent (decomposer, ui-librarian, mortician, test-backfiller) and that the incident coach's Moneypenny repair runs from events."
  },
  {
    "label": "refute:ops:29",
    "element": "Envelope gate (envelope.json + scripts/envelope-scan.mjs + tests/arch/envelope*.spec.ts), consulted by pipeline arm-auto-merge and ship.sh checkarm",
    "grounded": true,
    "evidence_for": "I checked every cited path and all of them exist. envelope.json:4 has \"lanes\": [\"feedback/\", \"research/\", \"design/\"], and envelope.json:6 starts the \"protected\" array. The $comment at envelope.json:2 calls the file the single source of truth, enforced by scripts/envelope-scan.mjs through tests/arch/envelope.spec.ts. scripts/envelope-scan.mjs:55-86 has globToRegExp and the rule matching. In lane mode it reads paths from `git diff --name-only` (comment at :100). tests/arch/envelope.spec.ts:90-133 tests the lane behavior: a non-lane branch is skipped, and a feedback/1 branch that touches a protected path fails. .github/workflows/pipeline.yml:272 defines the arm-auto-merge job. Its step \"Is the diff protected?\" (:352-359) runs envelope-scan --check, and the \"Arm auto-merge\" step (:361-362) is gated on blocking=='false'. scripts/ship.sh defines envelope_hits() at :386 and cmd_checkarm() at :397, and calls cmd_checkarm at :442 before arming auto-merge (see the comment at :463).",
    "evidence_against": "The label is mostly right, with a few gaps. (1) The scan does more than match globs: runtimeDepBreaches (envelope-scan.mjs:89-166, :213) also blocks any new runtime dependency in package.json, and envelope.json has an allowNewRuntimeDeps setting. (2) The pipeline and ship.sh filter on `.blocking`, not just on whether a path is protected. The diffAware exemption was removed on 2026-09-17 ($diffAwareComment), but the comments at pipeline.yml:345-348 and ship.sh:381 still describe it and are out of date. (3) The label says the spec is red on lane branches. It is only red when a lane branch touches a protected path or adds a new runtime dependency; non-lane branches (for example claude/*) are skipped. The arm-auto-merge --check path runs on every PR, not just lane branches. (4) A second check (steps.hold.outputs.held) also gates arming in the pipeline, so the envelope is not the only thing that decides whether auto-merge is armed.",
    "correction": "Change the label to: \"Envelope gate: path globs plus a new-runtime-dependency check. In --lane mode it runs on feedback/, research/ and design/ branches via tests/arch/envelope.spec.ts. In --check mode it runs on every PR through the pipeline arm-auto-merge 'Is the diff protected?' step (filtered on .blocking, alongside a separate hold check) and through ship.sh cmd_checkarm/envelope_hits.\" Leave diffAware off the diagram, because it was removed on 2026-09-17."
  },
  {
    "label": "refute:ops:30",
    "element": "#30 Pipeline (CI + CD) container",
    "grounded": true,
    "evidence_for": "Every part of the claim checks out against /home/user/skynet-capital/.github/workflows/pipeline.yml.\n- **verify (L68):** it uses actions/setup-node@v7 with node-version-file .nvmrc (L87-89), and .nvmrc contains 24. It uses actions/cache@v6 (L100, L119, L163). It commitlints the PR title (L154-157). A docs-only classifier skips the heavy steps (L128-136), and typecheck, lint and test run in parallel (L181-200).\n- **e2e job, display name 'integration tests' (L213):** it needs verify. It runs on a non-draft PR with code changes, or on a push to main (L220-224). It installs Playwright Chromium and runs `npx playwright test` (L257-262).\n- **arm-auto-merge (L272):** it gets an App token from ./.github/actions/app-token (L296). It re-reads the live `hold-merge` label (L306-314), checks the envelope with scripts/envelope-scan.mjs (L352-357), then arms auto-merge (L361).\n- **deploy, 'release · deploy' (L378):** it runs Semantic Release (L449) and superfly/flyctl-actions/setup-flyctl@master pinned to version 0.4.99 (L455-459). It captures the previous image with fly-image-ref.mjs (L461-469), runs `flyctl deploy --remote-only` (L476) and `scripts/smoke.sh` (L482), and rolls back on smoke failure (L494-499).\n- **deploy-bots, 'release · deploy bots' (L503):** it runs bots-deploy-preflight.mjs, whose comments say it defers to bot-relevant.mjs (L506, L537-553). It reuses the released image (L560-568), runs `smoke-bots.sh` (smoke step about L614-617) and rolls back on failure (about L621-626).\n- **.releaserc.json:** plugins are commit-analyzer, release-notes-generator, changelog, npm (npmPublish false) and github.\n- **package.json versions:** @playwright/test 1.62.1, semantic-release 25.0.9, @biomejs/biome 2.5.14, @rstest/core 0.12.0, @commitlint/cli 21.2.2.\n- **Other files:** all the cited scripts, playwright.config.ts and e2e/ exist. docs/DEPLOY.md:60 has '## Continuous deployment (merge → deploy)'.",
    "evidence_against": "Only small caveats; none makes the label misleading.\n- **Wording:** the file header (L4) says verify runs typecheck, lint and test \"on ONE runner\". They run as parallel processes on that one runner, not as parallel jobs, and the parallel step also covers the app/ typecheck and test.\n- **Version pin:** flyctl-actions is pinned `@master`, with only the flyctl binary pinned to 0.4.99.\n- **Step names:** the setup-node steps are named \"version from mise.toml\", but they actually read .nvmrc.\n- **Bot relevance:** the Resolve image step reuses skynet-capital's image, and the preflight step's comments say it defers to scripts/bots-deploy-preflight.mjs. The workflow never calls bot-relevant.mjs directly; the L506 comment names it as the single source of truth. That is consistent with the claim.",
    "correction": "Optional precision: \"verify runs typecheck, lint and test (root and app/) as parallel processes on one runner\". Also note the pin is superfly/flyctl-actions/setup-flyctl@master with flyctl version 0.4.99. Otherwise the element is accurate."
  },
  {
    "label": "refute:ops:31",
    "element": "CLAIM #31 Moneypenny event lane (ops / container)",
    "grounded": true,
    "evidence_for": "Every cited code root exists: .github/workflows/moneypenny-events.yml; scripts/moneypenny/ (index, events, audit, shipped, claim-lease, plan-claim, circuit-breaker, model-tier, etc.); the three event-*.mjs scripts; the feedback-build, plan-build and event-research prompts; .claude/agents/dep-warden.md; the app-token and oauth-token-gate actions; research-dispatch-budget.json, research-circuit-breaker.json and assessment-cadence.json; tests/scripts/moneypenny/. The workflow header at moneypenny-events.yml:3-6 says \"ONE ROUTER FOR EVERY ISSUE-DRIVEN AUTOMATION ... this file is a trigger shim and nothing else\", and it names scripts/moneypenny/index.mjs. Triggers are push, issues, issue_comment, pull_request and workflow_dispatch, with no cron (lines 53-91). The 2h lease is CLAIM_TTL_MS = 2h at claim-lease.mjs:31. The dependabot review job (yml:133-168) runs with --model claude-sonnet-5. The research matrix legs (yml:449-506) also run with --model claude-sonnet-5. The spend circuit breaker is at yml:230-255, which calls circuit-breaker.mjs. The quiet-pulse screen is step id \"screen\" plus event-material-scan (yml:174, 284). The never-assessed receipt sweep is at index.mjs:332-341. The shipped sweep is sweepShipped at index.mjs:318. The stall and conflict audit is audit.mjs. The docs/ROUTINES.md:41 Active row and the docs/MONEYPENNY.md:43 Authority section both exist. The CLI flags --dry-run, --event, --audit, --model-tier, --release, --check-claim and --guard-feedback-outcome are at index.mjs:797-881.",
    "evidence_against": "Two small inaccuracies. First, the tiering is three levels, not \"sonnet default, opus escalation\". model-tier.mjs has DEFAULT_MODEL claude-sonnet-5, STRONG_MODEL claude-opus-5 and LIGHT_MODEL claude-haiku-4-5, so single-criterion asks drop to Haiku. Plan issues always fall through to Opus. Second, a research-budget.json file sits next to research-dispatch-budget.json and is not listed. There are also three more prompts that are not listed: moneypenny-ci-repair.md, moneypenny-conflict-repair.md and moneypenny-event-stall-repair.md. The wording \"Audits ... conflicts on every push\" is broadly right: audit.mjs handles conflict re-dispatch.",
    "correction": "Change the model label to \"claude-code-action sessions (feedback/plan tier via model-tier.mjs: haiku light / sonnet default / opus escalation, plans on opus; sonnet pinned for research legs and dependabot review)\". Also consider adding the moneypenny-*-repair.md prompts to code_roots."
  },
  {
    "label": "refute:ops:32",
    "element": "CLAIM #32 Moneypenny repair lane (GitHub Actions workflow_run + workflow_dispatch, Node ESM router scripts/moneypenny/repair.mjs, claude-code-action on claude-opus-5)",
    "grounded": true,
    "evidence_for": "All 8 code_roots exist. .github/workflows/moneypenny-repair.yml:1 is named \"Moneypenny Repair (CI self-healing)\". The header at lines 2-4 says it \"watches the lanes\": a failed run on main files a capsule issue and dispatches a session. Lines 19-26 list the four loop guards: ignore its own failures, act only on default-branch runs, one open issue per signature, and needs-eric silences a signature. There are three entry points. The workflow_run list of watched workflows is at :59-66 (types completed). workflow_dispatch takes pr_number (:73, conflict repair, #909) and issue_number (:77, event-research stall). The triage job runs `node scripts/moneypenny/repair.mjs` (:120). The repair steps use anthropics/claude-code-action pinned to a SHA with `--model claude-opus-5` (:157/:180, :196/:206, :221/:235). In scripts/moneypenny/repair.mjs: REPAIR_WORKFLOW is at :36, signature(run, jobName) at :50, routeFailure at :93, and guard 1 at :96 (`if (run.name === REPAIR_WORKFLOW) return []`). scripts/moneypenny/repair-logs.mjs fetches the failed job logs and was split out of repair.mjs. docs/COACHES.md:222 has the 'Unlearned incidents' roster row, which names Moneypenny (repair) with the yml and repair.mjs. docs/COACHES.md:228 confirms the conflict-repair workflow_dispatch path. The three prompt files exist.",
    "evidence_against": "Two small points. First, scripts/repair-watchlist-scan.mjs does not run inside the lane. It is a separate drift gate (#933) that flags workflow files missing from the yml's hardcoded workflow_run.workflows list, and the workflow never invokes it. Second, 'watches the other lanes' is only true for the five workflows listed by name and by path (Moneypenny Events, Pipeline, Autonomy ops, Fly logs, Companion eval), not every workflow. Neither point makes the label misleading.",
    "correction": "Optional wording change: 'watches the five enumerated lanes (Moneypenny Events, Pipeline, Autonomy ops, Fly logs, Companion eval)'. Also mark scripts/repair-watchlist-scan.mjs as a companion drift gate for the watchlist, not a runtime part of the lane."
  },
  {
    "label": "refute:ops:33",
    "element": "CLAIM #33 Human-directed lane (.github/workflows/claude.yml + .github/prompts/interactive.md)",
    "grounded": true,
    "evidence_for": "/home/user/skynet-capital/.github/workflows/claude.yml:85-91 has triggers issues [opened, labeled], issue_comment [created, edited] and pull_request_review_comment [created, edited]. Lines 99-107 set concurrency group claude-<issue|pr number> with cancel-in-progress: true. Lines 122-130 are the gate `if:`: comment events pass without @claude, but the issues event requires contains(issue.body,'@claude'); it also checks sender.login != 'claude[bot]', excludes bodies containing '[Claude Code](https://claude', and requires author_association in OWNER/MEMBER/COLLABORATOR. Lines 162-172 mint the App token (./.github/actions/app-token) and pass it as github_token. Line 168 uses anthropics/claude-code-action (SHA-pinned). Line 182 passes --allowedTools \"Bash,Read,Write,Edit,Glob,Grep\" --model claude-opus-5 --max-turns 80. Lines 188-190 are the prompt shim telling the session to read .github/prompts/interactive.md. /home/user/skynet-capital/.github/prompts/interactive.md:1-30 backs the rest of the claim: verified-member trigger, a plain reply steers the work, restart semantics, and the thread is treated as untrusted data.",
    "evidence_against": "These are small gaps, not contradictions. (1) The claude job only runs when the gate step finds the CLAUDE_CODE_OAUTH_TOKEN secret (claude.yml:135-149, armed output); otherwise the lane does nothing. (2) The issues trigger also fires on `labeled`, so labelling an issue whose body contains @claude also starts a session, not only opening a new one. (3) The session is granted the tools Bash, Read, Write, Edit, Glob and Grep.",
    "correction": "Optional refinements: note the arming gate (the job runs only when the CLAUDE_CODE_OAUTH_TOKEN secret is set; claude.yml:135-149); note that the issues trigger covers opened+labeled and needs @claude in the issue body; and list the allowedTools (Bash, Read, Write, Edit, Glob, Grep). The label as written would not mislead a build session."
  },
  {
    "label": "refute:ops:34",
    "element": "CLAIM #34 Ops buttons (ops / container): autonomy-ops, fly-logs, companion-eval as phone-operable, reviewer-gated workflow_dispatch ops on flyctl 0.4.99",
    "grounded": true,
    "evidence_for": "All three cited files exist and all are workflow_dispatch-only. .github/workflows/autonomy-ops.yml: `workflow_dispatch` (line 18), and its action choices at lines 24-32 match the claim exactly: status, machine-status, logs, bootstrap-bots-app, flip-mode, set-playbooks, set-beta-forcing, set-hardcore. It has `environment: autonomy-ops` at line 88, with comments at lines 6 and 85-87 saying the job waits for required reviewers. flyctl is pinned `version: 0.4.99` at line 97. fly-logs.yml is a filtered log pull: a choice of app plus a required grep -E `filter` input (lines 16-36), then `flyctl logs --no-tail`, with flyctl 0.4.99 at line 51. companion-eval.yml checks out `inputs.ref` (lines 33, 53), then fetches ANTHROPIC_API_KEY live from the skynet-capital app with `flyctl ssh console -C \"printenv ANTHROPIC_API_KEY\"` (line 68) and masks it. It is also gated by `environment: autonomy-ops` (line 49). docs/AUTONOMY-DEPLOY.md lines 74-84 give the setup steps: the no-terminal path section, then creating the environment named exactly `autonomy-ops` and adding Required reviewers.",
    "evidence_against": "The label says the whole group is \"reviewer-gated\", but fly-logs.yml has no `environment:` key anywhere. The job at lines 41-44 is only name, runs-on and steps, so any user who can dispatch workflows can run it without approval. The file's header (lines 3-14) calls it a self-serve, read-only job that reuses the deploy tokens FLY_API_TOKEN / FLY_API_TOKEN_BOTS / FLY_ORG_TOKEN. The \"flyctl 0.4.99\" label is also not true everywhere: companion-eval.yml's `Set up flyctl` step (lines 60-61) has no `version:` pin, so it installs latest. That is the unpinned form docs/LESSONS.md warns 403s under runner load. Smaller point: companion-eval's key fetch uses secrets.FLY_API_TOKEN (line 66), the skynet-capital deploy token.",
    "correction": "Relabel the container as \"Phone-operable ops buttons (workflow_dispatch). autonomy-ops.yml and companion-eval.yml are gated by the autonomy-ops Environment (required reviewers). fly-logs.yml is NOT gated: it is self-serve and read-only, using the deploy-scoped Fly tokens.\" For technology, say flyctl pinned to 0.4.99 in autonomy-ops.yml and fly-logs.yml; companion-eval.yml is unpinned (latest). Otherwise the responsibilities, the list of action choices and the doc reference are correct."
  },
  {
    "label": "refute:ops:35",
    "element": "Claim #35 Secretary digest loop (ops/container)",
    "grounded": true,
    "evidence_for": "docs/ROUTINES.md:42 is the 'Secretary digest' row. It names a claude.ai Routine that runs daily at 12:00 with trigger trig_01KaMC2uR3cFW5XTUL6rzPuS. The row runs `node scripts/digest-scan.mjs --due`, where `due:false` means the loop stops, and otherwise follows .claude/skills/secretary. It also runs four ride-along scans: config-audit.mjs, comment-bloat-scan.mjs, incident-scan.mjs and doctrine-scan.mjs --due. Their findings go into the Needs-you tier. The row then writes docs/digests/<date>.md from TEMPLATE, ships it via /ship, and push-notifies the Needs-you count and the top headline. scripts/digest-scan.mjs:24-27 sets COMMIT_THRESHOLD=5 and HEARTBEAT_DAYS=7, and REQUIRED_SECTIONS lists Needs you, Headlines and Noise absorbed. docs/digests/2026-09-24.md is the newest dated digest. Every cited code root exists, including docs/plans/secretary.md and SKILL.md.",
    "evidence_against": "This is a minor gap only. comms-scan.mjs is not a ride-along in the Routine row. It is the landing meter (#456), which .claude/skills/secretary/SKILL.md:44 pastes into the digest as a table (`--table`). It is not an escalating audit. The push notification also carries the top headline, not only the Needs-you count.",
    "correction": "Label comms-scan.mjs as the digest's landing-meter table (SKILL.md:44), not as a Needs-you ride-along. The Routine row names only four ride-alongs: config-audit, comment-bloat, incident and doctrine. The push is the Needs-you count plus the top headline."
  },
  {
    "label": "refute:ops:36",
    "element": "Claim #36 (ops / container): Graphify structural map, a bash wrapper around graphifyy that regenerates docs/STRUCTURE-graph.md from graphify-out/ when run by hand",
    "grounded": true,
    "evidence_for": "All four cited paths exist: scripts/refresh-graph.sh, docs/STRUCTURE-graph.md, docs/GRAPHIFY.md and docs/BCP-GRAPHIFY.md.\n\nscripts/refresh-graph.sh:\n- Lines 2-9 say it is a manual refresh with no LLM, driven by `npm run graph:refresh`, and that \"The doc-rot gate watches the snapshot's age\".\n- Line 14 tells you to install graphifyy from PyPI.\n- Lines 18-21 run `graphify extract . --code-only` and then `graphify cluster-only .`.\n- The rest of the script writes a header and then `cat graphify-out/GRAPH_REPORT.md` into docs/STRUCTURE-graph.md.\n\npackage.json:32 defines `graph:refresh` as `bash scripts/refresh-graph.sh`.\n\ndocs/GRAPHIFY.md:\n- Lines 4-6 say graphifyy comes from PyPI and uses local tree-sitter with no API key.\n- Line 8 contains the quote \"The committed snapshot is refreshed by hand\", word for word.\n- Lines 17-20 list the explain, affected, path and query commands.\n\nscripts/doc-rot-scan.mjs:\n- Line 9 and lines 133-206 flag STRUCTURE-graph.md when its built-from commit is more than 30 days behind HEAD.",
    "evidence_against": "Two small points of precision, neither of which would mislead a build session:\n1. The script re-runs extraction on the whole repo (extract plus cluster-only) and only then copies graphify-out/GRAPH_REPORT.md. It does not just re-render an existing graphify-out/ folder.\n2. The watcher is scripts/doc-rot-scan.mjs, which checks commit age against a 30-day limit, not wall-clock time.",
    "correction": "Optional wording tweaks:\n- Say \"re-extracts the repo into graphify-out/ (git-ignored), then copies GRAPH_REPORT.md into docs/STRUCTURE-graph.md\".\n- Name the entry point: `npm run graph:refresh`.\n- Name the watcher: scripts/doc-rot-scan.mjs, which flags the snapshot when its built-from commit is more than 30 days behind HEAD."
  },
  {
    "label": "refute:ops:37",
    "element": "Repo ledgers and budgets (git) — git-tracked Markdown and JSON durable operating state",
    "grounded": true,
    "evidence_for": "I found every cited path in the repo. docs/research/events/ has exactly 688 entries, and that count includes TEMPLATE.md. src/domain/market-events/ holds 677 files named <id>.json. docs/research/forward-tests/*.md exists, docs/digests/YYYY-MM-DD.md exists, and so do docs/LESSONS.md and docs/IDEAS.md. The repo root has 14 *-budget.json files (dead, dupe, spec-gap, doc-rot, research, clone and others) plus assessment-cadence.json. docs/DELEGATION.md:34 is rail 5, 'Durable state = no record SPOF. Everything is externalized: the backlog in IDEAS.md, branches + PRs...'. docs/process/EVENT-RESEARCH.md:24 reads 'One file per event, named by its id. The calendar is src/domain/market-events/<id>.json'. scripts/event-scan.mjs:8 calls the '**Last assessed:** YYYY-MM-DD' header line 'this scanner's machine contract'.",
    "evidence_against": "Rail 5 (DELEGATION.md:34-37) only names IDEAS.md, branches/PRs and CLAUDE.md as the externalized record. It never lists the research ledgers, the budgets or the calendar. Saying that 'every lane' reads and writes all of this state through PRs goes beyond what the cited text says. The 688 figure counts TEMPLATE.md, so there are 687 real ledgers. The *-budget.json and assessment-cadence.json files are at the repo root, but the label does not give their location.",
    "correction": "Change the code_roots entries to '/*-budget.json' and '/assessment-cadence.json' so they point at the repo root. Change 'every lane reads and writes through PRs' to 'externalized state that lanes change through PRs'. Drop the exact file count or say '~687 ledgers + TEMPLATE.md'. Also consider adding CLAUDE.md, because rail 5 names it as part of the durable record."
  },
  {
    "label": "refute:ops:39",
    "element": "Bot runner (skynet-capital-bots) container",
    "grounded": true,
    "evidence_for": "fly.bots.toml:43 sets app=\"skynet-capital-bots\"; the header comment at lines 1-9 calls it the stateful half of the deploy split, deployed only when scripts/bot-relevant.mjs says so and reusing the dashboard's image. The [env] block sets SKYNET_AUTONOMOUS_MODE=\"observe\" (default until the autonomy-ops flip-mode action) and SKYNET_AUTONOMOUS_BOTS=\"sauron\". [processes] bots = \"npm run run:autonomous\". package.json:23 has run:autonomous = \"tsx src/scripts/run-autonomous.ts\". The Dockerfile starts FROM node:24-slim, keeps tsx as a runtime dependency, and .nvmrc=24. [mounts] source=\"skynet_bots_data\" at /data. pipeline.yml:503 has the deploy-bots job, and lines 384-390 show it reusing the image the dashboard deploy built. docs/AUTONOMY-DEPLOY.md:6-20, under 'The deploy split — one image, two apps', matches the claim. All code_roots exist: src/scripts/run-autonomous.ts, src/autonomous/, scripts/bot-relevant.mjs, scripts/smoke-bots.sh.",
    "evidence_against": "Only small gaps. The claim leaves out the private-network (6PN) link to the dashboard's insights/controls bridge (SKYNET_INSIGHTS_BRIDGE_URL -> app.process.skynet-capital.internal:8788). It also leaves out that the volume holds bots-state.db, the audit log and health.json. Neither makes the element wrong. pipeline.yml also has a force_bots_deploy workflow_dispatch input as a recovery lever that bypasses the classifier, so \"only when bot-relevant.mjs classifies\" is not strictly exhaustive.",
    "correction": "Optional additions, none required for accuracy: (1) Add a relationship edge from the bot runner to the dashboard's bridge listener on the private network (6PN, http://app.process.skynet-capital.internal:8788, set in SKYNET_INSIGHTS_BRIDGE_URL). If that bridge goes dark, the runner silently falls back to env-only controls; scripts/smoke-bots.sh checks for this. (2) Note that the volume holds /data/bots-state.db, /data/audit and /data/health.json. (3) The workflow_dispatch input force_bots_deploy can force a redeploy without the classifier. (4) The dashboard deploy job also lists src/server/insights-listener.ts as part of the bridge contract."
  },
  {
    "label": "refute:ops:38",
    "element": "Observatory server (skynet-capital) container, claim #38",
    "grounded": true,
    "evidence_for": "Every piece of this checks out. Dockerfile:2 is `FROM node:24-slim`. Dockerfile runs `npm run build:scene`, then builds the React shell with `cd app && npm ci && npm run build`, which produces app/dist. That shell is served at /app by src/server/app-shell-routes.ts. The Dockerfile's last instruction is `CMD [\"npm\",\"run\",\"serve:dashboard\"]`, and package.json:18 maps serve:dashboard to `tsx src/scripts/serve-dashboard.ts`. app/package.json pins react ^19.0.0 and uses @rsbuild/plugin-react, and app/rsbuild.config.ts exists. The server itself uses node:http: dashboard-server.ts:1 has `import { createServer } from \"node:http\"`. SSE comes from src/server/sse.ts:16, which sets the content-type to `text/event-stream`. fly.toml sets app = \"skynet-capital\", `[http_service] internal_port = 8787`, and a `[mounts]` entry with source skynet_data mounted at /data. Port 8788 is left out of http_service on purpose, so it is reachable only over the private network. insights-listener.ts:97-102 defaults to port 8788 and also serves `GET /controls` for the bots poll (lines 221-258). Filing feedback: feedback-issue.ts:98-100 gives every kind (bug, feature, idea) the \"feedback\" label. The deploy-lag signal lives in ops-status-deploy-lag.ts and uses the verdict logic in ops-status-deploy-verdict.ts. It reaches the ops-status page through ops-status-routes.ts and ops-status-service.ts. On deploys: .github/workflows/pipeline.yml triggers on push to main (line 26). Its `deploy` job (line 378) runs `flyctl deploy --remote-only` for the dashboard app on EVERY push to main (comment at line 381). The bots app deploys selectively, in a separate job.",
    "evidence_against": "Nothing contradicts the claim. Two small points are left out. The Dockerfile also builds a Babylon scene bundle (`npm run build:scene`). Separately, the deploy workflow itself is not listed as evidence: it is .github/workflows/pipeline.yml, `deploy` job. Also, feedback labels may be added in a POST after the issue is created rather than in the create call (see the note at feedback-service.ts:56-63). Either way the issue ends up with the \"feedback\" label.",
    "correction": "Optional precision only. Add .github/workflows/pipeline.yml (`deploy` job, `flyctl deploy --remote-only`) as evidence for \"deploys on every push to main\", and src/server/sse.ts for SSE. The technology list could also mention the Babylon scene bundle built in the image (npm run build:scene). The /controls poll and the insight bridge are the same listener on 8788 (src/server/insights-listener.ts), wired in through src/scripts/dashboard-insights-bridge.ts."
  },
  {
    "label": "refute:ops:40",
    "element": "CLAIM #40 eric -> ops relationship: \"Directs by intent; labels feedback/plan issues; comments ready; merges held platters; approves ops runs\"",
    "grounded": true,
    "evidence_for": "Every part of the claim checks out against the cited files.\n- \"Directs by intent\": CLAUDE.md:7 (\"how we work together (commander's intent)\"), CLAUDE.md:45-49 and CLAUDE.md:401 (\"plain intent routes to machinery\").\n- \"Labels feedback issues\": .github/workflows/moneypenny-events.yml:81 has `issues: types: [labeled]`, and :387 builds a feedback issue only when `github.event.label.name == 'feedback'`.\n- \"Comments ready\": moneypenny-events.yml:62-63 triggers on `issue_comment: types: [created]`. At :400-407, the step \"Claim a plan issue, if the owner said ready\" requires the commenter's association to be OWNER/MEMBER/COLLABORATOR and then runs `--claim-plan` (scripts/moneypenny/plan-claim.mjs).\n- \"Merges held platters\": CLAUDE.md:466-481 says irreversible-class PRs never auto-merge but board a platter, a held PR that \"he still merges\". pipeline.yml:280-288 skips arming auto-merge when the `hold-merge` label is present, and :307-314 re-checks the label live.\n- \"Approves ops runs\": autonomy-ops.yml:84-88 sets `environment: autonomy-ops`. Required reviewers on that environment make a workflow_dispatch run wait until an allowlisted reviewer approves it.",
    "evidence_against": "Only small imprecisions:\n1. A `plan` issue is not started by its label. Only a `feedback` label starts a build when it lands (moneypenny-events.yml:55-56 and :387). A plan issue waits until its owner comments \"ready\" (:403). \"Labels feedback/plan issues\" is still true as a description of what Eric does, but only the label on feedback issues is a trigger.\n2. The \"ready\" gate accepts any OWNER, MEMBER or COLLABORATOR, not only Eric.\n3. `hold-merge` can be applied by a session as well as by Eric (pipeline.yml:280). What stays Eric's alone is merging the held platter PR (CLAUDE.md:481).\nNone of these would mislead a build session.",
    "correction": "Optional wording change: \"Directs by intent; labels `feedback` issues (the label starts a build) and comments `ready` on `plan` issues (the comment starts the claim); merges held platter PRs (`hold-merge`); approves autonomy-ops runs (required reviewers on the GitHub Environment).\""
  },
  {
    "label": "refute:ops:41",
    "element": "CLAIM #41 member → ops: Files feedback that becomes a feedback-labelled issue; answers needs-info",
    "grounded": true,
    "evidence_for": "All three cited files exist. src/server/feedback-api-routes.ts:237 handles POST /api/feedback (filing), :241 handles POST /api/feedback/followup (a comment on the member's own issue, per the doc comment at :35), and :245 handles /api/feedback/claim. src/server/feedback-issue.ts:63 labelsFor() adds 'feedback' through the LABELS map at :98-100 (bug/feature/idea each include 'feedback'). It also adds member-<id>, and 'curated' when there is a spec, plus needs-eric and needs-info (:72, when spec.readiness === 'partial'). The comment at :57 says \"needs-info waits on the MEMBER, never on Eric\". .github/prompts/feedback-build.md:89 says \"Needs the member: Comment ONE specific question; label needs-info; stop\". src/server/feedback-status.ts:46 shows needs-info back to the member as 'Needs your input'.",
    "evidence_against": "The claim leaves out the channel for \"answers needs-info\". The member answers through POST /api/feedback/followup (feedback-api-routes.ts:241), which posts a comment on the issue. needs-info also has two sources, not one: the intake coach sets it at filing time through labelsFor (readiness partial), and the build lane sets it later through feedback-build.md:89. The 'ops' target is loose, since the issue actually lands on GitHub and the Moneypenny build lane picks it up (.github/workflows/moneypenny-events.yml:647).",
    "correction": "Label the edge as: \"Files feedback (POST /api/feedback → GitHub issue labelled feedback + bug|enhancement|idea + member-<id>); answers needs-info via POST /api/feedback/followup (issue comment)\". Also cite feedback-status.ts:46 for how needs-info is shown to the member."
  },
  {
    "label": "refute:ops:42",
    "element": "claude → ops: Runs skills and Workflow scripts, ships PRs, runs governor and secretary",
    "grounded": true,
    "evidence_for": "I checked every piece of cited evidence and it all exists. /home/user/skynet-capital/.claude/skills/ has 17 skill directories, each with a SKILL.md. Among them are governor/SKILL.md, whose description at lines 3-8 reads \"Run one head-coach dispatch cycle … dispatch the athlete … open its PR with auto-merge\", and secretary/SKILL.md, whose description at line 3 covers digests and verification teams. So \"runs governor and secretary\" is correct, because both are Claude skills. /home/user/skynet-capital/.claude/workflows/grind.js is a Workflow tool script: its meta block names 'grind' at lines 9-11 and it runs skill, instruction and script steps per item. A sibling script, symbol-sweep.js, is in the same directory. /home/user/skynet-capital/scripts/ship.sh exists, and its header at lines 4-17 describes local verify, push, opening the PR over REST, then one auto-merge call. It is wrapped by .claude/skills/ship/SKILL.md, which confirms \"ships PRs\".",
    "evidence_against": "I found nothing that contradicts the claim. Two small gaps: the evidence list leaves out .claude/workflows/symbol-sweep.js and the ship skill (.claude/skills/ship/SKILL.md) that wraps ship.sh. A grep of .github/workflows for 'governor' or 'secretary' also returned nothing. That supports running them through Claude sessions, although the secretary skill says a digest Routine can trigger it.",
    "correction": "This is optional and does not need to block the claim. Name the paths exactly: .claude/workflows/{grind,symbol-sweep}.js, and scripts/ship.sh wrapped by .claude/skills/ship/SKILL.md. Governor and secretary are skills in .claude/skills/, and the secretary can also be started by a digest Routine."
  },
  {
    "label": "refute:ops:43",
    "element": "CLAIM #43 ops → github: Opens issues and PRs, claims leases, arms auto-merge, cuts releases",
    "grounded": true,
    "evidence_for": "All cited evidence exists and says what the claim says. scripts/ship.sh:54-61 api() is a curl REST wrapper against $API/repos/<repo> with a Bearer token, used by `ship open` to open PRs. .github/actions/app-token/action.yml mints a GitHub App installation token via actions/create-github-app-token@v3, which is the identity used for these writes. scripts/moneypenny/index.mjs:140 claimHandoff() creates a lease ref under refs/tags/claim/<slug> (line 180 `ref=refs/tags/${ref}`); releaseClaim deletes it at line 194 through `gh api -X DELETE .../git/refs/tags/claim/${slug}`. .github/workflows/pipeline.yml:367 runs `gh pr merge --auto --squash \"$PR\"`. Releases are cut by `npx semantic-release` at pipeline.yml:452, configured in .releaserc.json. Moneypenny also writes to issues and PRs with `gh issue edit`/`gh issue comment`/`gh pr edit` (scripts/moneypenny/labels.mjs:38-46, index.mjs:528,579).",
    "evidence_against": "The evidence list is incomplete but not wrong. Nothing cited backs \"cuts releases\"; the real source is semantic-release at pipeline.yml:452 plus .releaserc.json. I found no scripted issue creation (no `gh issue create` or POST /issues in scripts/ or the workflows). The ops code mostly comments on and labels existing issues, so any new issues are probably opened by agent sessions through MCP or ship, not by a script. ship.sh:16 also says that for sessions, auto-merge is armed with the enable_pr_auto_merge MCP call (GraphQL), not ship.sh REST. Only CI does it with `gh pr merge --auto`.",
    "correction": "Add pipeline.yml:452 `npx semantic-release` (+ .releaserc.json) as the evidence for \"cuts releases\". Consider changing \"Opens issues\" to \"comments on and labels issues/PRs\" (moneypenny `gh issue edit/comment`), because no script creates issues. Optionally mark the technology as \"REST (curl/gh api) + GitHub App token; auto-merge via gh CLI / GraphQL\"."
  },
  {
    "label": "refute:ops:44",
    "element": "CLAIM #44 (ops / relationship): github → ops: \"Triggers lanes on push, label, comment, PR and workflow_run events\"",
    "grounded": true,
    "evidence_for": "Every event type in the label shows up in the four cited `on:` blocks. PUSH: pipeline.yml:42-43 (push to main) and moneypenny-events.yml:78-79 (push to main; the file calls every merge to main the \"tick\", with no cron). LABEL: moneypenny-events.yml:80-81 (issues: labeled) and claude.yml:86-87 (issues: opened, labeled). COMMENT: moneypenny-events.yml:62-63 (issue_comment: created, used for the plan lane's ready-flip), plus claude.yml:88-91 (issue_comment and pull_request_review_comment: created, edited). PR: pipeline.yml:25-41 (pull_request on main: opened, synchronize, reopened, ready_for_review, edited) and moneypenny-events.yml:89-90 (pull_request: opened, the Dependabot/dep-warden trigger). WORKFLOW_RUN: moneypenny-repair.yml:59-66 (workflow_run completed for Pipeline, Moneypenny Events, Autonomy ops, Fly logs and Companion eval, under both their names and their paths).",
    "evidence_against": "Only small gaps; nothing is wrong. (1) The label leaves out workflow_dispatch. All four cited files also accept manual dispatch: pipeline.yml:48, moneypenny-events.yml:91, moneypenny-repair.yml:71. (2) Three more workflows exist that were not cited: autonomy-ops.yml:17, fly-logs.yml:16 and companion-eval.yml:30. These run only on workflow_dispatch, so a human or agent starts them by hand; GitHub events never start them. (3) claude.yml also reacts to edited comments and PR review comments, which is more than a plain \"comment\" suggests. No workflow in the repo has a schedule/cron trigger, so the claim does not leave out any hidden scheduled trigger.",
    "correction": "Optional refinement: add \"+ manual workflow_dispatch\" to the edge label. You could also draw a separate human/agent → ops dispatch edge for the three workflows that only run by hand (autonomy-ops, fly-logs, companion-eval), so a build session does not assume every ops lane starts on a GitHub event."
  },
  {
    "label": "refute:ops:45",
    "element": "Claim #45 (relationship ops → ccp): \"Starts build, research and repair sessions; the digest Routine fires daily\"",
    "grounded": true,
    "evidence_for": "The pinned action anthropics/claude-code-action@cfc3eb22bfed5c26ef66e3223c982af27e4524de is used in exactly three workflows. (1) /home/user/skynet-capital/.github/workflows/claude.yml:168 is the human-directed comment/@claude lane (ad hoc fixes and reviews). (2) /home/user/skynet-capital/.github/workflows/moneypenny-events.yml:153, :473 (build-events, \"research due events\", line 416) and the build-feedback/build-plan jobs (lines 589 and 678, action at about line 612). (3) /home/user/skynet-capital/.github/workflows/moneypenny-repair.yml:157, :196 and :221 (repair). So \"build, research and repair sessions\" started from GitHub Actions is correct. /home/user/skynet-capital/docs/ROUTINES.md:42 documents the \"Secretary digest\" as a claude.ai Routine that runs daily at 12:00 (trig_01KaMC2uR3cFW5XTUL6rzPuS).",
    "evidence_against": "The digest clause is on the wrong edge. docs/ROUTINES.md:42 says the digest runs on a claude.ai-side schedule. No GitHub Actions workflow starts it (docs/ROUTINES.md:63 also says the config audit \"rides the secretary digest Routine (still claude.ai-side)\"). Putting \"the digest Routine fires daily\" on the ops → ccp arrow suggests GHA/ops starts it, and it does not. docs/ROUTINES.md:3 also says \"Event-driven first — no crons\": the build and research sessions are triggered by pushes to main, labels, comments and dispatches, not by a clock. The digest is the one exception (ROUTINES.md:48). The claim's \"three workflows\" undercounts the call sites: moneypenny-repair.yml alone has three action uses and moneypenny-events.yml has several.",
    "correction": "Relabel the edge: \"Runs Claude Code sessions via claude-code-action@cfc3eb2 (claude.yml comment lane; moneypenny-events.yml build-events/feedback/plan; moneypenny-repair.yml repair), triggered by push, label, comment and dispatch events.\" Take the digest off this arrow. Model it as its own claude.ai Routine (daily 12:00, trig_01KaMC2uR3cFW5XTUL6rzPuS) that runs inside ccp and ships docs/digests/<date>.md through /ship. It should not show as something ops starts."
  },
  {
    "label": "refute:ops:46",
    "element": "ops → fly: Deploys the verified image, smokes, rolls back, flips bot modes (pipeline.yml deploy/deploy-bots; autonomy-ops.yml flip-mode)",
    "grounded": true,
    "evidence_for": "/home/user/skynet-capital/.github/workflows/pipeline.yml: the `deploy` job (line 378) runs only on a push to main (385). It runs `flyctl deploy --remote-only` (476), smoke-tests with `scripts/smoke.sh https://skynet-capital.fly.dev` (480-482), and if the smoke fails it rolls back to the image it captured before deploying (494-499). The `deploy-bots` job (503) has `needs: deploy` (518) and reuses the dashboard's image from `needs.deploy.outputs.image` (564). It refuses to build again (570-572), deploys with `flyctl deploy --config ... --image` (610), smoke-tests with smoke-bots.sh (616-617), and rolls back (624-627). /home/user/skynet-capital/.github/workflows/autonomy-ops.yml: the `flip-mode` action (29) runs `flyctl secrets set SKYNET_AUTONOMOUS_MODE=<mode> -a $TARGET_APP` (223-227). When the mode is live on skynet-capital-bots, it first checks that the old bots group is gone (211-219).",
    "evidence_against": "\"Deploys the verified image\" is slightly off for the dashboard. `deploy` does not ship a prebuilt image that was already verified. It builds on Fly with `flyctl deploy --remote-only` (pipeline.yml:476), and the suite does not run again on main (lines 5-7, 380-381). What was verified is the commit: branch protection required `verify` to pass before merge. Only `deploy-bots` ships an existing image, the one the dashboard just built and smoke-tested. The label is also incomplete: autonomy-ops.yml does more than flip-mode. It also sets and clears the SKYNET_PLAYBOOKS, SKYNET_BETA_FORCING and SKYNET_HARDCORE_BOTS secrets (160-198), creates the bots app (137-139), and reads status, machine lists and logs (101-120).",
    "correction": "Suggested label: \"Builds the verified commit on Fly (remote build) and deploys the dashboard. Reuses that same smoke-tested image for the bots app. Smoke-tests and rolls back each one. Sets bot mode and roster secrets through flyctl (flip-mode, set-playbooks, and others).\" Technology: GitHub Actions + flyctl. Cite pipeline.yml:378-500 (deploy), 503-627 (deploy-bots), and autonomy-ops.yml:137-227."
  },
  {
    "label": "refute:ops:47",
    "element": "CLAIM #47 (ops / relationship): dependabot → ops: \"Opens bump PRs that the dep-warden lane reviews\"",
    "grounded": true,
    "evidence_for": "/home/user/skynet-capital/.github/dependabot.yml:23-42 sets up weekly npm updates (per-package PRs, open-pull-requests-limit 20, with Playwright kept as its own group) and weekly github-actions updates. The header comment at lines 1-3 says \"the dep-warden reviews them\". In /home/user/skynet-capital/.github/workflows/moneypenny-events.yml, lines 82-89 trigger on pull_request types [opened] and the comment names Dependabot's own PR as the trigger (#3315). The dep-warden job at lines 128-168 is gated on `github.event.pull_request.user.login == 'dependabot[bot]'` and runs anthropics/claude-code-action with allowed_bots \"dependabot\" and the GitHub App token. Its prompt tells it to follow /home/user/skynet-capital/.claude/agents/dep-warden.md for exactly one PR, and that file exists. The chain matches the claim: Dependabot opens a PR, the pull_request.opened event fires, and the dep-warden lane reviews it.",
    "evidence_against": "No evidence contradicts the claim. There are two minor points of detail. First, dep-warden does more than review: the dependabot.yml comment says it merges patch/minor bumps once the suite is green and escalates majors, and the job gets the App token specifically so it can merge or enable auto-merge. Second, the lane is only armed when CLAUDE_CODE_OAUTH_TOKEN is present (the oauth-token-gate step). Without that token, the PR stays open and nothing reviews it.",
    "correction": "Optional wording improvement: \"Opens weekly per-package bump PRs (npm + github-actions); pull_request.opened triggers the dep-warden job in moneypenny-events.yml, which reviews them, merges patch/minor bumps on green and escalates majors (the job only runs if CLAUDE_CODE_OAUTH_TOKEN is set).\""
  },
  {
    "label": "refute:ops:49",
    "element": "eric → session: Directs in chat",
    "grounded": true,
    "evidence_for": "/home/user/skynet-capital/CLAUDE.md:217-229 (\"Idea routing — the adapter is Claude, not Eric. He dumps raw; Claude classifies. Route every injected thought...\" with the act now / park / fan out / profile note / question routes, the NOW:/PARK:/FAN:/ME:/Q: overrides, and \"routing a raw thought in a live session, where Eric is right there\"). /home/user/skynet-capital/.claude/output-styles/orient.md is the session's standing opening move. Its step 2, \"Interrogate\", covers Eric's \"suggestion or command\" and \"directive\" in a live session (Eric, 2026-09-04 quote) before the act/park/fan routing. Both cited files exist and describe Eric sending directives and raw thoughts into a live chat session.",
    "evidence_against": "The word \"Directs\" is a little too strong. CLAUDE.md says Eric sends raw, unshaped thoughts and the session does the classifying (\"the adapter is Claude, not Eric\"). orient.md also says the session should question compounding directives rather than just follow them (\"the mechanism is on trial\"). So the session is not a pure executor of Eric's instructions. This is a nuance only. It would not mislead a build session.",
    "correction": "Optionally relabel it \"Dumps raw ideas / directives in chat (session routes: act/park/fan/ME/Q)\" so it is clear the session classifies and questions input rather than just executing it."
  },
  {
    "label": "refute:ops:48",
    "element": "CLAIM #48 ops → anthropic: \"Coach and companion turns from the deployed app; companion-eval replays\"",
    "grounded": false,
    "evidence_for": "Both halves describe real Anthropic calls. src/server/feedback-coach.ts:164-173 POSTs to https://api.anthropic.com/v1/messages (the cited line 166 is that URL). .github/workflows/companion-eval.yml:63-83 pulls ANTHROPIC_API_KEY from the Fly app (flyctl ssh printenv, line 68) and runs `npm run eval:companion` (line 83). That eval calls Anthropic through src/evals/companion/judge.ts:17 (JUDGE_URL) and replays companion turns.",
    "evidence_against": "The edge merges two different callers under one source, \"ops\". The coach and companion turns come from the deployed dashboard app on Fly: fly.toml app \"skynet-capital\", src/server/feedback-coach.ts, and src/server/companion-routes.ts calling src/companion/companion-tool-rounds.ts:22 (ANTHROPIC_URL). They do not come from an ops/CI container. The companion-eval replays run in GitHub Actions (CI). The label itself says \"from the deployed app\", which contradicts drawing the edge from ops. The companion half cites no evidence: feedback-coach.ts is the feedback coach only, and the companion call site is companion-tool-rounds.ts. A build session reading this edge would place the runtime Anthropic client in ops/CI, which is wrong. No existing ops container is defined in docs (docs/PICTURES.md has no ops/anthropic element).",
    "correction": "Split into two relationships. (1) Dashboard/web app container (Fly skynet-capital, src/server) → anthropic: \"Feedback-coach turns (src/server/feedback-coach.ts:166) and companion chat turns (src/companion/companion-tool-rounds.ts:22, via src/server/companion-routes.ts)\", HTTPS /v1/messages. (2) ops/CI (GitHub Actions) → anthropic: \"companion-eval replays + LLM judge (.github/workflows/companion-eval.yml:63-83, src/evals/companion/judge.ts:17); key borrowed from the Fly app secret at run time, never stored as a GitHub secret\"."
  },
  {
    "label": "refute:ops:50",
    "element": "CLAIM #50: eric → ops_buttons \"Approves and runs workflow_dispatch buttons\"",
    "grounded": true,
    "evidence_for": "/home/user/skynet-capital/.github/workflows/autonomy-ops.yml:18 (workflow_dispatch) and :84-88 (environment: autonomy-ops, required reviewers, protected-branches-only, run \"sits in Waiting until an allowlisted reviewer approves it\"). /home/user/skynet-capital/.github/workflows/companion-eval.yml:31 (workflow_dispatch) and :44-49 (same autonomy-ops environment, same allowlist). Eric as the approver is confirmed by /home/user/skynet-capital/docs/LESSONS.md:130 (\"Eric, on his phone and traveling, tapped the autonomy-ops GitHub Environment approval five times... so a session could read skynet-capital-bots logs\"). Also docs/AUTONOMY-DEPLOY.md:74-84 (the no-terminal path; the required-reviewers setup).",
    "evidence_against": "\"Runs\" overstates it. The dispatch is often made by a Claude session (LESSONS.md:130: taps were made \"so a session could read ... logs\"), and Eric's part is the approval step on the Environment's required-reviewer gate. Only these two workflows use `environment:`. Other workflow_dispatch workflows, such as moneypenny-events.yml, which is dispatched via `gh workflow run`, are not gated this way and should not be grouped under ops_buttons.",
    "correction": "Relabel it as: eric → ops_buttons \"Approves (required reviewer on the autonomy-ops Environment) and can dispatch autonomy-ops.yml / companion-eval.yml\". Sessions may trigger the dispatch, but a run waits until Eric approves it. Keep ops_buttons limited to these two workflows."
  },
  {
    "label": "refute:ops:51",
    "element": "CLAIM #51 member → dashboard: Uses the observatory; files feedback",
    "grounded": true,
    "evidence_for": "Both cited files exist. src/server/dashboard-auth-gate.ts:13-56: gateRequest guards every dashboard request with per-user OAuth (/login, /logout, provider callbacks, session check) or the legacy shared password, and hands back the member's Session. src/server/feedback-api-routes.ts:25-41 documents GET /api/feedback (the member's own filing history), POST /api/feedback (submitFeedback with the shared per-member throttle) and POST /api/feedback/followup (comments on the member's own filings), with identity resolved server-side through opaqueMemberId(session.email). The \"observatory\" wording matches the real module src/observatory/ (dashboard-shell.ts, standings-view.ts and others), which the dashboard serves.",
    "evidence_against": "Minor points only. The auth gate is the entry check, not the \"uses the observatory\" behaviour itself; better evidence for that would be src/observatory/dashboard-shell.ts or the dashboard server's route table. The legacy form route (feedback-routes.ts, /feedback plus /feedback/coach and /feedback/preview) is a second way to file feedback, and the claim leaves it out.",
    "correction": "Optional: label the edge \"Uses the observatory (HTTPS, OAuth session or shared password); files feedback via /feedback and /api/feedback\". Cite src/observatory/dashboard-shell.ts and src/server/feedback-routes.ts alongside the current files."
  },
  {
    "label": "refute:ops:53",
    "element": "session → ship: Lands branches (ops relationship)",
    "grounded": true,
    "evidence_for": "/home/user/skynet-capital/.claude/skills/ship/SKILL.md exists. Its description (lines 3-8) says \"Land a verified branch as a PR the resource-cheap way: local verify → push → open the PR over REST → one auto-merge call → STOP\", and it is meant to be used \"whenever you're opening a PR or merging a green branch\". So a session invokes /ship to land branches. The mechanics are in /home/user/skynet-capital/scripts/ship.sh, which exists. The flow section says `ship.sh open` runs `npm run verify`, pushes, and POSTs the PR over REST. The session then makes exactly one mcp__github__enable_pr_auto_merge (SQUASH) call.",
    "evidence_against": "ship does not land a branch by merging it directly. It opens a PR and arms auto-merge, and GitHub does the merge server-side once checks are green (the merge webhook signals completion). Direct REST merge into main is refused. For carve-outs (.github/workflows/**, and the envelope.json credentials/spend/outward-facing list) ship only opens the PR and Eric merges it. That part is detail, not a contradiction.",
    "correction": "Optional: a more precise label would be \"Lands branches (verify → push → REST PR → arm auto-merge; no polling)\", with ship noted as a skill that wraps scripts/ship.sh. Carve-outs get the PR opened but are merged by Eric."
  },
  {
    "label": "refute:ops:52",
    "element": "CLAIM #52 dashboard → github: Files feedback-labelled issues",
    "grounded": true,
    "evidence_for": "All three cited files exist. src/server/feedback-service.ts:101 createFeedbackIssue POSTs to https://api.github.com/repos/${repo}/issues (lines 112-113) using githubHeaders(config.token) from src/server/github-api.ts:8. It then attaches labels in a separate call, attachLabels (lines 79-96, POST .../issues/{n}/labels, called at line 122). The labels come from labelsFor in src/server/feedback-issue.ts:63, and every kind includes \"feedback\" (LABELS at lines 97-101: bug/enhancement/idea + feedback). src/scripts/dashboard-feedback.ts:14,29 wires resolveFeedback(env) into dashboard boot, and it is gated on SKYNET_FEEDBACK_GITHUB_TOKEN (line 32).",
    "evidence_against": "This is a small omission, not an error. The label transport lives in feedback-issue.ts (labelsFor/LABELS), which is not cited. Labels go on in a second REST call after the create call, never in the create call itself: moneypenny-events.yml triggers on issues.labeled. The relationship is optional because it is off when there is no token. The dashboard also calls GitHub for other feedback work (feedback-images upload, feedback-status, feedback-followup, feedback-similar), and this edge label doesn't cover those.",
    "correction": "Optionally relabel as \"Files feedback-labelled issues (REST: POST issue, then POST labels; token-gated SKYNET_FEEDBACK_GITHUB_TOKEN)\" and add src/server/feedback-issue.ts labelsFor() as evidence."
  },
  {
    "label": "refute:ops:55",
    "element": "CLAIM #55 (ops / relationship): session → graphify: Refreshes the structural map — evidence: scripts/refresh-graph.sh; docs/GRAPHIFY.md",
    "grounded": true,
    "evidence_for": "/home/user/skynet-capital/scripts/refresh-graph.sh:1-9 has the header \"Refresh the committed Graphify structural map from the current code\", and says refresh is MANUAL via `npm run graph:refresh`. Lines 18-21 run `graphify extract . --code-only` and `graphify cluster-only .`, and lines 23-43 write docs/STRUCTURE-graph.md from graphify-out/GRAPH_REPORT.md. /home/user/skynet-capital/package.json:32 has \"graph:refresh\": \"bash scripts/refresh-graph.sh\". /home/user/skynet-capital/docs/GRAPHIFY.md:8-10 says \"The committed snapshot is refreshed by hand — `npm run graph:refresh` (wraps scripts/refresh-graph.sh...)... There is no auto-refresh workflow today\". GRAPHIFY.md:33 also tells the session to run `graphify update .` after a change. So a session (a person or agent) is what starts the refresh, not CI.",
    "evidence_against": "Nothing contradicts the claim. The label does leave some things unstated. The refresh is manual only: no hook, workflow or CI job runs it, and CI cannot push to protected main (GH006). It uses the graphify CLI on the code's AST only, with no LLM or API key. It writes the committed snapshot docs/STRUCTURE-graph.md, while graphify-out/ is git-ignored. The doc-rot gate watches how old the snapshot is.",
    "correction": "Suggest the label \"Refreshes structural map manually (npm run graph:refresh → graphify extract/cluster-only, AST-only, writes docs/STRUCTURE-graph.md)\". Do not draw an automated or CI edge. No auto-refresh workflow exists, and the doc-rot gate only flags a stale snapshot."
  },
  {
    "label": "refute:ops:54",
    "element": "CLAIM #54 session → gates: Runs scans and governor cycles",
    "grounded": true,
    "evidence_for": ".claude/skills/governor/SKILL.md:23-28 lists each athlete's gate script (scripts/arch-scan.mjs, dupe-scan.mjs, dead-scan.mjs, spec-gap-scan.mjs), and :35 has the session run `node <its eye> --candidate` to pick targets (\"The gate picks; never hand-pick\"). .claude/settings.json:76-85 pre-approves the session to run the gate scanners (issue-lint, envelope-scan, arch-scan, dupe-scan, clone-scan, dead-scan, spec-gap-scan, doc-rot-scan, incident-scan, digest-scan), and :58-71 pre-approves npm run verify/lint/typecheck/test. So the session does invoke the gates directly.",
    "evidence_against": "The edge label is looser than the evidence. (1) A governor cycle is not something the session runs against the gates. The session runs the governor skill, which queries the gates for targets and then dispatches athlete subagents (SKILL.md:39-41). The gates are the thing being queried, not the thing that runs the cycle. (2) The settings.json allow list uses exact commands such as `Bash(node scripts/arch-scan.mjs)` with no wildcard. The governor's `--candidate` form is therefore not covered and would trigger a permission prompt. The allow list supports \"runs scans\" but only partly supports the governor path. (3) The gates also run in CI and pre-merge (SKILL.md:31, :102), so the session is not their only caller.",
    "correction": "Relabel the edge to \"Runs gate scans (node scripts/*-scan.mjs [--candidate]) for local verification and governor target selection\". Optionally add a separate edge, session → athletes, labelled \"governor cycle dispatch\". Note that settings.json only pre-approves the bare scan commands (lines 78-84), not the `--candidate` variants."
  },
  {
    "label": "refute:ops:56",
    "element": "CLAIM #56 ship → envelope: checkarm refuses to arm a protected diff",
    "grounded": true,
    "evidence_for": "/home/user/skynet-capital/scripts/ship.sh:386-395 envelope_hits pipes paths through `node scripts/envelope-scan.mjs --check` and exits 1 on any row with blocking=true. ship.sh:397-409 cmd_checkarm calls envelope_hits and, if anything hits, prints \"REFUSED — this diff touches the irreversible class, which never auto-merges\" and exits 5. cmd_automerge (ship.sh:442) calls `cmd_checkarm \"${paths[@]}\" --base \"origin/$base_ref\"` before arming. CLI dispatch is at ship.sh:800. The protected list is envelope.json, read through envelope-scan.mjs.",
    "evidence_against": "Small nuance only. The filter is on `blocking`, not `protected` (ship.sh:381-385). A diffAware rule whose real diff is a provably safe pure insertion comes back protected=true, blocking=false, and is allowed to arm when --base is passed. Also, the relationship is really ship → envelope-scan.mjs, which reads envelope.json; ship.sh does not read envelope.json directly.",
    "correction": "Optionally relabel the edge \"checkarm refuses to arm a blocking envelope hit (envelope-scan.mjs --check over envelope.json; exit 5)\". Note that a diffAware pure insertion under --base is protected but not blocking. automerge calls checkarm before arming (ship.sh:442)."
  },
  {
    "label": "refute:ops:57",
    "element": "ship → github: Opens the PR, arms auto-merge, boards platters (scripts/ship.sh cmd_open, cmd_automerge, cmd_platter_board)",
    "grounded": true,
    "evidence_for": "All three cited functions exist in /home/user/skynet-capital/scripts/ship.sh and each talks to GitHub. cmd_open starts at line 151 and opens the PR over REST with `api POST \"/pulls\"` at line 316, adding a hold-merge label at line 329. The `api` helper uses curl at line 55. cmd_automerge (line 411) GETs the pull and its files over REST, then sends the GraphQL `enablePullRequestAutoMerge` mutation with SQUASH at lines 444-446 and reads `auto_merge` back at line 492. cmd_platter_board (line 764) runs `git push -u origin` at line 781, then platter_sync_body, which PATCHes the PR body over REST at line 725. The dispatch table at lines 795-802 maps open, automerge and platter board to these functions.",
    "evidence_against": "These are small gaps, not wrong claims. (1) The edge hides that the transport is mixed. Opening and board-sync use REST at /pulls, platter board also does a git push, and only arming uses GraphQL. Lines 473-483 say arming often fails in this environment because the GraphQL proxy doesn't serve the mutation or the budget is exhausted. In that case the script tells the user to use the enable_pr_auto_merge MCP tool or run `ship.sh merge`. (2) The edge leaves out other GitHub interactions. cmd_merge (line 501) merges directly, cmd_open can call a ready_for_review endpoint (line 78), and cmd_platter_open (line 733) opens a held platter PR through cmd_open.",
    "correction": "Optional sharpening: label the edge \"REST /pulls (open, label, PATCH body) + git push; GraphQL enablePullRequestAutoMerge (arm; falls back to MCP enable_pr_auto_merge or `ship merge`)\". Also mention cmd_merge and platter open. As labelled now, it would not mislead a build session."
  },
  {
    "label": "refute:ops:58",
    "element": "CLAIM #58: github → pipeline relationship: pull_request and push main events",
    "grounded": true,
    "evidence_for": "/home/user/skynet-capital/.github/workflows/pipeline.yml:24-43. Line 25 has `pull_request:` with `branches: [ main ]` (line 26) and `types: [ opened, synchronize, reopened, ready_for_review, edited ]` (line 41). Line 42 has `push:` with `branches: [ main ]` (line 43). The event types and branch match the claim exactly.",
    "evidence_against": "The claim leaves out two things. First, a third trigger: `workflow_dispatch` (line 48), which takes a `force_bots_deploy` boolean input. It is a manual way to force a bots redeploy. Second, the pull_request trigger only fires for PRs whose base is main (line 26). The verify job also skips draft PRs (line 70). Neither gap makes the edge wrong.",
    "correction": "Label the edge \"pull_request→main (opened/synchronize/reopened/ready_for_review/edited), push main, workflow_dispatch (force_bots_deploy)\". It could also say that verify skips draft PRs."
  },
  {
    "label": "refute:ops:59",
    "element": "CLAIM #59: pipeline → gates, \"verify job runs the suite\" (evidence: the pipeline.yml step 'Typecheck + lint + test (parallel)')",
    "grounded": true,
    "evidence_for": "/home/user/skynet-capital/.github/workflows/pipeline.yml:68-69 defines the `verify` job. Lines 181-201 hold the step named 'Typecheck + lint + test (parallel)'. It starts five processes in parallel: `npm run typecheck`, `npm run lint`, `npm test`, `npm run typecheck --prefix app` and `npm test --prefix app`, then checks each exit code separately. In package.json, `test` is `rstest run` (line 12), `lint` is `biome check .` (line 14) and `typecheck` is `tsc` (line 11). /home/user/skynet-capital/rstest.config.* includes `tests/**/*.spec.ts`, which covers the architecture gate specs in tests/arch/ (god-file, dupe, dead, spec-gap, code-lines, grind-manifest). Those specs call scripts/arch-scan.mjs and the other gate scripts, for example tests/arch/god-file.spec.ts calls advisoryScan(\"scripts/arch-scan.mjs\"). So the verify job does run the suite, and the fitness gates run inside it.",
    "evidence_against": "Four limits apply. (1) The step is conditional (`if: steps.changes.outputs.code == 'true'`, line 182), so docs-only PRs skip typecheck, lint and tests but still report verify as passed (lines 11-12, 136). (2) verify runs only on pull_request. On a push to main it is skipped (lines 4-5, 16), and deploy relies on branch protection having already required verify to pass. (3) At least some tests/arch gates are advisory and never fail CI. The god-file gate has been advisory since 2026-08-29 (the comment in god-file.spec.ts says \"without blocking CI\"), so the job runs these gates but does not enforce them. (4) Integration tests (Playwright) are a separate `e2e` job (line 203), not part of this step.",
    "correction": "Label the edge \"verify (PR only, code changes only) runs typecheck + biome lint + rstest (root and app); the rstest suite includes the tests/arch gate specs, some of them advisory/non-blocking\". Do not draw a separate edge from verify to Playwright; that belongs to the e2e job, which needs verify."
  },
  {
    "label": "refute:ops:60",
    "element": "pipeline → envelope: Arms auto-merge only when the diff is unprotected (pipeline.yml arm-auto-merge 'Is the diff protected?' step)",
    "grounded": true,
    "evidence_for": ".github/workflows/pipeline.yml:272 defines the arm-auto-merge job. The step at :352-359 ('Is the diff protected?', id envelope) runs `git diff --name-only base head`, then passes that list to `node scripts/envelope-scan.mjs --check $FILES` and uses jq to set blocking=true if any entry is blocking. The arm step at :361-362 runs only when `steps.envelope.outputs.blocking == 'false'` (and held == 'false'). In scripts/envelope-scan.mjs:110-121, runCheck() calls breachOf(path) against the envelope.json rules and returns blocking:true for any protected path. So the pipeline really does call the envelope, and arming really does depend on its answer.",
    "evidence_against": "The envelope is not the only gate. Arming also needs verify to succeed, e2e to succeed or be skipped, the PR not to be a draft, and the event to be opened, reopened or ready_for_review (:283-288). It also needs no `hold-merge` label, checked twice: once in the event snapshot and again against the live labels (:306-317). The step also passes `--base <sha>`, but runCheck ignores it. It only drops the args that start with `--`, so the SHA is read as a path that never matches a rule. The comment at :345-348 says --check imports envelope-widening.mjs, but that file no longer exists. envelope-scan.mjs:13-19 says the diffAware mechanism was removed. That comment is stale. The claim itself is still accurate.",
    "correction": "Optional sharpening. Point the target at scripts/envelope-scan.mjs --check (the envelope.json rules), not a vague 'envelope'. Also note in the label that the live hold-merge label check and green verify/e2e gate arming too. Example: 'pipeline -> envelope-scan --check: arms auto-merge only if no changed path is protected (and not held)'."
  },
  {
    "label": "refute:ops:62",
    "element": "CLAIM #62 pipeline → dashboard: Deploys every push, smokes, rolls back (evidence: pipeline.yml deploy job)",
    "grounded": true,
    "evidence_for": "/home/user/skynet-capital/.github/workflows/pipeline.yml:378-499. Line 379 names the job `release · deploy`. The comment at line 381 says it \"deploys the DASHBOARD app on EVERY push\". Line 385 gates it with `if: github.event_name == 'push' && github.ref == 'refs/heads/main'`. Line 476 runs `flyctl deploy --remote-only`. Lines 481-482 run the smoke step, `bash scripts/smoke.sh https://skynet-capital.fly.dev`. Lines 495-499 roll back: `if: failure() && steps.smoke.conclusion == 'failure' && steps.prev.outputs.image != ''`, then `flyctl deploy -a skynet-capital --image <prev>` and a second smoke run. Lines 42-47 set the trigger as push to main, with the comment saying never to add paths filters, so every main push runs the job.",
    "evidence_against": "\"Every push\" is loose. The job runs only on pushes to main, not PR or branch pushes (line 385). Rollback is conditional. It runs only when the smoke step fails and a previous image exists (line 495). On a first deploy with no previous image, rollback is not available (line 471). The job also runs the release step (semantic-release) before deploying. The bots app is a separate, selective `deploy-bots` job (lines 503+, `needs: deploy`), so this edge covers the dashboard (skynet-capital) only, which the claim gets right.",
    "correction": "Relabel as: \"Releases and deploys on every push to main (flyctl), smoke-tests https://skynet-capital.fly.dev, and rolls back to the previous image if the smoke test fails.\" Keep this separate from the pipeline → bots edge, which is the selective deploy-bots job."
  },
  {
    "label": "refute:ops:61",
    "element": "CLAIM #61 pipeline → github: Arms auto-merge with the App token; semantic-release cuts tag and Release",
    "grounded": true,
    "evidence_for": "/home/user/skynet-capital/.github/workflows/pipeline.yml:272-376 is the `arm-auto-merge` job. It runs on pull_request opened/reopened/ready_for_review, after verify and e2e (e2e may be skipped), and not when the PR is a draft or has the `hold-merge` label. It gets a token from ./.github/actions/app-token (lines 295-298, vars.APP_CLIENT_ID plus secrets.APP_PRIVATE_KEY), sets GH_TOKEN to that token (lines 310 and 364) and runs `gh pr merge --auto --squash` (line 367). If that call fails it falls back to a direct `gh pr merge --squash` (line 371). The `deploy` job (line 378, push to main only) runs `npx semantic-release` at lines 449-452. /home/user/skynet-capital/.releaserc.json sets branches [\"main\"] and loads the plugins commit-analyzer, release-notes-generator, changelog, npm (npmPublish:false) and @semantic-release/github. The github plugin is what creates the tag and the GitHub Release.",
    "evidence_against": "Only small points. semantic-release authenticates with secrets.GITHUB_TOKEN (line 451), not the App token. So \"with the App token\" is true only for the auto-merge arm. arm-auto-merge can also merge the PR directly when arming fails (line 371). The config has no @semantic-release/git plugin, so the CHANGELOG/package.json changes are never pushed back to the repo; only the tag and Release reach GitHub.",
    "correction": "For clarity, write the label as: \"arm-auto-merge (PR events): gh pr merge --auto --squash with the GitHub App token; deploy (push to main): semantic-release with GITHUB_TOKEN cuts the tag and GitHub Release\". Consider splitting it into two edges, one per job and token."
  },
  {
    "label": "refute:ops:64",
    "element": "#64 github → mp_events: push main, issues labeled, issue_comment created, dependabot pull_request opened, workflow_dispatch",
    "grounded": true,
    "evidence_for": "The on: block in /home/user/skynet-capital/.github/workflows/moneypenny-events.yml (line 53) lists exactly five triggers: issue_comment types [created] (62-63), push branches [main] (78-79), issues types [labeled] (80-81), pull_request types [opened] (89-90), and workflow_dispatch with a command input (91-93). The trigger itself fires on any opened PR. The job-level guard at line 133 (`github.event.pull_request.user.login == 'dependabot[bot]'`) narrows it to Dependabot, so in practice only Dependabot PRs get acted on. The comment at 82-88 (#3315, dep-warden) confirms that intent.",
    "evidence_against": "The workflow trigger is a plain `pull_request: [opened]`, so a run starts for every opened PR. The Dependabot restriction comes from a job `if:` (line 133), not from the on: block. Also, the route job's handlers are narrower than the labels suggest: `issues labeled` is only acted on for label=feedback (line 387) or plan, and `issue_comment` only for plan ready-flips.",
    "correction": "Optional precision: label the edge \"pull_request opened (job-gated to dependabot[bot], line 133)\" and \"issues labeled (feedback/plan)\". The label as written would not mislead a build session."
  },
  {
    "label": "refute:ops:63",
    "element": "CLAIM #63 pipeline → bots: Deploys only when bot-relevant, reusing the dashboard image; smokes; rolls back",
    "grounded": true,
    "evidence_for": "The claim holds. .github/workflows/pipeline.yml:503-629 defines the `deploy-bots` job, which declares `needs: deploy` (line 518) and runs on a push to main after deploy succeeds, or on a workflow_dispatch with force_bots_deploy (lines 519-523). The preflight step at line 553 runs scripts/bots-deploy-preflight.mjs, which imports `classify` from scripts/bot-relevant.mjs (preflight.mjs:19). The \"Resolve image\" step (lines 560-573) reuses `needs.deploy.outputs.image`, which is the dashboard image. On the push path it refuses to rebuild if that image is missing (line 572). The \"Deploy bots\" step (line 610) runs `flyctl deploy --image`. The smoke test runs scripts/smoke-bots.sh (line 617). The rollback step (lines 621-627) redeploys the previous image when the smoke test fails. All three cited files exist.",
    "evidence_against": "\"Only when bot-relevant\" leaves some gates out. The preflight's decide() (bots-deploy-preflight.mjs:33-65) also checks, in order: whether a token is present, a pre-cutover skip, a force override, a GIT_SHA baseline diff that deploys when unsure, and a cooldown debounce. So a bot-relevant push can still be skipped (debounced or pre-cutover), and a push that is not bot-relevant can still deploy (forced, or no baseline). Rollback only happens if a previous bots image exists. On the dispatch path, the image is looked up from the running dashboard app instead of coming from this run's build.",
    "correction": "Optional refinement: \"Deploys (after deploy succeeds) when the preflight says so — bot-relevant diff vs the machine's stamped GIT_SHA, debounced, deploys when unsure — reusing the dashboard image; smoke-bots.sh; rolls back to the previous bots image on smoke failure\". The current label would not mislead a build session."
  },
  {
    "label": "refute:ops:65",
    "element": "mp_events → ccp (Claude Code sessions via claude-code-action): build-feedback, build-plan, build-events matrix legs, dep-warden",
    "grounded": true,
    "evidence_for": "/home/user/skynet-capital/.github/workflows/moneypenny-events.yml has exactly five jobs: dep-warden (L131), route (L170), build-events (L416), build-feedback (L589), build-plan (L678). Every job except route launches a session through anthropics/claude-code-action pinned @cfc3eb22 (v1): dep-warden L153, build-events L473 (one per matrix leg, matrix at L449, per-leg matrix.event.id L514), build-feedback L612, build-plan L701. Each leg first mints an app token (./.github/actions/app-token) and gates on CLAUDE_CODE_OAUTH_TOKEN (./.github/actions/oauth-token-gate), e.g. L141/147, L460/466.",
    "evidence_against": "The claim has only small gaps. route (L170) makes no session call. It is a Node-based dispatcher that sends a workflow_dispatch back to this same workflow (L369), because claude-code-action rejects push events. So build-events legs only ever run on workflow_dispatch. dep-warden runs only on Dependabot pull_request events and has no route dependency or claim lease.",
    "correction": "Optional: add the tech detail to the edge label, e.g. \"anthropics/claude-code-action@v1 (pinned cfc3eb22), OAuth token\". Also say that build-events legs are fanned out per due event through a route → workflow_dispatch re-fire, and that route itself makes no session call."
  },
  {
    "label": "refute:ops:66",
    "element": "mp_events → github: Claim leases, receipt issues, labels, screen PRs, re-dispatch",
    "grounded": true,
    "evidence_for": "Every verb in the label shows up in the cited code. Claim leases: scripts/moneypenny/index.mjs:126-230 (claimHandoff writes the git ref and tag-object lease, releaseClaim at :191, the read-only check at :202). Receipt issues: index.mjs:333-345 (routeSweep dedupes receipt issues) and the `gh issue create` call around :711-714 (ensureLabel then \"create\"). Labels: ensureLabel/ensureVocabulary imported from labels.mjs (index.mjs:57, labels.mjs:167-170), called at index.mjs:573, 593, 621 and 711. Screen PRs: scripts/moneypenny/open-screen-pr.mjs:51-58 (openPr, a REST POST to /pulls), plus auto-merge over GraphQL (:66) and a merge fallback (:75). The workflow runs it at moneypenny-events.yml:337-341. Re-dispatch: moneypenny-events.yml:373 `gh workflow run moneypenny-events.yml --ref \"$GITHUB_REF_NAME\" -f command=scan`, with actions: write at :109. index.mjs:597 and :654 also run `gh workflow run` for the conflict/stall re-fire.",
    "evidence_against": "The label leaves some GitHub traffic out. Moneypenny also posts stall, conflict and needs-eric comments and flags (index.mjs:573-621), sweeps issues and closes shipped ones (shippedSweep at :387), and feedback-guard.mjs and repair.mjs call ensureLabel too. The GitHub calls go through the gh CLI wrapper (gh.mjs) and through raw REST/GraphQL fetch in open-screen-pr.mjs. None of this would mislead a build session.",
    "correction": "Optional: add \"comment/close issues\" to the label and write the technology as \"gh CLI + REST/GraphQL\". Also cite scripts/moneypenny/labels.mjs and scripts/moneypenny/gh.mjs as evidence."
  },
  {
    "label": "refute:ops:67",
    "element": "mp_events → mp_repair: Hands over conflicted PRs and stalled research issues",
    "grounded": true,
    "evidence_for": ".github/workflows/moneypenny-events.yml:221 runs `node scripts/moneypenny/index.mjs --audit` on push. The audit()'s conflict loop is at scripts/moneypenny/audit.mjs:136-168. It emits flag-conflict intents for conflicted PRs, and commentAndFlagConflict (scripts/moneypenny/index.mjs:590-610) then runs `gh workflow run moneypenny-repair.yml -f pr_number=N`. For stalled research issues, audit.mjs:70-80 emits flag-stall for unclaimed [event-research] receipt issues. stallRepairDispatch at index.mjs:649-664 builds one batched `gh workflow run moneypenny-repair.yml -f issue_number=a,b,c`, and dispatchEventStallRepair at index.mjs:680-692 fires it. moneypenny-repair.yml:33-40 and :215 describe these entry points. The handover is a real workflow_dispatch from events to repair.",
    "evidence_against": "The two citations are slightly off. CONFLICT_REPAIR_CAP (audit.mjs:16, value 3) is the limit where handing over STOPS: once it is spent, commentAndFlagConflictCap (index.mjs:618-623) sends the PR to needs-eric and does not dispatch repair. So the cap is not evidence of the handover. The function that actually sends conflicted PRs is commentAndFlagConflict. Also, stallRepairDispatch only builds the plan without making the call. dispatchEventStallRepair makes the call, and it batches every stalled issue into one dispatch per audit run.",
    "correction": "Change the evidence to scripts/moneypenny/index.mjs commentAndFlagConflict (pr_number dispatch) plus stallRepairDispatch/dispatchEventStallRepair (one batched issue_number dispatch per audit run), triggered by the `--audit` step in moneypenny-events.yml. Suggested label: \"workflow_dispatch moneypenny-repair.yml: conflicted PRs (pr_number, up to CONFLICT_REPAIR_CAP=3 attempts, then needs-eric) and stalled [event-research] issues (batched issue_number)\"."
  },
  {
    "label": "refute:ops:68",
    "element": "CLAIM #68 github → mp_repair: workflow_run completed (failure on main) for the five watched workflows",
    "grounded": true,
    "evidence_for": ".github/workflows/moneypenny-repair.yml:59-66: workflow_run.workflows lists five names: Moneypenny Events, Pipeline, Autonomy ops, Fly logs, Companion eval (plus the path form of each), with types: [completed]. Line ~103: the triage job's `if: github.event.workflow_run.conclusion == 'failure'`. The main-only filter lives in the router: scripts/moneypenny/repair.mjs:95 (conclusion !== failure → no-op) and :98 (head_branch !== defaultBranch||'main' → no-op). scripts/repair-watchlist-scan.mjs:36-61 parses the same list and flags any workflow in .github/workflows/ that is missing from it (7 workflow files: the 5 watched, moneypenny-repair itself which is exempt, and claude.yml).",
    "evidence_against": "The \"on main\" filter is not in the workflow trigger. GitHub delivers every completed run on any branch, and the job-level `if` checks only conclusion=failure. The default-branch guard runs inside scripts/moneypenny/repair.mjs:98 after a runner has already started. repair-watchlist-scan.mjs is a drift gate that checks the list stays complete. It does not take part in this runtime relationship. It is fair to cite it as evidence of the list, but it is not a runtime participant. mp_repair also has a second trigger, workflow_dispatch with pr_number/issue_number, which comes from the moneypenny-events router rather than from a github workflow_run. That is a separate edge.",
    "correction": "Keep the edge. Optionally note that the main/default-branch filter and the loop guards run in scripts/moneypenny/repair.mjs (lines 95-98), not in the trigger. Show repair-watchlist-scan.mjs as a CI gate that keeps the watch list complete, not as part of the runtime flow."
  },
  {
    "label": "refute:ops:69",
    "element": "mp_repair → ccp: Repair sessions (evidence: moneypenny-repair.yml repair job, three mutually exclusive steps)",
    "grounded": true,
    "evidence_for": "/home/user/skynet-capital/.github/workflows/moneypenny-repair.yml: the `repair` job (lines 122-130) has three steps that each run anthropics/claude-code-action@cfc3eb22 (pinned v1). Step 1 (line 156) is the CI-failure repair, guarded by `steps.gate.outputs.armed == 'true' && needs.triage.outputs.issue != ''`. Step 2 (line 195) is the PR conflict repair, guarded by `workflow_dispatch && inputs.pr_number != ''`. Step 3 (line 220) is the stalled event-research issue diagnosis, guarded by `workflow_dispatch && inputs.issue_number != ''`. All three are gated on the CLAUDE_CODE_OAUTH_TOKEN oauth-token-gate (line 148), and they use the App token from line 142 to push and open PRs. So the edge is real: Moneypenny Repair calls the Claude Code action to start repair sessions.",
    "evidence_against": "The steps are only mostly mutually exclusive. Step 1 runs only on a workflow_run event, because triage is skipped on dispatch, so it can never overlap with steps 2 or 3. Steps 2 and 3 have separate conditions, though: a workflow_dispatch that sets both pr_number and issue_number would run both sessions in the same job. Nothing in the workflow rejects that input combination. The comment at lines 126-129 only claims the workflow_run and dispatch paths are exclusive.",
    "correction": "Label is fine. If the diagram shows the three variants, write \"three steps (CI-failure repair / PR conflict repair / stalled-issue diagnosis)\". Only the CI-failure step is exclusive of the other two. The two workflow_dispatch steps (pr_number vs issue_number) can both run if a dispatch sets both inputs."
  },
  {
    "label": "refute:ops:70",
    "element": "CLAIM #70 mp_repair → github: \"Files the ci-failure capsule, opens the repair PR\"",
    "grounded": true,
    "evidence_for": "scripts/moneypenny/repair.mjs:50 signature(), :59 issueBody(), :103-111 an open-issue intent with body issueBody(run, failure). :212-213 a recurrence runs gh issue comment. :265 gh issue list dedupes by signature. .github/workflows/moneypenny-repair.yml: the triage job's router step sets GH_TOKEN=secrets.GITHUB_TOKEN and runs node scripts/moneypenny/repair.mjs. The permissions block includes \"issues: write # file the capsule\" and \"pull-requests: write # the repair session opens the PR\". .github/prompts/moneypenny-ci-repair.md:43 says \"Open the PR with gh pr create\".",
    "evidence_against": "repair.mjs itself never opens a PR. It only files or comments on the capsule issue, using the ambient GITHUB_TOKEN. The PR is opened in the separate repair job by the claude-code-action session. That session follows .github/prompts/moneypenny-ci-repair.md and uses the GitHub App token (GH_TOKEN: steps.app-token.outputs.token), not the triage GH_TOKEN. The claim is also incomplete. The lane comments recurrences instead of filing again. It has two more workflow_dispatch entry points: conflict-repair on a PR, and event-stall repair on an issue.",
    "correction": "If mp_repair stands for the whole moneypenny-repair.yml lane, the edge is correct. It would help to split it into two: (1) the triage step, repair.mjs using GITHUB_TOKEN, which files or dedupes the ci-failure capsule issue and comments on recurrences; (2) the repair job, a Claude session using the App token and the .github/prompts/moneypenny-ci-repair.md instructions, which runs gh pr create to open the repair PR. The App token matters because it makes pull_request.opened fire, so verify actually runs. Do not attribute PR creation to repair.mjs."
  },
  {
    "label": "refute:ops:71",
    "element": "CLAIM #71 (ops / relationship): github → claude_lane: \"Member comment on an issue or PR\". Evidence: the claude.yml gate job.",
    "grounded": true,
    "evidence_for": "In /home/user/skynet-capital/.github/workflows/claude.yml, lines 86-92 set the triggers: issues [opened, labeled], issue_comment [created, edited] and pull_request_review_comment [created, edited]. The gate job's `if` at lines 122-130 accepts any issue_comment or pull_request_review_comment without needing an @claude mention. It then excludes sender claude[bot] and any body containing the '[Claude Code](https://claude' footer, and it requires author_association to be OWNER, MEMBER or COLLABORATOR. The header (lines 3-6 and 8-18) calls this \"THE HUMAN-DIRECTED LANE\": a comment from a repo member starts or steers a Claude Code session. Line 104 sets concurrency cancel-in-progress to true, which is how a new comment steers a run already in progress. The claim's substance holds: a member's comment triggers the lane, and the gate job is the right evidence.",
    "evidence_against": "The label leaves out some details. (1) A brand-new issue whose body contains @claude also triggers the lane (line 126). The `labeled` event is subscribed too, but it only passes the gate when the issue body contains @claude. (2) \"Member\" here means OWNER, MEMBER or COLLABORATOR author_association, not org membership only. (3) Comments carrying the Claude Code footer, and comments from claude[bot], are excluded. (4) The lane does nothing unless the CLAUDE_CODE_OAUTH_TOKEN secret is set: the downstream job only runs when needs.gate.outputs.armed == 'true' (line 149). (5) New and edited comments both count, and a new one cancels the run in progress on the same issue or PR, so the lane steers work as well as starting it.",
    "correction": "Suggested label: \"Member (OWNER/MEMBER/COLLABORATOR) comment, new or edited, on an issue or PR review thread, or a new issue with @claude in its body. Starts or steers the session (cancel-in-progress); inert until CLAUDE_CODE_OAUTH_TOKEN is set.\" Evidence: .github/workflows/claude.yml:86-92 (triggers), :103-104 (concurrency), :122-130 (gate if)."
  },
  {
    "label": "refute:ops:72",
    "element": "CLAIM #72 ccp → secretary: Fires the daily digest Routine",
    "grounded": true,
    "evidence_for": "docs/ROUTINES.md:37 is the \"## Active\" heading. The table row at docs/ROUTINES.md:42 reads \"**Secretary digest** | claude.ai Routine · daily 12:00 (`trig_01KaMC2uR3cFW5XTUL6rzPuS`, repo-wired 2026-08-19)\". On each fire the Routine runs `node scripts/digest-scan.mjs --due`, then follows `.claude/skills/secretary`. So the claude.ai Routine platform (ccp) does fire the secretary digest every day. Line 48 also calls the digest's daily clock \"the one surviving exception\" among the schedules.",
    "evidence_against": "The fire is gated. When `digest-scan.mjs --due` returns due:false, the Routine replies \"no digest due\" and stops, so not every fire produces a digest. The same Routine also runs four scans alongside the digest: config-audit, comment-bloat-scan, incident-scan and doctrine-scan --due. The label leaves out that the digest ships through /ship and sends a push notification.",
    "correction": "Optional wording: \"Fires daily 12:00 digest Routine (trig_01KaMC2u…; gated by digest-scan --due; also runs the config, comment-bloat, incident and doctrine audits)\". As labelled now, the element would not mislead a build session."
  },
  {
    "label": "refute:ops:74",
    "element": "secretary → eric: Push-notifies the needs-you count and top headline",
    "grounded": true,
    "evidence_for": "/home/user/skynet-capital/docs/ROUTINES.md:42. The \"Secretary digest\" row's May do column reads \"write `docs/digests/<date>.md` from its TEMPLATE, ship via /ship, push-notify the Needs-you count + top headline\". ROUTINES.md:47-49 says the same thing another way: the digest's daily clock survives because \"its deliverable is the push notification, which only a claude.ai Routine can send\". Line 18 names the phone push to Eric as a claude.ai-side capability.",
    "evidence_against": "The doc says the push is something the routine may do, not a guarantee. When digest-scan reports due:false, the routine replies \"no digest due\" and stops, so no push goes out. The sender is the claude.ai Routine trig_01KaMC2uR3cFW5XTUL6rzPuS running the secretary skill, not a repo-resident component. I did not check any code that actually sends the push; the evidence here is documentation only.",
    "correction": "Optional precision: label the edge \"push-notifies (claude.ai Routine, daily, only when a digest is due): Needs-you count + top headline\". It should also show that the sender is the claude.ai Routine, not a repo process."
  },
  {
    "label": "refute:ops:73",
    "element": "#73 secretary → ledgers: Writes docs/digests/<date>.md and ships it",
    "grounded": true,
    "evidence_for": ".claude/skills/secretary/SKILL.md:52-53 says \"**Deliver:** write `docs/digests/<YYYY-MM-DD>.md`, ship via `/ship` (docs auto-merge), and push-notify with the Needs-you count + top headline only.\" docs/digests/TEMPLATE.md exists (header \"# Digest — <YYYY-MM-DD>\", filename is the date). There are real dated digests on disk: docs/digests/2026-08-15.md through 2026-09-08.md and later.",
    "evidence_against": "The edge leaves out two things. Shipping goes through /ship (scripts/ship.sh), which opens a PR that auto-merges, so the file is not written straight onto main. Delivery also includes a push notification to Eric. Neither of these makes the arrow wrong.",
    "correction": "Optional: label it \"Writes docs/digests/<YYYY-MM-DD>.md (from TEMPLATE.md) and ships it via /ship (auto-merged docs PR)\", and add a separate secretary → Eric edge for the push notification (Needs-you count + top headline)."
  },
  {
    "label": "refute:ops:75",
    "element": "CLAIM #75 mp_events → ledgers: \"Research write-ups and screen rows land via PRs\"",
    "grounded": true,
    "evidence_for": ".github/prompts/event-research.md:18-20: never-assessed events produce docs/research/events/<id>.md, and interval-elapsed events append one ledger row. Lines 82-84 and 109: ship ONE PR on branch research/<event-id>, then arm auto-merge through scripts/ship.sh. .github/workflows/moneypenny-events.yml:285-286: a screen \"writes its own ledger row and lands it via a PR, never a direct push to main\" (issue #915). Lines 298-352 (step id `screen`): runs scripts/event-material-scan.mjs --screen-due, creates branch moneypenny/screen-<run_id>-<attempt>, commits docs/research/events/*.md, pushes, and calls scripts/moneypenny/open-screen-pr.mjs, which opens the PR and tries to arm auto-merge (file header line 2). Both cited evidence sources exist and support the claim.",
    "evidence_against": "Only minor gaps. (1) The workflow runs the screen itself as a deterministic node script, not an LLM session. The research write-ups come from a claude-code-action session (build-events) driven by the prompt. (2) The ledgers are markdown files in the repo (docs/research/events/<id>.md), and research sessions also append rows to docs/research/forward-tests/<event-id>.md (prompt lines 48-56). (3) The screen PR is skipped when another screen PR is already open (--check-open, yml:337-340), so a screen can end without writing anything that tick.",
    "correction": "Change the label to name what actually does the writing and where it goes: \"Research session (research/<event-id> branch) and deterministic screen (moneypenny/screen-* branch) → PR with auto-merge → docs/research/events/<id>.md ledgers (+ docs/research/forward-tests/<id>.md)\". Mark the ledger target as markdown files in the repo, not a datastore."
  },
  {
    "label": "refute:ops:76",
    "element": "CLAIM #76 gates -> ledgers: Ratchets budgets down after a correction lands",
    "grounded": true,
    "evidence_for": "docs/COACHES.md:328 says \"Budgets **only ratchet down** (`--update` after a correction lands)\". scripts/dead-scan.mjs:11 documents `--update # rewrite dead-budget.json (ratchet: only lower)`. The code at lines 63-71 reads BUDGET_FILE=dead-budget.json (line 20) and writes `Math.min(prev, debt)` with writeFileSync. The same shape exists across the other gates, each with its own root ledger: dead-budget.json, dupe-budget.json, spec-gap-budget.json, doc-rot-budget.json, clone-budget.json, dep-graph-budget.json, comment-bloat-budget.json, doctrine-budget.json, incident-budget.json, forward-test-id-budget.json, ci-install-duration-budget.json and others. The gate also reads the ledger and fails when debt exceeds budget (dead-scan.mjs:81-87). COACHES.md:335-338 also says each coach's --update rewrites one shared budget file, once per landed wave.",
    "evidence_against": "The write is not automatic. It only happens when someone runs the scanner with `--update` by hand, after the fix lands. Plain gate runs only read the ledger and compare against it. arch-scan is an exception: its legacy over-cap files live in arch-grandfather.json, not in a numbered budget. The label should not suggest that the ratchet runs every time in CI.",
    "correction": "Label the edge \"--update (manual, after a correction lands): writes min(prev, debt) to *-budget.json\", and add a separate read edge ledgers -> gates: \"CI compares debt to budget; fails if it grew\". Note that arch-scan uses arch-grandfather.json instead of a numbered budget."
  },
  {
    "label": "refute:ops:78",
    "element": "CLAIM #78 ops_buttons -> bots: flip-mode, set-playbooks, set-hardcore, logs (autonomy-ops.yml steps)",
    "grounded": true,
    "evidence_for": "/home/user/skynet-capital/.github/workflows/autonomy-ops.yml is a workflow_dispatch \"Autonomy ops\" workflow for the always-on bots process (lines 1-8). Its choice options at lines 24-32 include logs, flip-mode, set-playbooks and set-hardcore. The steps: logs runs `flyctl logs -a \"$TARGET_APP\"` (l.119-120); set-playbooks runs `flyctl secrets set SKYNET_PLAYBOOKS` (l.149-163); set-hardcore runs `flyctl secrets set SKYNET_HARDCORE_BOTS` (l.189-198); flip-mode runs `flyctl secrets set SKYNET_AUTONOMOUS_MODE=observe|live` (l.224-227). By default target_app is skynet-capital-bots (l.41-43). Every step runs through flyctl with FLY_API_TOKEN_BOTS or the org token, behind the autonomy-ops GitHub Environment's required reviewers (l.6).",
    "evidence_against": "The edge is not a direct call into the bots process. The workflow goes through the Fly API (flyctl): it sets Fly secrets on the app, which causes a machine restart and env reload, and it reads logs through Fly. It does not use the controls bridge. The label also leaves out other actions: status, machine-status, bootstrap-bots-app, set-beta-forcing, and the preflight step that checks the old skynet-capital bots group before a live flip (l.205-219). target_app can also be skynet-capital, not only the bots app.",
    "correction": "Label the edge \"GitHub Actions workflow_dispatch -> Fly API (flyctl secrets set / logs), gated by the autonomy-ops environment reviewers\" rather than implying a direct call to bots. Optionally add set-beta-forcing, status/machine-status and bootstrap-bots-app, and note that the default target is skynet-capital-bots with skynet-capital still selectable. Bot Alpaca credentials are NOT set through this path: they arrive through the app and the controls bridge (l.10-15)."
  },
  {
    "label": "refute:ops:77",
    "element": "CLAIM #77 (ops / relationship): bots → dashboard: Controls and insight bridge",
    "grounded": true,
    "evidence_for": "fly.bots.toml:42 app = \"skynet-capital-bots\"; fly.bots.toml:53-55 says the URL \"still points at the DASHBOARD app\" and sets SKYNET_INSIGHTS_BRIDGE_URL = \"http://app.process.skynet-capital.internal:8788\". src/autonomous/bot-controls-client.ts:24-25 describes the bots process polling `/controls` on the internal bridge, \"same origin as the insight relay, SKYNET_INSIGHTS_BRIDGE_URL\"; :81-83 builds the endpoint from env.SKYNET_INSIGHTS_BRIDGE_URL + CONTROLS_BRIDGE_PATH. src/server/insights-listener.ts:97-102 uses default port 8788; :153-168 routes /controls, bot-credentials, /decisions and /insights. The listener runs on the dashboard side: src/scripts/dashboard-insights-bridge.ts:26/90 (startInsightsBridge → createInsightsListener) is called from src/scripts/dashboard-ops-status.ts:62. The call direction is bots → dashboard: bots GETs /controls and POSTs /insights and /decisions.",
    "evidence_against": "The label leaves out part of what the bridge carries. The same listener and URL also serve the bot-credentials route (src/autonomous/bot-credentials-client.ts, BOT_CREDENTIALS_PATH) and POST /decisions for decision replication (src/autonomous/decision-replication-client.ts, insights-listener.ts:19,163). The actual insight POST client is src/autonomous/insight-bridge-client.ts, which the claim does not cite. The transport detail is also missing: internal HTTP on 6PN port 8788 with a shared-secret header.",
    "correction": "Suggested label: \"bots → dashboard (app process): internal HTTP bridge over Fly 6PN :8788 (SKYNET_INSIGHTS_BRIDGE_URL, shared-secret header). Bots GET /controls and POST /insights, /decisions and bot-credentials.\" Add the evidence src/autonomous/insight-bridge-client.ts, src/autonomous/decision-replication-client.ts and src/scripts/dashboard-insights-bridge.ts (the listener is started from dashboard-ops-status.ts:62)."
  }
]
