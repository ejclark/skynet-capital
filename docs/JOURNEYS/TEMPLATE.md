# Journey — <the question, as a short name>

_Started <YYYY-MM-DD>. Status: **open | closed** — <one line: what would close it, or what closed it>._

<!-- A reasoning journey: the record of a long exchange where the thinking was the output.
     Format + intent: .claude/skills/journey/SKILL.md. Target is RECONSTRUCTABILITY, not
     completeness — write during or immediately after the exchange, never "later".
     Validated leniently by scripts/journey-scan.mjs (headings + filename only — the contract
     never taxes the capture habit). Delete comments when instantiating. -->

---

## The question, verbatim

> <the original ask, unedited — typos, fragments and all; it is the only uncontaminated record>

---

## Where it stands

<!-- The 30-second stop: conclusion / ratings / build order BEFORE the reasoning. Any number
     carries its evidence basis ("ordinal judgments with reasoning attached — not measurements").

     OPTIONAL one small picture, if it genuinely compresses the state — a journey that fires
     beats a journey that's pretty. NEVER mermaid's `journey` type (a UX-satisfaction chart —
     wrong shape for reasoning). Tables must fit a phone: ≤3–4 columns up top.

     Snippet A — the open-forks map (forks as diamonds, what-settles-each on the edges):
     ```mermaid
     flowchart LR
         Q{<the fork>} -- "<what would settle it>" --> A[<option A>]
         Q -- "<what would settle it>" --> B[<option B>]
     ```

     Snippet B — the spine scoreboard (before the prose):
     | # | Claim | Challenged by | Where it landed |
     |---|---|---|---|
     | 1 | <claim> | <the challenge> | <what survived> |
-->

---

## What moved, and what moved it

<!-- The spine — write this FIRST, it decays fastest. One entry per claim that moved.
     Record corrections in BOTH directions: a journey where only one side was ever wrong
     is a sales document, not an instrument. -->

### 1. "<the claim, quoted or compressed>"

**Claim** — <the position held, and by whom>. **Challenge** — <what was put to it, and by whom>.
**Resolution** — <what survived — including "X was right, Y overcorrected">.

## Rejected branches

<!-- The highest-value section: what was considered and killed, WITH the argument that killed it.
     A killed branch re-proposes itself every six months until the why is written down.
     Deferred-not-killed? Say the condition that revives it. -->

- **<branch>** — killed by: <the argument>.

## Open forks

<!-- Only useful paired with what would close each: a measurement, an experiment, a threshold. -->

- **<question>** — settled by: <the thing that would close it>.

## Side quests banked

<!-- Logged to docs/IDEAS.md, referenced here — never duplicated, never built mid-journey. -->

- <one-liner> → docs/IDEAS.md _(src: <Eric | Claude> · while: journey on <topic>)_
