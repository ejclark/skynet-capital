{
  "scripts/moneypenny/index.mjs": "pinned by tests/arch/lease-namespace.spec.ts + tests/arch/gh-json-fields.spec.ts, which assert literal source text",
  "src/observatory/shell-style.ts": "a single CSS template literal — no natural split without duplicating the stylesheet",
  "src/server/auth/authenticator.ts": "dominated by one ~2600-line inline HTML template literal (the cinematic /login page)"
}
